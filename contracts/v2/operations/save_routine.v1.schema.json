{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sonny.app/contracts/v2/operations/save_routine.v1.schema.json",
  "title": "save_routine v1",
  "description": "Save a named routine as a goal. Each run is a new task that plans the goal again, so write the goal the way the user would say it. Floor effect: create.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100
    },
    "goal": {
      "description": "What the routine does, in plain words; every run plans it again.",
      "type": "string",
      "minLength": 1,
      "maxLength": 4000
    },
    "schedule": {
      "description": "When it runs, in the user's words, if they said.",
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    }
  },
  "required": [
    "name",
    "goal"
  ],
  "additionalProperties": false
}
