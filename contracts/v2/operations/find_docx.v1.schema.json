{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sonny.app/contracts/v2/operations/find_docx.v1.schema.json",
  "title": "find_docx v1",
  "description": "List the .docx files in a folder and its subfolders that don't have a PDF yet. Changes nothing. Floor effect: observe.",
  "type": "object",
  "properties": {
    "folder": {
      "description": "A path on the Mac, as the user said it or as an earlier step reported it.",
      "type": "string",
      "minLength": 1,
      "maxLength": 1024
    }
  },
  "required": [
    "folder"
  ],
  "additionalProperties": false
}
