### Plaud and Circle, read from their own help centres (new 2026-09-17, SONNY-512)

Packaged app (`./scripts/package-app.sh`, then `open .build/arm64-apple-macosx/debug/MacAgent.app`). No sign-in is needed for any row below, and Sonny must not be asked to sign in anywhere: every row stops at the first approval. These check the plan only: deny at that approval and read what Sonny planned on the Tasks page. Both packs' flows come from help-centre articles anybody can re-open, so both catalogue rows say `deep`.

- [ ] On the Skills page, **add Plaud**. It shows two tasks, both on Plaud Web: creating, renaming or deleting a folder, and exporting a recording, transcript, summary or mind map. Each names a `support.plaud.ai` article as its source. Ask the widget: `export my latest plaud transcript as a docx with speaker labels`. Deny at the first approval. The plan starts at `https://web.plaud.ai/`, opens the file, clicks the **Share** icon in the top-right corner, chooses the transcript, picks **DOCX**, turns on speaker labels and ends at **Export**. **The plan should carry the pack's own caveat: signed out, `web.plaud.ai` opens a sign-in page, and the steps are on the library shown after signing in.** **What would be a finding:** a plan that tries to sign in for you, that offers **WAV** (the help page says Plaud Web exports audio as MP3 only), or that starts somewhere other than `web.plaud.ai`.
- [ ] **Add Circle.** Its one task is creating a post in a post space. Ask the widget: `write a welcome post in my circle community`. Deny at the first approval. The plan starts at `https://login.circle.so/sign_in` and says you open your community once signed in. It then goes to the post space from the sidebar, clicks **New post**, fills the **Title** field and the body, and ends at **Publish**. **What would be a finding:** a plan that types into the sign-in form, that clicks **Sign up**, or that picks **Schedule post** or **Save draft** when you did not ask for them. The help page says only admins and moderators have those two options.
- [ ] Then **remove both** from the Skills page and ask the Plaud command again. The plan no longer names the pack's steps, such as the **Share** icon. That shows the rows above came from the pack and not from the model's own knowledge of these sites.
