### Two of this group's newly deep sites, used in a real command (new 2026-09-17, SONNY-503)

Setup: the packaged app — `./scripts/package-app.sh` then
`open .build/arm64-apple-macosx/debug/MacAgent.app`. No sign-in anywhere is needed to check
these two rows; what is being checked is that Sonny knows the steps, not that it can take them.

- [ ] Ask the widget something about Luma — "how do I create an event on Luma" — and Sonny's
      answer walks the real create flow: the title, the date and timezone, the event type
      (In-Person, Online or Hybrid), the cover image and theme, the calendar the event belongs
      to, the Public / Private / Member-Only visibility choice, and Create Event at the end.
- [ ] Ask about Semrush — "set up position tracking in Semrush" — and the answer has the two
      configuration steps the help centre has, Targeting then Keywords: scope, search engine,
      one device, location, then the keywords, then the green Start Tracking button.
- [ ] Neither answer asks you to sign in, type a password, or touch anything to do with money,
      a plan or a card.

### The three sites in this group that are still facts alone (new 2026-09-17, SONNY-503)

No setup. This is a row to read rather than press, so that a later pass does not re-report it
as a gap.

- [ ] Metricool, TikTok Ads and LinkedIn Ads still answer with facts and no steps, and that is
      the recorded state rather than a defect: each is named on SONNY-503 with what was
      measured. Nothing in the app should look broken — a facts-only pack is the shallow depth
      the Skills page has always had.
