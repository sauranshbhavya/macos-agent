### Four packs whose flows were read off the live site, not off documentation (new 2026-09-17, SONNY-512)

Packaged app (`./scripts/package-app.sh`, then `open .build/arm64-apple-macosx/debug/MacAgent.app`). No sign-in is needed for any row below, and Sonny must not be asked to sign in anywhere: every row stops at the first approval. These check the plan only — deny at that approval and read what Sonny planned.

- [ ] On the Skills page, **add Canva**. It shows two tasks — creating a design from a template, and starting one from scratch at a custom size — each carrying the Canva help page its steps came from. Ask the widget: `make a poster in canva from a template`. Deny at the first approval. On the Tasks page, that task's plan starts at `https://www.canva.com/` and follows the pack's own steps — the homepage search bar, **Templates**, then **Customize this template** — rather than searching the web for Canva. **What would be a finding:** a plan that names no Canva URL, or reaches the editor by a path the pack does not describe.
- [ ] **Add Grok.** Its one task is asking a question, and its source is `https://grok.com/` itself — the page, not an article about the page. Ask the widget: `ask grok.com what the weather is like in tokyo`. Deny at the first approval. The plan starts at `https://grok.com/`, types into the box labelled **Ask Grok anything**, and ends at **Submit**. **What would be a finding:** a plan that starts at a sign-in page, or that asks you to sign in before typing the question.
- [ ] **Add OpenRouter.** Its one task is finding a model in the catalogue. Ask the widget: `find the cheapest anthropic model on openrouter`. Deny at the first approval. The plan starts at `https://openrouter.ai/models` — not at the OpenRouter home page — and uses the **Search models…** box and the **List view**/**Table view** tabs the pack names. **What would be a finding:** a plan that goes to a sign-in page or to an account page instead of the catalogue.
- [ ] **Add Zoho Desk.** Its one task is merging duplicate tickets. Ask the widget: `merge these two duplicate tickets in zoho desk`. Deny at the first approval. The plan follows the cited steps — the **Tickets** module, select the **Department**, select the tickets, **Merge** from the **Mass Actions**, then **Yes, Merge** — and it states the requirement the page states, that only tickets in the same department can be merged. **What would be a finding:** a plan that merges across departments, or that invents a confirmation the page does not have.
- [ ] Then **remove all four** from the Skills page and ask the Canva command again. The plan no longer names the pack's steps, which is what proves the four rows above were the pack talking and not the model's own knowledge of these sites.
