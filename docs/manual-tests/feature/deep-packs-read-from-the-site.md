### Four packs read in a browser signed out — two from the product itself, two from its documentation (new 2026-09-17, SONNY-512)

Packaged app (`./scripts/package-app.sh`, then `open .build/arm64-apple-macosx/debug/MacAgent.app`). No sign-in is needed for any row below, and Sonny must not be asked to sign in anywhere: every row stops at the first approval. These check the plan only — deny at that approval and read what Sonny planned. Grok's and OpenRouter's flows were taken from the running product (`task_flow_docs` says `site`); Canva's and Zoho Desk's from their own help pages (`deep`).

- [ ] On the Skills page, **add Grok**. It shows two tasks — asking a question, and making an image or a video with Imagine — and the source of each is a Grok page rather than an article about Grok. Ask the widget: `ask grok.com what the weather is like in tokyo`. Deny at the first approval. On the Tasks page, the plan starts at `https://grok.com/`, types into the box labelled **Ask Grok anything** and ends at **Submit**. **What would be a finding:** a plan that starts at a sign-in page, that asks you to sign in before typing the question, or that looks for a **side navigation** — grok.com has none, and Imagine, Settings, Sign in and Sign up sit in one row across the top right.
- [ ] Ask the widget: `make me an image of a red bicycle with grok imagine`. Deny at the first approval. The plan starts at `https://grok.com/imagine`, describes the picture in the same **Ask Grok anything** box, chooses **Image** from the Image/Video/Agent row, and ends at **Submit**. **What would be a finding:** a plan that starts at `grok.com/` and navigates to Imagine, when the pack gives the Imagine page as the start.
- [ ] **Add OpenRouter.** Its one task is finding a model in the catalogue. Ask the widget: `find an anthropic model on openrouter`. Deny at the first approval. **The plan starts at `https://openrouter.ai/models` and does not pass through the OpenRouter home page first** — the pack's own first step says so. It uses the **Search models…** box, and the **All / Text / Image / Video / Speech / Transcription / Embeddings / Rerank** tabs or the **List**/**Table** pair. **What would be a finding:** a plan whose first step is "go to openrouter.ai and click Models", which is what the flow used to say and what its start page contradicted.
- [ ] **Add Canva.** Two tasks — creating a design from a template, and starting one from scratch at a custom size — each carrying the Canva help page its steps came from. Ask the widget: `make a poster in canva from a template`. Deny at the first approval. The plan starts at `https://www.canva.com/` and follows the help article's steps — the homepage search bar, **Templates**, then **Customize this template**. **The plan should carry the pack's own caveat: those controls are on the signed-in Canva homepage.** Signed out, `canva.com` is a marketing page with no search box on it at all and no **Custom size** control, so a plan that promises to type into a search bar on the page a signed-out visitor sees is describing something that is not there.
- [ ] **Add Zoho Desk.** Its one task is merging duplicate tickets. Ask the widget: `merge these two duplicate tickets in zoho desk`. Deny at the first approval. The plan follows the cited steps — the **Tickets** module, select the **Department**, select the tickets, **Merge** from the **Mass Actions**, then **Yes, Merge** — and states the requirement the page states, that only tickets in the same department can be merged. **What would be a finding:** a plan that merges across departments, or that invents a confirmation the page does not have.
- [ ] Then **remove all four** from the Skills page and ask the Canva command again. The plan no longer names the pack's steps, which is what proves the rows above were the pack talking and not the model's own knowledge of these sites.
