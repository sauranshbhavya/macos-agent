### Chatwork, read from its own help centre (new 2026-09-18, SONNY-512)

Packaged app (`./scripts/package-app.sh`, then `open .build/arm64-apple-macosx/debug/MacAgent.app`). No sign-in is needed, and Sonny must not be asked to sign in anywhere: the row stops at the first approval. It checks the plan only: deny at that approval and read what Sonny planned on the Tasks page. The flow comes from a Chatwork help article anybody can re-open, so the row says `deep`.

- [ ] On the Skills page, **add Chatwork**. It shows one task, writing, editing or deleting a message in a chat, and its source is a `support-en.chatwork.com` article. Ask the widget: `send "running late, start without me" in my team's chatwork chat and mention Aiko`. Deny at the first approval. The plan starts at `https://www.chatwork.com/` and says you open the chat once signed in. It clicks the **To** button to pick the person, types into the box that reads **Enter your message here**, and ends at **Send**. **What would be a finding:** a plan that types into Chatwork's Login page, that clicks **Sign Up (Free)**, or that starts at `chatwork.com/login`. That address showed a signed-out visitor only "User wasn't found!", which is why the pack starts at `www.chatwork.com/`.
- [ ] Then **remove Chatwork** from the Skills page and ask the same command again. The plan no longer names the **To** button or the **Enter your message here** box. That shows the row above came from the pack and not from the model's own knowledge of Chatwork.
