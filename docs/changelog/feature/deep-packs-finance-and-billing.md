### Branch: feature/deep-packs-finance-and-billing
Status: in progress — the first session stopped at a green point about seventy minutes in (claimed 20:11 EDT, stopped 21:20); a relaunched session continues on this branch
Date: 2026-09-18
Tickets: SONNY-519 — deep Skills packs for the eighteen `finance_billing` sites; twelve converted in this session, three left out with the reason each cannot load, three still to finish. Also carries SONNY-526's one row for this group, Wise: its sign-in address `https://wise.com/login` was opened signed out and is right, so nothing changed.
Reviewed by: not yet — the fresh review follows the relaunched session's last commit.

Spec sections covered: none directly — this branch is Skills pack data for sites the catalogue already lists. The Skills mechanism (pack format, loader, content rules, trigger check, start-page rule) is SONNY-452's, SONNY-482's and SONNY-510's, and is untouched here.
Files changed: twelve pack files under `Sources/MacAgent/Resources/SkillPacks/` (`airwallex`, `clockify`, `dext`, `gocardless`, `lemon_squeezy`, `qonto`, `ramp`, `square`, `stripe`, `toggl_track`, `wave`, `wise`, each shallow to deep), `docs/sonny-skill-sites.tsv` (those twelve rows' `doc_url` cells), `docs/manual-tests/feature/deep-packs-finance-and-billing.md` (new) and this entry. No Swift source, no test, no other group's pack, no other row, no other column (`git diff --name-only 8f3d1d02 0c1f8341 -- Sources | wc -l` → 12, and the same diff without the path filter adds only the catalogue and this branch's two record files).
Tests: **Measured at `0c1f8341`**, with `git status --porcelain` empty. `env CLANG_MODULE_CACHE_PATH="$PWD/.build/clang-module-cache" swift test --disable-sandbox -Xswiftc -F -Xswiftc /Library/Developer/CommandLineTools/Library/Developer/Frameworks -Xlinker -rpath -Xlinker /Library/Developer/CommandLineTools/Library/Developer/Frameworks -Xlinker -rpath -Xlinker /Library/Developer/CommandLineTools/Library/Developer/usr/lib` -> exit 0, `Test run with 3475 tests in 251 suites passed after 95.714 seconds with 8 known issues.`, `grep -cE 'failed after [0-9]'` → 0 and `grep -c 'recorded an issue'` → 0. It started at 21:14:50 gated on `ps -axww -o command | grep -cE '(^| )/[^ ]*(swift-frontend|swift-driver|swiftpm-testing-helper|xctest)'` reading 0 twice, 10 seconds apart, against 4 both times for the same anchored search on `claude` as the control that the search can see anything; the fallback did not fire, and the one-minute load average was 6.67 at the start and 8.46 at the end. The completeness tests are shown to have run rather than skipped by their own lines in that log: `Test everyCatalogueRowHasExactlyOnePack() started.` then `… passed after 10.367 seconds.`, and `Test everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue() started.` then `… passed after 10.192 seconds.`. The same command with `--filter 'SkillPackTests|SkillGuidanceTests|SkillSelectionStoreTests'` -> exit 0, `Test run with 59 tests in 3 suites passed after 7.328 seconds.`, on the tree that became `0c1f8341`. An earlier full run at `19f182cf`, before Airwallex and Ramp, was also green (3475 tests in 251 suites, 101.751 seconds) and is superseded by this one. No server command is owed: nothing under `server/` changed. `server/scripts/check-secrets.sh`, `scripts/no-attribution tree` and `scripts/changelog-order` read tracked files, this entry among them, so they are run at the entry's own commit and reported on SONNY-519. **The carry to the entry's commit**: that commit changes only this file, so the suite figures are carried across it under WORKFLOW.md step 5's tree-identity proof on `Sources`, `Tests`, `Package.swift` and `docs/sonny-skill-sites.tsv`, whose output is on SONNY-519.
Mutation plan: none. Data only — this branch adds no executable line, and the ticket says so.

Behavior added:
- Twelve sites gain deep packs, eighteen flows (`for s in stripe wise square toggl_track clockify wave gocardless dext qonto lemon_squeezy airwallex ramp; do git show 0c1f8341:Sources/MacAgent/Resources/SkillPacks/$s.skillpack.json | python3 -c 'import json,sys; print(len(json.load(sys.stdin)["flows"]))'; done | paste -sd+ - | bc` → 18, and the same loop at `8f3d1d02` → 0 as the control): Stripe (invite a team member, search the Dashboard), Wise Business (download a statement, download a list of transfers), Square (create an item, export the team roster), Toggl Track (create a project, create a client), Clockify (track time with the timer, enter time manually, create a project), Wave (import a customer list from a CSV file), GoCardless (invite a team member), Dext (add a user), Qonto (download an account statement), Lemon Squeezy (export a store's orders), Airwallex (download a Global Account statement), Ramp (download receipts in bulk). Deep packs in the tree: `git grep -l '"depth": "deep"' <sha> -- Sources/MacAgent/Resources/SkillPacks | wc -l` → 172 at `0c1f8341` against 160 at `8f3d1d02`, and 0 for the same search on `"depth": "nope"` as the control.
- Every one of the twelve carries a start-page record for each start URL, all twelve `sign-in`: the page signs in an existing account and shows a separate route for creating one. Square's and Clockify's records carry the second title their landing reported (`otherTitles`); Stripe's, GoCardless's and Dext's heading is empty because none of the three has an `h1` or `h2` in its DOM.
- Each of the twelve rows' `doc_url_1..3` now names the pages its flows were read from, with the unused cells emptied.

Behavior preserved (required, no blanket claims):
- **The shipped catalogue still loads with no failure, and every row still has exactly one pack** — `everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue` and `everyCatalogueRowHasExactlyOnePack`, both shown started and passed in the run above, since a skipped completeness test reads exactly like a passing one.
- **No other catalogue row and no other column moved**, measured cell by cell against the branch's base `8f3d1d02` by a script comparing every cell of every row: 473 rows in the same order before and after, 33 changed cells on 12 rows, read at `0c1f8341`, every one a `doc_url` cell on one of this branch's rows, exit 0. The control fires: the same script over a copy with Stripe's `sign_in_url` and Payoneer's `doc_url_1` changed exits 1 and names both cells.
- **No row moved between `deep` and `site`**, so the tally in `theCommittedCatalogueIsTheListTheFoundersDecided` is untouched and `SkillPackTests.swift` is not edited.
- **The 463 packs this branch does not touch are byte-identical** — the name-only diff above lists twelve pack files of 473.
- **The identity-host list is untouched**: every one of the twelve lands on its own pack's site, so no pairing was needed, and `theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake` passes unchanged.
- **Triggers, summaries, names and sign-in addresses are unchanged** on all twelve packs; only `depth`, `flows` and `startPages` moved.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **Two sites cannot be deep without an identity-host pairing, and that list is Swift this ticket may not touch.** QuickBooks: the pack's domain is `quickbooks.intuit.com`, the product is `qbo.intuit.com` (off that domain, so a flow cannot even start there), and signed out it lands on `accounts.intuit.com/app/sign-in`, which is neither on the pack's site nor a listed identity host. Zoho Books: `books.zoho.com/` lands on the marketing page `www.zoho.com/us/books/`, and its sign-in is `accounts.zoho.com`, also unlisted. Each needs a founder decision on SONNY-524's list, not a data edit.
- **A bot check is a start page nobody can read.** FreshBooks' `my.freshbooks.com/` lands on `auth.freshbooks.com/oauth/authorize` titled "Checking your browser – reCAPTCHA" and stayed there; a lane presses nothing on a bot check, so there is no reading to record and no pack.
- **The money rule refuses a real control name, and the flow goes rather than the name.** (Ramp's case is the mild one: the page's own noun "link", in "an email with a link to a ZIP file", reads as an action verb beside "Reimbursements", so that one sentence says the ZIP arrives by email without the word.) Wise's team-member flow runs through a control called "Team members and payment approvals", and Wave's add-a-customer flow through a menu called "Sales & Payments"; beside an "Add" or "Remove" each is `add + payment` or `remove + payment`. Both are false refusals by the rule's own admission, and renaming a control a user must find is worse than losing the flow. Wave's import flow names the same menu and loads, because it names no listed action verb, which says the rule is reading words and not the task.
- **Money sat one control away on most of these pages, and the flows stop short of it.** Wise's team-member page ends in payment approvals and card spending limits; Square's add-a-team-member steps set pay type and a passcode; Dext's add-a-user page offers "Purchase additional users" at the plan limit; Stripe's customer-portal page (the row's old `doc_url_2`) configures cancelling plans and changing cards. None is a flow seed here, and Dext's flow says to stop at the plan's user limit rather than change the plan.
- **Two help centres refuse `get_page_text`** (clockify.me and support.waveapps.com answer "Permission denied for reading page content on this domain"), and some output is withheld by the tool as query-string data. Reading the DOM through `javascript_tool`, with `?`, `=` and `&` folded to spaces in the output, works on both.
- **Machine load decides whether a page can be read at all.** At a load average of 77 to 91 (another lane's deliberate CPU burners, then Swift builds) the renderer froze on Zoho's marketing page, Razorpay's and Airwallex's sign-in pages, NetSuite's marketing site and Ramp's sign-in, and the tab group reset three times. A freeze is not a reading and none is recorded as one: Airwallex and Ramp were read once the load fell to about 11, and both then rendered normally.
- **Payoneer's sign-in heading rotates.** Two arrivals at `login.payoneer.com/` disagreed: one had a visible `h2` "Welcome to Payoneer's new login screen" in its right-hand panel, the next had no `h1` or `h2` at all and showed "20+ years of borderless ambition" there instead. Two reads that disagree settle nothing, so no record is written yet; `otherTitles` holds a racing title and the format has no field for a racing heading.

Known limitations / deferred scope:
- Three sites still to finish: `razorpay` (its sign-in's settled title reads "Razorpay Sign Up & Login", so it may be one combined form), `netsuite` (its sign-in reads "NetSuite - Customer Login" with no sign-up route, so it passes only if netsuite.com offers no self-serve sign-up anywhere, which is unread), and `payoneer` (sign-in with a separate Sign up, heading unsettled as above; a monthly-statement flow page is found, `payoneer.custhelp.com/app/answers/detail/a_id/33168`, "Reports and Statements - FAQ", though it names no path to the page). The relaunched session continues them on this branch.
- `quickbooks`, `zoho_books` and `freshbooks` are left out, for the reasons above, and named on SONNY-519.

Open questions (required, write "none" if true): whether QuickBooks and Zoho Books should get identity-host pairings (`accounts.intuit.com` → `intuit.com`, `accounts.zoho.com` → `zoho.com`) under SONNY-524's rule; asked on SONNY-519.

Next branch: the relaunched SONNY-519 session, on this branch.
