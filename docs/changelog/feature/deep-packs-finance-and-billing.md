### Branch: feature/deep-packs-finance-and-billing
Status: complete
Date: 2026-09-18
Tickets: SONNY-519 — deep Skills packs for the eighteen `finance_billing` sites: thirteen are deep, five are held and named on the ticket with the reason each is (two moved to SONNY-531 by the coordinator). Also carries SONNY-526's one row for this group, Wise: its sign-in address `https://wise.com/login` was opened signed out and is right, so nothing changed.
Reviewed by: pending — a fresh session with a browser connected, per the ticket's acceptance criteria.

Spec sections covered: none directly — this branch is Skills pack data for sites the catalogue already lists. The Skills mechanism (pack format, loader, content rules, trigger check, start-page rule) is SONNY-452's, SONNY-482's and SONNY-510's, and is untouched here.
Files changed: thirteen pack files under `Sources/MacAgent/Resources/SkillPacks/` (`airwallex`, `clockify`, `dext`, `gocardless`, `lemon_squeezy`, `payoneer`, `qonto`, `ramp`, `square`, `stripe`, `toggl_track`, `wave`, `wise`, each shallow to deep), `docs/sonny-skill-sites.tsv` (those thirteen rows' `doc_url` cells), `docs/manual-tests/feature/deep-packs-finance-and-billing.md` (new) and this entry. No Swift source, no test, no other group's pack, no other row, no other column (`git diff --name-only 8f3d1d02 3e01a05d -- Sources | wc -l` → 13; the same diff without the path filter adds only the catalogue and this branch's two record files).
Tests: **Measured at `3e01a05d`**, with `git status --porcelain` empty. `env CLANG_MODULE_CACHE_PATH="$PWD/.build/clang-module-cache" swift test --disable-sandbox -Xswiftc -F -Xswiftc /Library/Developer/CommandLineTools/Library/Developer/Frameworks -Xlinker -rpath -Xlinker /Library/Developer/CommandLineTools/Library/Developer/Frameworks -Xlinker -rpath -Xlinker /Library/Developer/CommandLineTools/Library/Developer/usr/lib` -> exit 0, `Test run with 3475 tests in 251 suites passed after 99.814 seconds with 8 known issues.`, `grep -cE 'failed after [0-9]'` → 0 and `grep -c 'recorded an issue'` → 0. It started at 23:31:19 gated on `ps -axww -o command | grep -cE '(^| )/[^ ]*(swift-frontend|swift-driver|swiftpm-testing-helper|xctest)'` reading 0 twice, 11 seconds apart, against 4 both times for the same anchored search on `claude` as the control that the search can see anything; the fallback did not fire. **It was not a quiet run**: the one-minute load average was 15.53 at the start and 12.68 at the end, from other lanes' work on the same machine. The completeness tests are shown to have run rather than skipped by their own lines in that log: `Test everyCatalogueRowHasExactlyOnePack() started.` then `… passed after 11.321 seconds.`, and `Test everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue() started.` then `… passed after 11.342 seconds.`. The same command with `--filter 'SkillPackTests|SkillGuidanceTests|SkillSelectionStoreTests'` -> exit 0, `Test run with 59 tests in 3 suites passed after 7.267 seconds.`, on the tree that became `3e01a05d`, load 9.52 before and 9.36 after. Two earlier full runs on this branch were also green — at `19f182cf` (101.751 s, load 12.49 → 11.22) and `0c1f8341` (95.714 s, load 6.67 → 8.46) — and are superseded by this one. No server command is owed: nothing under `server/` changed. `server/scripts/check-secrets.sh`, `scripts/no-attribution tree` and `scripts/changelog-order` read tracked files, this entry among them, so they are run at the entry's own commit and reported on SONNY-519 and the PR. **The carry to the entry's commit**: that commit changes only the two record files, so the suite figures are carried across it under WORKFLOW.md step 5's tree-identity proof on `Sources`, `Tests`, `Package.swift` and `docs/sonny-skill-sites.tsv`, whose output is on SONNY-519.
Mutation plan: none. Data only — this branch adds no executable line, and the ticket says so.

Behavior added:
- Thirteen sites gain deep packs, twenty flows (`for s in stripe wise square toggl_track clockify wave gocardless dext qonto lemon_squeezy airwallex ramp payoneer; do git show 3e01a05d:Sources/MacAgent/Resources/SkillPacks/$s.skillpack.json | python3 -c 'import json,sys; print(len(json.load(sys.stdin)["flows"]))'; done | paste -sd+ - | bc` → 20, and the same loop at `8f3d1d02` → 0 as the control): Stripe (invite a team member, search the Dashboard), Wise Business (download a statement, download a list of transfers), Square (create an item, export the team roster), Toggl Track (create a project, create a client), Clockify (track time with the timer, enter time manually, create a project), Wave (import a customer list from a CSV file), GoCardless (invite a team member), Dext (add a user), Qonto (download an account statement), Lemon Squeezy (export a store's orders), Airwallex (download a Global Account statement), Ramp (download receipts in bulk), Payoneer (download a monthly statement, export the list of transactions). Deep packs in the tree: `git grep -l '"depth": "deep"' <sha> -- Sources/MacAgent/Resources/SkillPacks | wc -l` → 173 at `3e01a05d` against 160 at `8f3d1d02`, and 0 for the same search on `"depth": "nope"` as the control.
- Every one of the thirteen carries a start-page record for each start URL, all `sign-in`: the page signs in an existing account and shows a separate route for creating one. Square's and Clockify's records carry the second title their landing reported (`otherTitles`). Stripe's, GoCardless's, Dext's and Payoneer's heading is empty because none has a visible `h1`, `h2` or `role="heading"` element — the founders' ruling of 2026-09-18 on SONNY-510, which arrived while this branch was open, counts a visible `role="heading"` as a heading, so all four were read for one — Payoneer on its two arrivals at 23:20 EDT, Stripe, GoCardless and Dext again at about 23:40, load about 10 — and none has one. GoCardless's "Sign in to your account" is an `h3`, Dext's "Log in to Dext" a `span` and Payoneer's "Welcome back" a `p`, so an empty heading on this branch means no visible heading of any kind.
- **Dext's add-a-user flow opens with a stop**: "Stop and tell the person, and change nothing, if Dext says the account has reached its plan's limit on users. Never change the plan or the user bundle to make room." Its page offers "Purchase additional users" at exactly that point, and the stop is written as the first instruction rather than a description after the Add button, which is the distinction PR #273 paid a round for.
- Each of the thirteen rows' `doc_url_1..3` names the pages its flows were read from, with the unused cells emptied.

Behavior preserved (required, no blanket claims):
- **The shipped catalogue still loads with no failure, and every row still has exactly one pack** — `everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue` and `everyCatalogueRowHasExactlyOnePack`, both shown started and passed in the run above, since a skipped completeness test reads exactly like a passing one.
- **No other catalogue row and no other column moved**, measured cell by cell against the branch's base `8f3d1d02` by a script comparing every cell of every row, read at `3e01a05d`: 473 rows in the same order before and after, 36 changed cells on 13 rows, every one a `doc_url` cell on one of this branch's rows, exit 0. The control fires: the same script over a copy with Stripe's `sign_in_url` and Razorpay's `doc_url_1` changed exits 1 and names both cells.
- **No row moved between `deep` and `site`**, so the tally in `theCommittedCatalogueIsTheListTheFoundersDecided` is untouched and `SkillPackTests.swift` is not edited.
- **The 460 packs this branch does not touch are byte-identical** — the name-only diff above lists thirteen pack files of 473, and the five held packs (`razorpay`, `netsuite`, `quickbooks`, `zoho_books`, `freshbooks`) are among the untouched, still shallow with no flows.
- **The identity-host list is untouched**: every one of the thirteen lands on its own pack's site, so no pairing was needed, and `theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake` passes unchanged.
- **Triggers, summaries, names and sign-in addresses are unchanged** on all thirteen packs; only `depth`, `flows` and `startPages` moved.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **Five sites are held, each for a start-page reason, and none is a reading that failed.** Razorpay: `dashboard.razorpay.com/` lands on `accounts.razorpay.com/auth/`, titled "Razorpay Sign Up & Login - Access Your Dashboard", whose one field is headed "Get started with your email or phone number" and whose page has no sign-in, sign-up, create or register control anywhere, on two arrivals — the form is how an account gets created and there is no separate route. NetSuite: its sign-in page has no sign-up route, so it could pass only if netsuite.com offers no self-serve sign-up anywhere; it does — the homepage's "Test Drive NetSuite Next — Your free 3-day trial is here" leads to a page headed "Test Drive the Next Generation of NetSuite" with a "Get my free test drive account" button — so that is tl;dv's shape and it is held. FreshBooks: `my.freshbooks.com/` lands on a reCAPTCHA "Checking your browser" interstitial that did not clear; nobody completes a bot check. QuickBooks and Zoho Books: both sign in on hosts off their pack's site that no identity-host pairing admits, and the pairing cannot land ahead of the pack because `theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake` holds the list equal to the landings — so the coordinator moved both to SONNY-531, and this branch's readings of them are on SONNY-519 for that lane to confirm.
- **QuickBooks needs more than a pairing, which only reading showed.** Its row's domain is `quickbooks.intuit.com`, and both product URLs, `qbo.intuit.com` and `app.qbo.intuit.com/app/login`, are off it — so a flow cannot even start there, whatever the identity-host list says. The one on-domain sign-in URL, `quickbooks.intuit.com/login/`, is a product chooser headed "Choose a QuickBooks product to sign in" beside "Buy now & save", "30-day free trial" and "50% off QuickBooks Online", which is no start page. Zoho Books has no such problem: `books.zoho.com/app` is on its own site and lands on `accounts.zoho.com/signin`, so a pairing alone admits it.
- **A rotating promotional heading is not the page's heading** (founders, 2026-09-18, answering this branch). Payoneer's sign-in form has no heading element of its own — its "Welcome back" is a `<p>` — and the only `h2` ever seen on the page was promo copy in a right-hand panel that changed between visits ("Welcome to Payoneer's new login screen", then "20+ years of borderless ambition", then not in the document at all). So the record's heading is empty. `otherTitles` is for a title that races between two fixed values and is not for copy that cycles by design. Zoho's sign-in page has the same carousel shape and no heading element either.
- **The money rule refuses a real control name, and the flow goes rather than the name.** Wise's team-member flow runs through a control called "Team members and payment approvals", and Wave's add-a-customer flow through a menu called "Sales & Payments"; beside an "Add" or "Remove" each is `add + payment` or `remove + payment`. Both were dropped rather than renamed. Wave's import flow names the same menu and loads, because it names no listed action verb, which says the rule reads words and not the task. Ramp's case is the mild one: the page's own noun "link" ("an email with a link to a ZIP file") reads as an action verb beside "Reimbursements", so that sentence says the ZIP arrives by email without the word.
- **Money sat one control away on most of these pages, and the flows stop short of it.** Wise's team page ends in payment approvals and card spending limits; Square's add-a-team-member sets pay type and a passcode; Dext's add-user page offers "Purchase additional users"; Stripe's customer-portal page (the row's old `doc_url_2`) configures cancelling plans and changing cards. None is a flow seed here.
- **Two help centres refuse `get_page_text`** (clockify.me and support.waveapps.com answer "Permission denied for reading page content on this domain"), and some `javascript_tool` output is withheld as query-string data. Reading the DOM, with `?`, `=` and `&` folded to spaces in what it returns, works on both. Ramp's help centre titles every page "Ramp Help Center", so its citation is checked by its `h1`, and Payoneer's articles carry a site-wide `h1` ("How can we help you?"), so theirs is checked by the title.
- **Machine load decides whether a page can be read at all, and load goes beside every timing.** At a load average of 77 to 91 (another lane's deliberate CPU burners, then Swift builds) the renderer froze on Zoho's marketing page, Razorpay's and Airwallex's sign-in pages, NetSuite's marketing site and Ramp's sign-in, and the tab group reset three times; each was read later at a load of about 10, when every one rendered. A freeze is not a reading, and none is recorded as one.

Known limitations / deferred scope:
- `quickbooks` and `zoho_books` are SONNY-531's, by the coordinator's decision, with Microsoft Azure, Microsoft 365 and Zoho Desk; the pairings and the packs land there together.
- `razorpay`, `netsuite` and `freshbooks` stay shallow for the reasons above, and are named on SONNY-519.

Open questions (required, write "none" if true): none. The three this branch raised — the two pairings, Payoneer's rotating heading and FreshBooks' bot check — were answered by the coordinator on 2026-09-18 and are recorded on SONNY-519 and SONNY-510.

Next branch: none owed by this group; SONNY-531 carries the two sites that need pairings.
