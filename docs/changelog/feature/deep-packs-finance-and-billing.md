### Branch: feature/deep-packs-finance-and-billing
Status: complete
Date: 2026-09-18, review round 2026-09-19
Tickets: SONNY-519 — deep Skills packs for the eighteen `finance_billing` sites: fourteen are deep, four are held and named on the ticket with the reason each is (two moved to SONNY-531 by the coordinator). Also carries SONNY-526's one row for this group, Wise: its sign-in address `https://wise.com/login` was opened signed out and is right, so nothing changed.
Reviewed by: fresh session (per WORKFLOW.md step 7), review of PR #280 at `e1307556` with a browser connected. It found no wrong flow and reproduced the Razorpay, NetSuite and QuickBooks readings. Three findings blocked: **F1**, two flows walked a free-plan user toward a paid option (Clockify's Private, Toggl's estimates); **F2**, GoCardless's and Payoneer's empty headings were wrong under the corrected heading rule, and this entry said an empty heading meant no heading of any kind; **F3**, Wise's statement flow ended at an identity confirmation it described rather than stopped at. All three are fixed in `d83b7bd9`, with **F5** (GoCardless's invite step now offers Read-only first) and the reviewer's wording for **F4** (Dext's stop). F6 was SONNY-531's scope and is corrected there; F7 was answered by the founders (a cookie banner's heading is never the page's). The round also made FreshBooks deep, by founder decision, after its hold turned out to rest on readings taken in a window that was not drawn.

Spec sections covered: none directly — this branch is Skills pack data for sites the catalogue already lists. The Skills mechanism (pack format, loader, content rules, trigger check, start-page rule) is SONNY-452's, SONNY-482's and SONNY-510's, and is untouched here.
Files changed: fourteen pack files under `Sources/MacAgent/Resources/SkillPacks/` (`airwallex`, `clockify`, `dext`, `freshbooks`, `gocardless`, `lemon_squeezy`, `payoneer`, `qonto`, `ramp`, `square`, `stripe`, `toggl_track`, `wave`, `wise`, each shallow to deep), `docs/sonny-skill-sites.tsv` (those fourteen rows' `doc_url` cells), `docs/manual-tests/feature/deep-packs-finance-and-billing.md` (new) and this entry. No Swift source, no test, no other group's pack, no other row, no other column (`git diff --name-only 8f3d1d02 d83b7bd9 -- Sources | wc -l` → 14; the same diff without the path filter adds only the catalogue and this branch's two record files).
Tests: **Measured at `d83b7bd9`**, with `git status --porcelain` empty. `env CLANG_MODULE_CACHE_PATH="$PWD/.build/clang-module-cache" swift test --disable-sandbox -Xswiftc -F -Xswiftc /Library/Developer/CommandLineTools/Library/Developer/Frameworks -Xlinker -rpath -Xlinker /Library/Developer/CommandLineTools/Library/Developer/Frameworks -Xlinker -rpath -Xlinker /Library/Developer/CommandLineTools/Library/Developer/usr/lib` -> exit 0, `Test run with 3475 tests in 251 suites passed after 94.411 seconds with 8 known issues.`, `grep -cE 'failed after [0-9]'` → 0 and `grep -c 'recorded an issue'` → 0. It started at 10:33:32 on 2026-09-19 gated on `ps -axww -o command | grep -cE '(^| )/[^ ]*(swift-frontend|swift-driver|swiftpm-testing-helper|xctest)'` reading 0 twice, 10 seconds apart, against 5 both times for the same anchored search on `claude` as the control that the search can see anything; the fallback did not fire. The one-minute load average was 4.98 at the start and 4.93 at the end. The completeness tests are shown to have run rather than skipped by their own lines in that log: `Test everyCatalogueRowHasExactlyOnePack() started.` then `… passed after 10.385 seconds.`, `Test everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue() started.` then `… passed after 10.329 seconds.`, and `Test theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake() started.` then `… passed after 4.234 seconds.`. The same command with `--filter 'SkillPackTests|SkillGuidanceTests|SkillSelectionStoreTests'` -> exit 0, `Test run with 59 tests in 3 suites passed after 7.192 seconds.`, on the tree that became `d83b7bd9`, load 3.23 before and 3.20 after. Earlier full runs on this branch were also green — at `19f182cf` (101.751 s, load 12.49 → 11.22), `0c1f8341` (95.714 s, load 6.67 → 8.46) and `3e01a05d` (99.814 s, load 15.53 → 12.68) — and are superseded by this one. No server command is owed: nothing under `server/` changed. `server/scripts/check-secrets.sh`, `scripts/no-attribution tree` and `scripts/changelog-order` read tracked files, this entry among them, so they are run at the entry's own commit and reported on SONNY-519 and the PR. **The carry to the entry's commit**: that commit changes only the two record files, so the suite figures are carried across it under WORKFLOW.md step 5's tree-identity proof on `Sources`, `Tests`, `Package.swift` and `docs/sonny-skill-sites.tsv`, whose output is on SONNY-519.
Mutation plan: none. Data only — this branch adds no executable line, and the ticket says so.

Behavior added:
- Fourteen sites gain deep packs, twenty-two flows (`for s in stripe wise square toggl_track clockify wave gocardless dext qonto lemon_squeezy airwallex ramp payoneer freshbooks; do git show d83b7bd9:Sources/MacAgent/Resources/SkillPacks/$s.skillpack.json | python3 -c 'import json,sys; print(len(json.load(sys.stdin)["flows"]))'; done | paste -sd+ - | bc` → 22, and the same loop at `8f3d1d02` → 0 as the control): Stripe (invite a team member, search the Dashboard), Wise Business (download a statement, download a list of transfers), Square (create an item, export the team roster), Toggl Track (create a project, create a client), Clockify (track time with the timer, enter time manually, create a project), Wave (import a customer list from a CSV file), GoCardless (invite a team member), Dext (add a user), Qonto (download an account statement), Lemon Squeezy (export a store's orders), Airwallex (download a Global Account statement), Ramp (download receipts in bulk), Payoneer (download a monthly statement, export the list of transactions), FreshBooks (create a client, import clients from a CSV file). Deep packs in the tree: `git grep -l '"depth": "deep"' <sha> -- Sources/MacAgent/Resources/SkillPacks | wc -l` → 174 at `d83b7bd9` against 160 at `8f3d1d02`, and 0 for the same search on `"depth": "nope"` as the control.
- Every one of the fourteen carries a start-page record for each start URL, all `sign-in`: the page signs in an existing account and shows a separate route for creating one. Square's and Clockify's records carry the second title their landing reported (`otherTitles`).
- **Six flows carry a stop, each written as an instruction.** Four open with one: Wise's statement flow stops before the confirmation Wise asks for, "that step asks them to confirm their identity, which only they can do"; Dext's add-a-user flow stops at the plan's user limit; and both FreshBooks flows stop at the plan's client limit. In the other two, Clockify's and Toggl's create-a-project steps stop, rather than change the plan, when the person wants Private and it is not offered.
- Each of the fourteen rows' `doc_url_1..3` names the pages its flows were read from, with the unused cells emptied. FreshBooks' row used to cite two online-payment articles, which are gone with it.

Behavior preserved (required, no blanket claims):
- **The shipped catalogue still loads with no failure, and every row still has exactly one pack** — `everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue` and `everyCatalogueRowHasExactlyOnePack`, both shown started and passed in the run above, since a skipped completeness test reads exactly like a passing one.
- **No other catalogue row and no other column moved**, measured cell by cell against the branch's base `8f3d1d02` by a script comparing every cell of every row, read at `d83b7bd9`: 473 rows in the same order before and after, 39 changed cells on 14 rows, every one a `doc_url` cell on one of this branch's rows, exit 0. The control fires: the same script over a copy with Stripe's `sign_in_url` and Razorpay's `doc_url_1` changed exits 1 and names both cells.
- **No row moved between `deep` and `site`**, so the tally in `theCommittedCatalogueIsTheListTheFoundersDecided` is untouched and `SkillPackTests.swift` is not edited.
- **The 459 packs this branch does not touch are byte-identical** — the name-only diff above lists fourteen pack files of 473, and the four held packs (`razorpay`, `netsuite`, `quickbooks`, `zoho_books`) are among the untouched, still shallow with no flows.
- **The identity-host list is untouched**: every one of the fourteen lands on its own pack's site, so no pairing was needed, and `theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake` passes unchanged.
- **Triggers, summaries, names and sign-in addresses are unchanged** on all fourteen packs; only `depth`, `flows` and `startPages` moved.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **A page read in a window that is not drawn is not a reading, and this branch held a site on one.** FreshBooks' `my.freshbooks.com/` was read twice on 2026-09-18 and stayed on a reCAPTCHA page, "Checking your browser – reCAPTCHA", so it was held as a bot check. On 2026-09-19, with the window drawn, the same page cleared by itself with nothing pressed, twice, and landed on `auth.freshbooks.com/service/auth/en/integrations/sign_in`, titled "FreshBooks", headed "Log in to FreshBooks", with a separate Sign Up. It is the shape SONNY-510 records for Freshsales, Teams and Microsoft Lists: a page that never progresses reads exactly like one that offers nothing. **How the window came not to be drawn is the part to keep**: Claude's tab was one of eleven tabs in a Chrome window that other sessions also use, and only a window's active tab is painted, so `document.visibilityState` read `"hidden"` and a one-second `requestAnimationFrame` count read 0 while screenshots still captured the page. The fix was to make the tab the window's active tab through AppleScript, by the tab id the automation tool uses (AppleScript's `id of tab` is the same number), before every read, after which the same checks read `"visible"` and 101 frames; every reading in the review round carries that `visibilityState` beside it. The cost is the other sessions' tabs in that window going unpainted while this one reads, which the founders chose knowingly.
- **An empty heading on this branch now means no visible heading of any kind, and two records needed changing to make that true.** The heading rule was corrected twice while this branch was open (SONNY-510: a visible `role="heading"` counts, then any of `h1`–`h6` does). Payoneer's first visible heading is an `h4 role="heading"` wrapping its Forgot password control. This branch had said its only heading was an `h3`, from queries that did not ask for an `h4`, which was wrong when written. GoCardless has two `h3`s, and which one is visible first depends on the window: at 1920 px wide the review saw the left promo panel's "Instant payments. Without card fees." first, and at 960 px that panel is `display: none` and the form's own "Sign in to your account" is the only one. The record names the form's `h3`, the one visible at every width. Stripe and Dext have no heading element of any kind and stay empty. A cookie banner's heading is never the page's (founders, 2026-09-18), which is why Toggl's record keeps its `h1` though a Cookiebot `role="heading"` precedes it.
- **A paid option one click into a flow is a paywall even when the flow's words name no money** (F1). Clockify's help page prints, directly under its create-a-project steps, that free-plan users can set only Public. Toggl's pricing page disagrees with itself in two panes. The visible annual card and full comparison table list "Project and task estimates" under Free and "Private projects and tasks" under paid plans. A hidden monthly pane lists "Project time estimates and alerts" under Starter, which is the pane the review read. Both readings are right about different panes, so the step drops estimates and stops at Private, which satisfies either. FreshBooks documents its plan's client limit in a collapsed answer on its import page ("Why am I being asked to pay?"), which only a DOM read shows.
- **The guards refuse the words a stop needs, and this branch hit it three ways (SONNY-536).** A stop at a paid option cannot say "upgrade" beside "plan", because the money rule's `purchaseControls` and `pricedWords` pair into a refusal, so Clockify's, Toggl's and FreshBooks' stops say "never change the plan". A stop before Wise's identity confirmation cannot say "password", because the credential rule refuses it anywhere, so it says the step asks the person to confirm their identity. Dext's stop cannot name the page's own "Purchase additional users" control, because `purchaseActs` refuses "purchase" alone, so it forbids changing "the plan, the user bundle or the number of users the plan allows". Three lanes have now met this, which is why the ticket exists.
- **Four sites are held, each for a start-page reason.** Razorpay: `dashboard.razorpay.com/` lands on `accounts.razorpay.com/auth/`, titled "Razorpay Sign Up & Login - Access Your Dashboard", whose one field is headed "Get started with your email or phone number". On two arrivals the page had no sign-in, sign-up, create or register control anywhere, so the form is how an account gets created and there is no separate route. NetSuite: its sign-in page has no sign-up route, so it could pass only if netsuite.com offers no self-serve sign-up anywhere. It does: the homepage's "Test Drive NetSuite Next — Your free 3-day trial is here" leads to a page with a "Get my free test drive account" button. QuickBooks and Zoho Books: both sign in on hosts off their pack's site, and are SONNY-531's. QuickBooks also needs its row's domain decided, because both its product URLs are off `quickbooks.intuit.com`.
- **The money rule refuses a real control name, and the flow goes rather than the name.** Wise's team-member flow runs through "Team members and payment approvals" and Wave's add-a-customer flow through "Sales & Payments", so each pairs into `add + payment` or `remove + payment`. Both were dropped. Ramp's page noun "link" read as an action verb beside "Reimbursements", so that sentence was reworded.
- **Two help centres refuse `get_page_text`** (clockify.me and support.waveapps.com), and some `javascript_tool` output is withheld as query-string data. Reading the DOM, with `?`, `=` and `&` folded to spaces in what it returns, works on both. Ramp's help centre titles every page "Ramp Help Center", so its citation is checked by its `h1`. Payoneer's articles carry a site-wide `h1`, so theirs is checked by the title.
- **Machine load decides whether a page can be read at all, and load goes beside every timing.** At a load average of 77 to 91 on 2026-09-18 (another lane's deliberate CPU burners, then Swift builds) the renderer froze on several pages, and the tab group reset three times. Each was read later at a load of about 10. A freeze is not a reading, and none is recorded as one.

Known limitations / deferred scope:
- `quickbooks` and `zoho_books` are SONNY-531's, by the coordinator's decision; QuickBooks there needs a decision about its row's domain rather than a pairing.
- `razorpay` and `netsuite` stay shallow for the reasons above, and are named on SONNY-519.

Open questions (required, write "none" if true): whether GoCardless's heading should name the form's own `h3` or the promo panel's, which is first only in a wide window; this branch named the form's, the one visible at every width, and says so on SONNY-519 for the founders to overrule.

Next branch: none owed by this group; SONNY-531 carries the two sites that need a pairing or a domain decision.
