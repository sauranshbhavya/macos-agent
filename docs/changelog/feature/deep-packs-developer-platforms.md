### Branch: feature/deep-packs-developer-platforms
Status: complete, awaiting its fresh review
Date: 2026-09-18
Tickets: **SONNY-520** — fourteen of the group's twenty sites are deep on this branch: `digitalocean`, `google_cloud`, `firebase`, `aws`, `netlify`, `optimizely`, `auth0`, `expo`, `render`, `heroku`, `algolia`, `namecheap`, `godaddy`, `hostinger`. Six stay shallow, each held for a reason named under Known limitations: `microsoft_azure` (moved to **SONNY-531** by the coordinator), `google_play_console`, `anthropic_console`, `growthbook`, `railway` and `app_store_connect`. **SONNY-526**, two rows only — `expo` and `railway`: each row's guessed sign-in address was opened, and both land where the row says (below). Wave 13, cut from `main` at `8f3d1d02`, independent of every other lane — no stack.
Reviewed by: **review-278**, a fresh session with a browser connected from the start, on PR #278. Its findings reached this lane through the coordinator, and this round answers the ones routed here:
- two start-page records that said `""` while their page shows a visible `h3`, DigitalOcean and Hostinger;
- Google Cloud's Enable-an-API flow, whose page says some APIs ask to accept their Terms of Service first;
- App Store Connect, which the reviewer could read and which is held, not unreadable (Known limitations).
The coordinator's ruling in the same round added a stop step to Render's and Netlify's environment-variable flows. Three of the review's findings are not this branch's and are routed elsewhere: a credential-creating Google Cloud flow the rule does not refuse (SONNY-534), a clock-bound test that failed at load 93 (being fixed on PR #277), and GoDaddy's dashboard answering again.

Spec sections covered: none directly. This is Skills pack data: fourteen resource files and fourteen rows of the catalogue.
Files changed:
- `Sources/MacAgent/Resources/SkillPacks/` `digitalocean`, `google_cloud`, `firebase`, `aws`, `netlify`, `optimizely`, `auth0`, `expo`, `render`, `heroku`, `algolia`, `namecheap`, `godaddy`, `hostinger` (`.skillpack.json`): each goes `shallow` to `deep` and gains its flows and one `startPages` record — 30 flows and 113 steps. Field by field against `8f3d1d02`, only `depth`, `flows` and `startPages` moved, on these fourteen and on no other pack. Every header line — name, domain, summary, `signInURL`, triggers, sections — is byte-identical to the base.
- `docs/sonny-skill-sites.tsv`: the `doc_url_1` to `doc_url_3` cells of those fourteen rows, and nothing else in the file — 37 cells.
- This entry and `docs/manual-tests/feature/deep-packs-developer-platforms.md`.

Tests:
- The flagged suite, the exact command in `CLAUDE.md`: exit 1, `Test run with 3475 tests in 251 suites failed after 180.840 seconds with 9 issues (including 8 known issues).`, over the tree committed as `7e457294`. **The one issue beyond the eight known ones is not this branch's.** It is `theCutoffIsTheServersClockAndNotThisMacs()` at `TaskDeletionReachesTheServerTests.swift:832:9`, `Expectation failed: (bound.timeIntervalSince(Date()) → -3704.556867957115) > (-3700 → -3700.0)`: a clock-bound test with 100 s of slack that took about 104.6 s. That is the defect review-277 found at the same line, and it is being fixed on PR #277. No pack file, catalogue row or record is read by that test. Every other test passed. The log holds 3 `failed after` lines (that test, its suite and the run) and 7 `skipped` lines, all environment gates and none Skill-related: the `SonnyLiveGatewayTests` suite and its four tests, the capture-corpus measurement, and one window-coordinator test.
  - **The load, recorded.** The run started at 04:58:02Z, after `ps -axww -o command | grep -cE '(^| )/[^ ]*(swift-frontend|swift-driver|swiftpm-testing-helper|xctest)'` read 0; its control, the same count for `zsh|bash`, read 14. The load average was 61.20 at the start and 39.15 at the end (05:01:04Z).
  - **Two other runs at this head are not the measurement.** One at 04:54:18Z was started while that count read 23, another lane's build. It failed with `59 issues (including 8 known issues)`. Its issue lines name only `SonnyBackendClientTests.swift` (32) and `SonnyGoogleSignInTests.swift` (8): stubbed calls never arriving, and 20-second timeouts. It is listed here only so that nobody cites it. A further attempt after 05:01Z was refused by its own guard, because the count read 2.
  - `c4d808c3`'s clean run, 3475 tests passed at load 9, describes the first pack commit and no later head.
- **An earlier run at `6faa5466` failed, and it is recorded, not dropped.** That was the head after the two heading fixes. It exited 1, `Test run with 3475 tests in 251 suites failed after 293.243 seconds with 130 issues (including 8 known issues)`. Every failing line, 122 issues across 41 tests, was in `VisionSessionRunTests.swift`, and every message was the hang backstop's own verdict: "This is main-actor starvation and not a stuck loop", "gave up after 189.0s waiting for: the run to finish, having managed to evaluate the condition only 11 times". The load average was 31.62 when the run started (04:41:40Z) and 125.54 soon after it ended (`uptime` at 00:47 local). The pack data moved after that run anyway, so it describes no head that merges.
- The Skills suites, `--filter 'SkillPack|Skill'`: exit 0, `Test run with 74 tests in 5 suites passed after 22.598 seconds.`, over the tree committed as `7e457294` (started 04:53:39Z, load 21.28), with only this entry and the manual-test rows uncommitted. The six tests that read the shipped catalogue each printed one `started` and one `passed` line, and none printed `skipped`: `everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue`, `everyCatalogueRowHasExactlyOnePack`, `theCommittedCatalogueIsTheListTheFoundersDecided`, `everyShippedTriggerIsDistinctiveOrAnchoredToItsSite`, `theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake` and `everyShippedDeepPackRecordsWhereItsStartPagesLanded` (`grep -c "Test <name>() started"`, `passed` and `skipped` over that run's log → 1, 1 and 0 for each).
- **Both Swift figures carry from `7e457294` to the head that merges by a tree-identity check**, since every commit above it touches only `docs/`. `zsh -c 'old=7e457294; new=HEAD; for p in Sources Tests Package.swift docs nosuchdir; do a=$(git rev-parse --verify --quiet "${old}:${p}"); ea=$?; b=$(git rev-parse --verify --quiet "${new}:${p}"); eb=$?; …; done'` reads each side's exit code rather than the emptiness of its output. The revisions are braced, and the loop variable is not `path`. It must print `IDENTICAL` for `Sources`, `Tests` and `Package.swift`, `MOVED` for `docs` and `REFUSED` for `nosuchdir`; the last two are the controls. The printed hashes are on SONNY-520.
- The population, over a `git archive` of the packs folder: `python3 -c "import json,glob;P=[json.load(open(p)) for p in glob.glob('<dir>/Sources/MacAgent/Resources/SkillPacks/'+'*.skillpack.json')];print(len(P),sum(d['depth']=='deep' for d in P),sum(len(d['flows']) for d in P),sum(len(d.get('startPages',[])) for d in P))"` → `473 160 399 171` at `8f3d1d02` and `473 174 429 185` at `7e457294`: fourteen packs, 30 flows and 14 records more. It read `473 174 430 185` at `c4d808c3`, before Google Cloud's Enable-an-API flow was dropped.
- Which fields moved: a Python comparison of every pack's parsed JSON at `8f3d1d02` against `7e457294`, key by key, prints `14 [('depth', 'flows', 'startPages')]` — fourteen packs changed and every one of them changed exactly those three keys.
- The catalogue, cell by cell against the base: comparing `git show 8f3d1d02:docs/sonny-skill-sites.tsv` with the committed file row by row and column by column prints `changed cells: 37 in scope: 37 out of scope: 0`, where in scope means a `doc_url_*` column of one of the fourteen rows. It printed 38 before Google Cloud's row stopped citing the Enable-an-API page. **The control fires**: the same comparison over a copy with `digitalocean`'s `sign_in_url` changed from `/login` to `/signin` prints `OUT OF SCOPE: digitalocean sign_in_url` and exits 1. The row count is asserted equal first, so a row added or dropped cannot pass as zero changed cells. The tally in `theCommittedCatalogueIsTheListTheFoundersDecided` did not move: every one of the fourteen rows was and is `deep`.
- `npm run check:secrets`, `scripts/no-attribution tree` and `scripts/changelog-order`, each run over the committed tree holding this entry, with nothing uncommitted: `server/scripts/check-secrets.sh` from the repository root exits 0, `check-secrets: clean (1306 tracked files scanned, 12 patterns, 8 baselined fixtures)`; `scripts/no-attribution tree` exits 0, `0 of 1302 tracked file(s) carry an attribution (4 excluded by path)`; `scripts/changelog-order` exits 0, `19 entry file(s) under docs/changelog and 19 under docs/manual-tests resolve`.
- No Postgres, no server command beyond the secrets scan, and no `scripts/warnings`: nothing under `server/`, `Tests/` or any Swift file changed (`git diff --name-only 8f3d1d02 HEAD | wc -l` → 17: the fourteen pack files, the catalogue and this branch's two record files).
Mutation plan: none. The ticket declares this branch data only — no rule, loader or test changed, so there is no behaviour of this branch's own for a mutant to break.

Behavior added:
- **How every page was read.** In the Chrome extension's browser, confirmed signed out before anything else was read: `https://accounts.google.com/` landed on `/v3/signin/identifier`, titled "Sign in - Google Accounts", with no account listed, at 2026-09-19T00:14:47Z, and `https://github.com/` showed its "Sign in" link and no signed-in user at 00:14:51Z. Chrome dropped the tab group once mid-session, and the Google check was repeated in the new group at about 00:39Z with the same answer. Every start page was navigated through its own start URL, waited on, and read twice; its heading was first read as the first `h1` or `h2` with rendered area, not hidden by `display` or `visibility`, and carrying text. Every documentation page was verified by its landed title before its steps were used. Nothing was typed, submitted or pressed except ordinary navigation. The `read` field says `2026-09-18`, the local date; the readings ran from 00:14Z to 01:05Z UTC.
- **Every start page's heading was re-read under the ruling recorded on SONNY-510 on 2026-09-18.** Under that ruling the heading is the first visible heading of any kind: any of `h1` to `h6`, or any element with `role="heading"`, in page order. The re-read was one query over `h1,h2,h3,h4,h5,h6,[role=heading]` with the same visibility test, twice per page, on 2026-09-19 between 04:35:17Z and 04:38:45Z. The profile was confirmed signed out first: `accounts.google.com` landed on its identifier page at 04:34:55Z, and GitHub showed no user at 04:34:58Z.
  - **Two records changed.** DigitalOcean's and Hostinger's pages each show a visible `h3` and nothing above it — "Log in to your account" and "Log in" — which the first `h1`/`h2` reading had recorded as `""`.
  - **The other twelve hold.** Namecheap's page has no heading element of any kind in its DOM, so `""` is right. For the other eleven, the first visible heading of any level is the one already recorded. review-278 re-read all fourteen the same way and reached the same answer. The `read` date of the two changed records stays `2026-09-18`, the day their landing was first recorded.
- **DigitalOcean** (3 flows: create a project, add a DNS record to a domain, create a cloud firewall), from three `docs.digitalocean.com` how-to pages. Start page `https://cloud.digitalocean.com/` lands on `/login`, "Log in to your account | DigitalOcean". The heading is "Log in to your account", **an `h3`**, the page's only heading element. The first reading recorded `""` because it looked at `h1` and `h2` only. "Don't have an account? Sign up" is a separate route.
- **Google Cloud** (2: create a project, grant a role to a principal), from `docs.cloud.google.com`. A third flow, enable an API, was written and then dropped: see the trimmed flows under pitfalls. `https://cloud.google.com/...` addresses redirect there, and the flows cite the landed pages. Start page `https://console.cloud.google.com/` lands on `accounts.google.com/v3/signin/identifier`, titled "Google Cloud Platform", heading "Sign in", with "Create account" separate. `accounts.google.com` is already paired with `google.com`, which is this pack's site.
- **Firebase** (2: register a web app, create a Cloud Firestore database). Start page `https://console.firebase.google.com/` lands on the same Google identifier page, titled "Sign in - Google Accounts".
- **AWS** (2: create an S3 bucket, turn on versioning for a bucket), from the S3 user guide. Start page `https://console.aws.amazon.com/` lands on `us-east-2.signin.aws.amazon.com/oauth`, "Amazon Web Services Sign-In", heading "IAM user sign in", with "Create a new AWS account" separate. That host is a subdomain of the pack's `aws.amazon.com`.
- **Netlify** (2: add a domain alias, create a site environment variable, which opens with the stop step below). Start page `https://app.netlify.com/` stays where it is: "Netlify", heading "Log in", "Sign up" separate.
- **Optimizely** (3: create an A/B test experiment, add a metric, schedule an experiment), all from one Zendesk article found through the help centre's own section listing, never from a guessed slug. Its title matched: "Steps to create an experiment – Support Help Center".
  **This is the wave's first use of decision A on a site nobody had argued about, so the evidence is recorded rather than only the conclusion.** A later reader who sees only "passes on decision A" cannot tell a checked site from an assumed one.
  - **The sign-in page.** `https://app.optimizely.com/` lands on `/signin`, titled "Optimizely", heading "Log In". It was read twice at 00:18:34Z. Its visible controls are, in full: "Need help logging in?", "Log In", "Log in using SSO", "Terms of Service" and "Privacy Policy", beside one email field and one password field. So the page shows no route for creating an account, and under the rule's first half it would be held. Decision A is the only reason it passes.
  - **Finding 1, "Get started".** `www.optimizely.com`'s "Let's grow" and its navigation's "Get started" both go to `/get-started/`, titled "Get started with Optimizely", heading "Start your journey with Optimizely". It reads "Share your objectives and challenges, and our team will help you identify the right solutions…", which is a sales contact, not an account. The page rendered no visible input outside a frame when read.
  - **Finding 2, the plans page.** `/plans/`, titled "Plans and pricing for Optimizely products", holds no line matching free, trial, $0, no credit card or sign up, case-insensitively. Among its visible links and buttons, the only one matching free, trial, sign up, get started, start, demo, contact, try, create or buy, apart from a product link, is "Request a demo". It goes to `/get-started/`.
  - **Finding 3, sign-up by address.** `https://app.optimizely.com/signup` goes to `https://www.optimizely.com/signup`, a 404 headed "Looks like you took a wrong turn".
  - The three site readings were taken between 00:18Z and 00:20Z, each once, with the URL and title printed on every read. **What they do not cover, said plainly:** a trial offered only inside a sales conversation, or on a page none of these links reach, would not show here. The ruling's test is self-serve sign-up anywhere on the site, and these are the site's own routes to one.
- **Auth0** (3: create a tenant role, create an Organization role, assign a role to a user). Start page `https://manage.auth0.com/` lands on `auth0.auth0.com/u/login/identifier`, "Log in | Auth0 Dashboard", heading "Welcome", "Sign up" separate. That host is a subdomain of `auth0.com`.
- **Expo** (2: create an organization, invite a member), from `docs.expo.dev/accounts/account-types/`. Start page `https://expo.dev/login` stays: "Log in — Expo", heading "Log in to Expo", with "New to Expo? Create an account" linking to `/signup`. That link rendered late, and the first read, taken at five seconds, did not list it.
- **Render** (2: roll back to a previous deploy, add an environment variable, which opens with the stop step below). Start page `https://dashboard.render.com/` lands on `/login`, "Render Dashboard", heading "Sign In to Render", "Sign up" separate.
- **Heroku** (2: add and remove a collaborator), from the Dev Center's collaborating article. Start page `https://dashboard.heroku.com/` lands on `id.heroku.com/login`, "Heroku | Login", heading "Heroku", with "New to Heroku? Sign Up".
- **Algolia** (2: create an index, upload a file of records), from the row's own import page. Start page `https://dashboard.algolia.com/` lands on `/users/sign_in`, "Sign in | Algolia". The first visible `h1` contains the "No account yet?" link to `/users/sign_up` inside it, so the recorded heading is its rendered text, "Log in NO ACCOUNT YET?".
- **Namecheap** (2: set up free email forwarding, point a domain at custom nameservers), from two knowledgebase articles, each verified by its title since the knowledgebase keys on article numbers. Start page `https://www.namecheap.com/myaccount/login/` stays, "Namecheap.com • Login", with no visible heading and "Sign up" going to `/myaccount/signup/`.
- **GoDaddy** (2: add an A record, forward a domain). `forward-my-domain-12123` redirects to `forward-my-godaddy-domain-12123`, "Forward my GoDaddy domain", and the flow cites the landed address. The flows start at `https://sso.godaddy.com/`, which stays: "Sign In to Your Account - GoDaddy", heading "Sign in", with "New to GoDaddy? Create an account" linking to `/account/create`. They do not start at the row's `dashboard.godaddy.com`, because that host answered Chrome's "This site can't be reached … ERR_TIMED_OUT" at 01:01Z. The pack's `signInURL` was left as the row has it.
- **Hostinger** (1: point a domain at a Hostinger site with nameservers). Start page `https://auth.hostinger.com/login` stays, "Hostinger login | Sign in to Hostinger account". The heading is "Log in", **an `h3`**, the page's only heading element; the first reading recorded `""` for the same reason as DigitalOcean. "Sign Up" is separate.
- **SONNY-526's two rows.** `https://expo.dev/login` lands on itself, "Log in — Expo", and `https://railway.com/login` lands on itself, titled "Login". Both guessed addresses are real sign-in pages, so neither row's `sign_in_url` changes. Railway's pack stays shallow for the separate reason below.
- **Render's and Netlify's environment-variable flows each open with a stop step.** The step reads: "Stop and ask before adding or reading a value. Environment variables often hold sensitive values that the user enters themselves, so this flow never types or reads one without the user saying so." Neither flow reads or types a secret; the person does. But each flow ends at the panel where such values are entered, and the credential rule cannot see that, because it reads what the steps say and not where the flow ends up. The ruling recorded on SONNY-510 sets the trigger by destination: any flow that ends where credentials, keys or environment variables are entered is guarded this way.
  - **It follows an existing pattern.** Quo's call flow opens with "Stop and ask before dialling a number outside the US or Canada", for a money path one click away that the money rule also could not see. Same shape, and in the same place: the step a person reads before Sonny acts.
  - **It is written as an instruction to stop, never as a description of the page.** A step that describes a limit reads identically to one that binds, and that difference cost PR #273 a round.
  - **The wording avoids "secret" and "credential"**, because both are words the credential rule refuses.
  - This is distinct from SONNY-534, which is about a flow that *creates* a credential and which the rule should refuse outright. These two do not create one.
- **No start page on this branch carries a biometric or recording consent.** A scan of each start page's text for biometric capture or recording, of a voice, a call or a meeting, found none. Four carry a terms line, which is evidence and not the test: DigitalOcean's, AWS's, Netlify's and GoDaddy's.

Behavior preserved (required, no blanket claims):
- The other 459 packs are unchanged. The key-by-key comparison above finds fourteen changed packs and names them.
- The six held sites keep their shallow packs byte for byte, and their catalogue rows are unchanged.
- Every header field of the fourteen deepened packs is unchanged, triggers included, so matching a command to these sites works exactly as before.
- `SkillPackStartPageRule.identityHosts` is unchanged, and so is every Swift file. The two landings off a pack's own site on this branch are both on `accounts.google.com`, which was already paired with `google.com`.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **Five places where a flow was trimmed to pass a rule, each with its reason.** A step trimmed to pass a rule reads exactly like one trimmed for clarity, so every one is named here:
  1. **Render's custom-domain flow is left out, because the task can cost money.** `render.com/docs/custom-domains` says "Each custom domain you add beyond this amount costs $0.25 per month". The ticket warns that the money rule cannot see a flow that ends in a purchase without naming money. Written faithfully, the flow names the cost and the rule refuses it. Written without the cost, it hides a charge. So it is not written. Render ships a rollback flow and an environment-variable flow instead.
  2. **Firebase's create-a-project flow is left out, because its steps accept terms.** `firebase.google.com/docs/web/setup` includes "If prompted, review and accept the Firebase terms, then click Continue." That is a terms acceptance inside the flow. Firebase ships a register-a-web-app flow and a create-a-database flow instead.
  5. **Google Cloud's Enable-an-API flow is left out, for the same reason as Firebase's.** `docs.cloud.google.com/service-usage/docs/enable-disable` says "Some APIs ask you to first accept their Terms of Service, or are in preview and require approval before you can use them." The flow's own steps never named the acceptance, so for those APIs its last step, Enable, would have led to a terms acceptance the flow does not mention. Trimming could only have hidden that, so the flow is dropped. The page was re-read on 2026-09-19 to judge it, and the row's `doc_url` cells no longer cite it.
  3. **Google Cloud's billing field is left to the form.** The create-a-project page says to "enter a project name and select a billing account as applicable". Naming the billing account in a flow whose last step is Create trips the money rule as `create + account`, beside `billing`. The steps name the project name and the Parent resource box, and the billing field, which the page itself calls "as applicable", is left to what the form shows.
  4. **GoDaddy's last step names neither a code nor a password.** Both GoDaddy pages end with identity verification for a domain under Domain Protection: "enter the code we sent via SMS, or enter the code from your authenticator app. Otherwise, enter the one-time password…". Each is a credential the rule refuses and Sonny must never type. The last step says GoDaddy "then asks the account owner to confirm their identity", which tells the reader a check is coming without describing how to pass it.
- **GoDaddy starts at `sso.godaddy.com`: a measurement, not a preference.** The row's `dashboard.godaddy.com` was read at 01:01:22Z and gave no document, then at 01:01:34Z showed Chrome's "This site can't be reached — dashboard.godaddy.com took too long to respond … ERR_TIMED_OUT". `https://sso.godaddy.com/` was read at 01:01:50Z and again at 01:03:02Z, and landed on itself both times. The pack's `signInURL` is unchanged, since this ticket moves only the `doc_url` cells, so whether the dashboard host answers normally is left for a later reading.
- **Heavy machine load changes what the browser tools return, in ways that read as answers.** The load average was 74 at 00:39Z (`uptime`). Three effects showed:
  - `browser_batch` calls timed out at their ceiling and **kept executing afterwards**, so the next read could land on the page the timed-out batch had navigated to. A read labelled Render once returned Heroku's page, and was caught only because it printed its own URL.
  - `get_page_text` twice returned the previous page's text under the new page's tab. A title check on every read is what caught both.
  - Chrome dropped the whole tab group once, and the signed-out check was repeated in the new group.
- **The extension refuses screenshots on some domains, which is a different failure from a stalled renderer.** On `appstoreconnect.apple.com` it answered "Permission denied for this action on this domain". That page's sign-in form sits in a cross-origin `idmsa.apple.com` frame, which no script reads, so a screenshot was the only reader left, and it is refused.
- **The extension's output filter blocks any script result containing a query-string shape.** Page text was read with `?`, `=` and `&` replaced by spaces and URLs elided, as SONNY-510's gotchas record for URLs.

Known limitations / deferred scope — the six holds and the backlog, each named on SONNY-520 with what was tried:
- **Microsoft Azure: left shallow here and moved to SONNY-531** by the coordinator, beside QuickBooks, Zoho Books, Microsoft 365 and Zoho Desk. The page passes the rule. The pack still cannot load, because `identityHosts` pairs `login.microsoftonline.com` only with `office.com` and `microsoft.com`, and this pack's site is `azure.com`. **Why this lane could not add the pairing, beyond the never-touch list:** `theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake` asserts that the list *equals* the landings the shipped pack files make, built from the raw files without consulting the list. A pairing added ahead of the pack that needs it therefore fails, so the pairing and the pack must land in one change, and that change cannot be one of three parallel pack lanes. The reading, in the record's format, so that lane confirms rather than rediscovers:
  - `url` `https://portal.azure.com/`, `landedURL` `https://login.microsoftonline.com/organizations/oauth2/v2.0/authorize`, `title` "Sign in to Microsoft Azure", `heading` "Sign in", `offers` `sign-in`, `read` `2026-09-18`. Read at 01:00:33Z, and twice more at 03:18:57Z and 03:18:59Z after a fresh signed-out check at 03:18:40Z.
  - **The heading is "Sign in", and it comes from a `role=heading` div rather than an `h1` or `h2`.** The page has no visible `h1` or `h2`. The "Sign in" a visitor reads is a `div` with `role="heading"` and `aria-level="1"`. The coordinator reproduced that markup exactly. The ruling recorded on SONNY-510 on 2026-09-18 is that a visible `role=heading` element is the page's heading, because the field records what a visitor encounters, whatever the tag. So the record says "Sign in". The markup is unusual, and the SONNY-531 lane will find it that way, not misread. An empty heading means no visible heading of any kind, and only that.
  - **Why it is `sign-in`.** The form asks for "Enter your email, phone, or Skype.", with Next, "Sign-in options" and "Can't access your account?". "No account? Create one!" is a separate link, `id="signup"`, going to `login.live.com/oauth20_authorize.srf`. The page carries no terms line in its body text and no biometric or recording consent.
- **Google Play Console: no start page on its own site.** `https://play.google.com/console/`, `/console/u/0/developers` and `/apps/publish/` all land on `https://google.play/business/`, a marketing page on another domain. Its "Go to Play Console" button goes to `accounts.google.com`, which is not on `play.google.com`, so it cannot be a declared start URL.
- **The Claude Platform (`anthropic_console`): held twice over.** `https://console.anthropic.com/` lands on `https://platform.claude.com/`, "Claude Platform", which is off the pack's own site and unpaired. That page also shows one "Continue with Google / email / SSO" form with no separate sign-up route. Most of its documentation is about API keys besides.
- **GrowthBook: one form for both.** `https://app.growthbook.io/` lands on `growthbook.auth0.com/login`, titled "Welcome to GrowthBook - Sign in or register", with the heading "Register for GrowthBook Cloud!". It is also off the pack's site and unpaired. This is what SONNY-510 found, re-read now.
- **Railway: the tl;dv shape.** `https://railway.com/login` offers "Continue with GitHub" and "Log in using email" and no sign-up route. The site does offer self-serve sign-up: `/pricing` reads "Free", "$0", "Start with a 30-day free trial with $5 credits" and "No credit card required", and its "Deploy with Hobby" goes to `railway.com/login` itself. So the log-in page is how accounts get created.
- **App Store Connect: held under the rule.** This lane could not read it. `https://appstoreconnect.apple.com/` lands on `/login`, titled "App Store Connect", and its sign-in form sits in a cross-origin `idmsa.apple.com/appleauth/auth/signin` frame that no script reads. Screenshots first stalled Chrome, then the extension refused them on the domain (pitfalls). **review-278 did read it**, as the coordinator relayed:
  - Apple's sign-in frame shows no route for creating an account;
  - Apple offers self-serve developer enrollment elsewhere on its site.
  That is the tl;dv shape, so the page is held, not unreadable. "Held with a reason" and "could not be read" are different records, and only the first is true. The reading is the reviewer's, not this lane's.
- **Only fourteen of 266.** Shallow packs on catalogue rows whose `task_flow_docs` says `deep` number 266 at `8f3d1d02` and 252 at `7e457294` (for each row with that value, its pack at that commit read for `depth`, over `git show <sha>:docs/sonny-skill-sites.tsv` and a `git archive` of the packs folder).

Open questions (required, write "none" if true): none on this branch. The Azure pairing is SONNY-531's, and App Store Connect is held on the reviewer's reading.

Next branch: whatever the coordinator hands out next in wave 13.
