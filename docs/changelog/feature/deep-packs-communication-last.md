### Branch: feature/deep-packs-communication-last
Status: complete
Date: 2026-09-17, fix round 2026-09-18
Tickets: SONNY-502 — deep Skills packs for the communication group, third session; ten more of the group's twenty-five sites converted, the last five named on the ticket with the reason each is held. SONNY-507 — this group's four rows citing pages a flow must not be built from (callrail, justcall, skool, otter) now cite the pages their flows were built from, and otter's tracking parameter is gone; samcart and quickbase are other lanes' and untouched.
Reviewed by: fresh session (per WORKFLOW.md step 7), review-273, one round at `28860203` (a pre-hop head, `972db676` after the hop; the map is in Tests below) with a browser connected from the start. It read all seventeen source pages rather than a sample and found **no wrong flow**. Two findings blocked the merge, and the founders answered both. **1**: several start pages carry a "by continuing you agree" line, which a same-day ruling on another PR had treated as a reason to hold. The founders replaced that with one rule — a start page is acceptable when its form signs in an existing account and the site has a separate route for creating one, and not when that same form is also how an account gets created; a terms line is evidence, not the test. Read AI, Krisp, Quo, Fathom and Otter were re-judged under it, and all five pass, recorded per site on SONNY-502 and in the pitfalls below. **2**: Quo's call flow did not tell Sonny to stop before an overseas number that existing credit would pay for; it now opens with that stop (`d49c9b47` before the hop, `c9751124` after it). Finding 3: all ten start pages are recorded on SONNY-502 in PR #272's format, from the reviewer's readings, each re-opened and confirmed. Finding 7: the fireflies bad seeds are on SONNY-507. Finding 8: this entry's flow count now carries a command that runs. Findings 4, 5, 6 and 9 are left where the review routed them — Quo's bare-origin start URL and the peer sign-up controls to PR #272's retrofit, and JustCall's 2021 wrap-up page and the Skool cover-image ADD as recorded residuals. **Re-check at `2913ae10`** (pre-hop, `31a25c20` after the hop) (the coordinator verifies this round directly, and no reviewer returns): both blocking findings closed, nothing new blocks. Three record corrections, all made in this round and on the tickets, none in code: Krisp's heading under the visible-heading definition, Otter's title recorded both ways, and the withdrawn claim that Fathom is the one site a stricter rule would move — all three are in the pitfalls below. Its three non-blocking notes on Quo's stop — that the sentence is the only guard and only advice, that the contact and conversation routes do not tell Sonny to read the number before the click that dials, and the optional tightening — are on SONNY-506 by founder decision, and the flow is unchanged.

Spec sections covered: none directly — this branch is Skills pack data for sites the catalogue already lists. The Skills mechanism itself (pack format, loader, content rules, trigger check, guidance budget) is SONNY-452's and SONNY-482's and is untouched here.
Files changed: ten pack files under `Sources/MacAgent/Resources/SkillPacks/` (`aircall`, `callrail`, `fathom`, `justcall`, `krisp`, `otter`, `quo`, `read_ai`, `skool`, `webex`, each shallow to deep), `docs/sonny-skill-sites.tsv` (ten rows, 27 `doc_url` cells), `docs/manual-tests/feature/deep-packs-communication-last.md` (new), and this entry. No Swift source, no test, no other group's pack, no other catalogue row, no other column.
Tests: **every figure here is measured at `40b2bdf0`, the head after this branch's one hop onto `main` at `6addb16f`** (the hop is described at the end of this field). `env CLANG_MODULE_CACHE_PATH="$PWD/.build/clang-module-cache" swift test --disable-sandbox -Xswiftc -F -Xswiftc /Library/Developer/CommandLineTools/Library/Developer/Frameworks -Xlinker -rpath -Xlinker /Library/Developer/CommandLineTools/Library/Developer/Frameworks -Xlinker -rpath -Xlinker /Library/Developer/CommandLineTools/Library/Developer/usr/lib` -> exit 0, `Test run with 3446 tests in 250 suites passed after 95.820 seconds with 8 known issues.`, `grep -cE 'failed after [0-9]'` → 0 and `grep -c 'recorded an issue'` → 0, at `40b2bdf0` with `git status --porcelain` empty. It was run rather than carried, though the hop's range held no Swift file. It started at 13:55:18 gated on `ps -axww -o command | grep -cE '(^| )/[^ ]*(swift-frontend|swift-driver|swiftpm-testing-helper|xctest)'` reading 0 twice, 10 seconds apart, against 4 for the same anchored search on `claude` as the control that the search can see anything; the stated 15-minute fallback did not fire, and the one-minute load average was 5.20 at the start and 6.84 at the end. The catalogue-completeness tests are shown to have run rather than skipped by their own log lines in that run: `Test everyCatalogueRowHasExactlyOnePack() started.` then `… passed after 11.477 seconds.`, and `Test everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue() started.` then `… passed after 11.296 seconds.`. The same command with `--filter 'Skill'` -> exit 0, `Test run with 65 tests in 5 suites passed after 7.443 seconds.`, 0 failure lines and 0 issue lines, at `40b2bdf0`, gated the same way (load 4.43), with `aDeepPackNeedsDocumentedOrSiteReadFlowsAndAShallowPackSitsOnAnyRow()` started and passed beside the two above. `server/scripts/check-secrets.sh` -> exit 0, `clean (1281 tracked files scanned, 12 patterns, 8 baselined fixtures)`, and `scripts/no-attribution tree` -> exit 0, `0 of 1277 tracked file(s) carry an attribution (4 excluded by path)`, both at `40b2bdf0` with `git status --porcelain` empty, so the index they read was that commit's; `git ls-tree -r --name-only 40b2bdf0 | wc -l` → 1281. **Those two scan the whole tree and cannot ride the proof below**, whose one MOVED path is `docs`: this entry's own edit changes a file's contents and adds none, so at the merging head each reads the same population — `git diff --name-only 40b2bdf0 <merging head>` is the single path `docs/changelog/feature/deep-packs-communication-last.md`, a file already tracked at `40b2bdf0`. `scripts/changelog-order read` and `scripts/changelog-order manual-tests` are run at the entry's own commit and reported on SONNY-502 and the PR, since a figure about this file cannot be printed inside it. No server command is owed: this branch touches nothing under `server/`, and the hop's range touched nothing there either. **The carry to the head that merges**: the entry commit on top of `40b2bdf0` changes only this file, so the figures above are carried across it under WORKFLOW.md step 5's tree-identity proof — `Sources`, `Tests`, `Package.swift`, `docs/sonny-skill-sites.tsv`, `docs/manual-tests` and `server` each read as one hash at `40b2bdf0` and at the entry commit, taken as the **exit code** of `git rev-parse --verify --quiet "${sha}:${pth}"` on each side rather than the emptiness of its output, in a loop whose variable is `pth` and not `path`, and shown producing all three outcomes before any is believed: IDENTICAL on those six, MOVED on `docs`, REFUSED on a path that resolves in neither tree. The proof's own output is on SONNY-502. **The one hop.** When this branch was next to merge it was rebased once, from its pre-hop merge-base `0d645464` onto `main` at `6addb16f`, and pushed with `--force-with-lease` pinned to its pre-hop head `dff2973f`. The range `git diff --name-only 0d645464 6addb16f` is 22 files — PR #269's tooling and records and PR #270's twelve pack files plus `docs/sonny-skill-sites.tsv` — and `grep -cE '\.swift$|^Package\.swift$|^Tests/'` over it → 0, against `grep -c 'skillpack\.json$'` → 12 as the control. None of #270's twelve packs or twelve changed catalogue rows (30 cells) is one of this branch's ten. The rebase met no conflict, and `git range-diff 0d645464..dff2973f 6addb16f..40b2bdf0` marks all five commits `=`. **Pre-hop to post-hop, for every commit this branch stamped:** `1041a0c0` → `bf64474d`, `28860203` → `972db676`, `d49c9b47` → `c9751124`, `2913ae10` → `31a25c20`, `dff2973f` → `40b2bdf0`. The left-hand SHAs are no longer ancestors of this branch (`git merge-base --is-ancestor <sha> HEAD` exits 1 for each), so wherever they appear in this entry they are timestamps on history, not citations; no figure rests on them. Before the hop the same suites were green at `d49c9b47`, and one gated full run there was red only in SONNY-515's three `SonnyBackendClientTests` authentication tests, which passed alone — history, superseded by the measurements above.
Mutation plan: none. Data only — this branch adds no executable line, and the ticket says so explicitly.

Behavior added:
- Ten sites gain deep packs, twenty flows (`for s in aircall callrail fathom justcall krisp otter quo read_ai skool webex; do git show 40b2bdf0:Sources/MacAgent/Resources/SkillPacks/$s.skillpack.json | python3 -c 'import json,sys; print(len(json.load(sys.stdin)["flows"]))'; done | paste -sd+ - | bc` → 20, and the same loop at `6addb16f` → 0 as the control): Aircall (create a contact, edit a contact), CallRail (download a call's recording, rename a tracking number), Fathom (share a call recording, copy a call's transcript), JustCall (turn call recording on or off for a number, set a wrap-up time after calls), Krisp (upload a file to transcribe, create a meeting folder), Otter.ai (choose which meetings the Notetaker joins, choose who recorded meetings are shared with), Quo (make a call, set up hold music for a phone number), Read AI (share a meeting report, download a meeting report), Skool (create an event in your group, create a course in the Classroom), Webex (create a space, add people to a space).
- Each of those ten rows' `doc_url_1..3` cells now carries that pack's flow sources in flow order, with the unused cells emptied. Quo, Otter and Aircall each rest both flows on one page, so they carry one cell; the other seven carry two.
- **Quo's call flow opens with a stop** (the fix round, `c9751124` after the hop): "Stop and ask before dialling a number outside the US or Canada", ahead of every way the flow can dial, because a call there is paid from credit already on the account whether or not the flow adds any.
- SONNY-507's four bad seeds in this group are gone with it: callrail's billing-and-invoices and payment-method articles, justcall's change-my-credit-card article, skool's cancel-subscription article and otter's delete-your-account article, plus otter's `_gl=` tracking parameter. A scan of the ten rows' cells for `_gl=` or `utm_` answers 0, against 1 for the same scan over otter's row at `0d645464`.

Behavior preserved (required, no blanket claims):
- **The shipped catalogue still loads with no failure and every row still has exactly one pack.** `everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue` and `everyCatalogueRowHasExactlyOnePack` both pass, and both are shown started as well as passed, because a skipped completeness test reads exactly like a passing one.
- **The 463 packs this branch does not touch are byte-identical.** `git diff --name-only 6addb16f 40b2bdf0 -- Sources | wc -l` → 10, all ten of them pack files (the same list through `grep -vc '\.skillpack\.json$'` → 0), of 473 (`git ls-tree -r --name-only 40b2bdf0 -- Sources/MacAgent/Resources/SkillPacks | grep -c '\.skillpack\.json$'`).
- **No other catalogue row and no other column moved.** Measured cell by cell at `40b2bdf0` against the post-hop merge-base `6addb16f` — re-baselined at the hop, because against the old base `0d645464` the same script reports 30 findings, which are exactly the 30 cells PR #270 moved on twelve rows that are not this branch's: 473 rows before and after, 10 rows changed, 27 cells, every one a `doc_url` cell on one of this branch's rows, 0 findings, exit 0. The control fires: the same script over a copy tampered three ways — `telegram` removed, `slack.doc_url_1` rewritten, `webex.name` rewritten — exits 2 and names all three.
- **Every flow's source equals its row's non-empty `doc_url` cells in flow order**, for all ten packs, checked by a script rather than by eye.
- **The trigger check still passes for every shipped pack.** No trigger was touched; all ten keep the words they shipped with, so `everyShippedTriggerIsDistinctiveOrAnchoredToItsSite` answers about the same strings it answered about before.
- **The money and credential content rules still refuse what they refused**; no rule file, loader or test was touched.
- **The five sites held this session are untouched**, packs and rows both: `google_chat`, `google_voice`, `mattermost`, `mighty_networks` and `tldv`. So are the ten packs this group's two earlier sessions shipped — `dialpad`, `discord`, `google_meet`, `loom`, `simpletexting` and `streamyard`, then `clicksend`, `demio`, `livestorm` and `textmagic` — none of which appears in `git diff --name-only 6addb16f 40b2bdf0` (a `grep -cE` for their ten file names over it → 0, against 2 for `aircall` and `callrail` as the control).

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **Five of the fifteen are held, each for a start-page reason, and all five are recorded on SONNY-510 as evidence for its open question rather than decided here.** `tldv`: every app URL lands on `tldv.io/app/login`, "Sign in with your work email", Google, Microsoft and SSO only, a checkbox "I agree to the Terms of Service and Privacy Policy", and no sign-up route anywhere. This lane weighed it as a sign-in page with a formality attached; the coordinator's reading is the decision and it is the better one — a form that takes no email and no password is SSO-only, a product with no separate sign-up creates the account on first SSO login, and nobody accepts terms to sign in to an account they already have. So it is shape 3 (account creation as the primary control) wearing a sign-in label, and **the tell is the terms checkbox together with the absence of any sign-up route**, neither alone. `mattermost`: mattermost.com's own Login menu links "My Workspace" to `customers.mattermost.com/cloud/connect-workspace`, an on-domain page with no account creation that genuinely reaches a Cloud workspace at `<name>.cloud.mattermost.com` and cannot reach a self-hosted server, the common case — the first example on SONNY-510 that fails while neither leaving the domain nor offering sign-up. `mighty_networks`: `mightynetworks.com/sign_in` only emails links to your networks, which live on `<name>.mn.co` or a custom domain, so the flow's first step is reachable only off the pack's domain. `google_chat` and `google_voice`: measured by the coordinator on 2026-09-17 in a profile with no Google account at all, read twice — `chat.google.com` lands on `https://workspace.google.com/products/chat/`, h1 "AI-powered chat for teams of all sizes", 0 password fields, a marketing page on another host; `voice.google.com/landing` lands on `https://accounts.google.com/v3/signin/identifier?continue=https://voice.google.com/landing`, 1 password field, a sign-in page on another host. **Chat is the second Google product to land on `workspace.google.com` signed out, after Meet**, so it is Google's pattern for signed-out product URLs rather than one site's quirk. This lane's own profile still shows a remembered, signed-out Google account on the chooser and could not take that reading; the founders chose to hold both rather than clear it.
- **SONNY-510's cheap fix went three for three.** Krisp's `app.krisp.ai/` lands on `/sign-up` ("Sign up for free", Ghost's shape 1); JustCall's `app.justcall.io/` lands on the marketing homepage at `justcall.io/`; Skool's `www.skool.com/` is "Discover Communities or Create Your Own", a grid of communities priced by the month. In each case the row's own `sign_in_url` path was right — `app.krisp.ai/login` ("Sign in to your account", "Sign up" a link), `justcall.io/login/` (lands on `/apex/login`, "Sign in to your account", "Start free trial" a link), `www.skool.com/login` ("Log in to Skool", "Sign up for free" a link) — and those three packs start there.
- **Money sat in a collapsed FAQ, and only the DOM shows it.** Quo's "Making calls" page answers "Can I make international calls?" with "enable international calling in your settings and add credit to your account for calls outside the US and Canada" — inside an accordion that `get_page_text` never returned, because a collapsed panel is not in `innerText`. The same rule SONNY-463 records for absences (read the DOM when a claim rests on what a page lacks) applies to money neighbours: a lane checking a page for a purchase path by its rendered text would have passed this one. **And the second half, which review-273 found and this lane missed:** stopping short of *adding* credit is not stopping short of *spending* it. On an account that already has international calling and credit, the first version's steps would dial whatever number the user gave, and its last step only described the flow's scope. The flow now opens with "Stop and ask before dialling a number outside the US or Canada". The content rule cannot see either half, and its own doc comment names the gap: a flow that reaches a money page through steps that name nothing about money. Other neighbours read and kept out of flows: Mighty Networks' "add a payment option for access" beside creating a Space, and Skool's Classroom listing "How to set up one time course purchases?" beside the course-access setting the course flow names. Two flows state a paid-plan precondition because their page states it first — Krisp's upload is "Core and Advanced plans", Read AI's `.mp4` download is the Enterprise plan — which is the convention SONNY-510's paid-precondition founder question is still open on.
- **The start-page rule is now one rule, and all five pages it was re-applied to pass** (founders, 2026-09-18). A start page is acceptable when its form signs in an existing account and the site has a separate route for creating one, and not when that same form is also how an account gets created; a terms line is evidence, not the test, and a cookie notice is never relevant. Each of the five was checked by following its sign-up route by clicking only. Read AI's Create account tab changes the URL to `/analytics/signup` ("Read - Sign up", "Continue with …" controls, "By creating an account, I agree…"), while its sign-in controls all read "Sign in with …" under "By signing in, I agree…". Krisp's `/sign-up` has its own email form submitting "Sign up" where `/login`'s submits "Continue". Quo's `/signup` ("Create an account to start your free trial") offers "Continue with email" where `/login` offers "Email code" and "Email & password". Otter's `/signup` has `h1` "Create account" and a different control set. **Krisp and Quo put a word-for-word identical terms line on both pages**, which is why the line cannot be the test. **Fathom is the one site whose sign-in request could be read, and that is all it is.** Its sign-up page's two forms post to the same endpoints as its sign-in page's, `/users/auth/google_oauth2` and `/users/auth/microsoft_graph`, but with different requests. Read as the length of each query value, the sign-in Google scope is 20 characters against sign-up's 77, and Microsoft's is 37 against 133. Review-273 matched that digit for digit and read the scope tokens: sign-in asks Google for `email`, `profile` and `openid`, and sign-up adds `calendar.events.readonly`. So they are not the same form. An earlier version of this record, and a note on SONNY-510, called Fathom the site a stricter rule would move. **That is withdrawn.** Otter's, Krisp's and Quo's provider buttons have no readable destination on either page, so under a stricter rule they would be unmeasured rather than cleared, and a tightening would need a full sweep. What no reading here can settle, on any of the five, is whether a provider button creates an account the first time an unknown identity uses it; that would take starting a sign-in, which a lane does not do.
- **A start-page record's heading is the first heading a visitor can see, and its title can depend on when it is read** (founders, 2026-09-18). Both of these went wrong here first. **Heading:** two definitions were in use, and they disagree on Krisp. The first `h1` or `h2` in the document there is the cookie dialog's `H2`, "Privacy Preference Center", which is not visible. By the visible definition, which wins, Krisp has no heading, and its record says so. A heading nobody can see describes nothing, and a consent banner's presence depends on the profile and the jurisdiction. Visible is measured by a non-zero rendered rectangle, with `display` not `none`, `visibility` not `hidden` and `checkVisibility` true. Every record whose heading came from an element was re-read that way rather than translated. The case for re-reading is Otter: this afternoon its hidden cookie `H2` came *before* its visible `H1` "Sign in" in document order, the reverse of the morning, so under the old definition its record would have flipped with the banner's timing. Fathom's `H1` "Sign in to Fathom" and Read AI's promotion-panel `H1` "Meetings + Ask Read" are visible and stand. **Title:** arriving the way the flow arrives is necessary and not sufficient. By the identical route, `otter.ai/home`, this lane read Otter's title as "Otter.ai - Access real-time and shareable meeting notes" three times in the morning and "Sign In: Otter Voice Meeting Notes - Otter.ai" twice in the afternoon, and review-273 read both by that route too. The redirect is client-side, the head can hold two `<title>` elements, and `document.title` returns whichever comes first. So Otter's record carries both titles, with a note that the page reports whichever comes first. This lane first put the difference down to the route. That was wrong, and it is corrected on SONNY-510 as well as here.
- **A sixth bad seed in this group.** `tldv`'s `doc_url_2`, "How to stop tl;dv", walks through cancelling the subscription through Stripe and deleting the account. Left in place because `tldv` did not ship, as `google_voice`'s "Delete your Google Voice number" was last session; both are named on SONNY-507. Otter's help-centre home leads with "Cancel an Otter subscription", "Delete your Otter account" and payment history as its first links, so following an index's most prominent entries is its own way to meet these seeds.
- **Three readers failed three different ways, and a screenshot was the reader that settled one of them.** JustCall's sign-in form lives in an iframe from `ui-vercel.justcall.io`: `get_page_text` answered "No text content found" and `read_page` found only the Intercom button, two readers agreeing on nothing about a page that renders fine — so SONNY-463's rule that two agreeing readers make an exclusion honest would have excluded a readable page here; a screenshot read twice is what showed the form. Aircall's article answered `get_page_text` with only its footer (`main`'s `innerText` 77 characters) while a DOM read of the article's container, with scripts and styles stripped, returned 4,687 characters of steps, read in chunks. And on two sign-in pages — Fathom's, and Krisp's sign-up, which says it is protected by reCAPTCHA — `javascript_tool` hung for its 45-second timeout with the renderer unresponsive, and after two more tabs hung (tl;dv's sign-in and Krisp's help centre) the whole tab group disappeared; `get_page_text` and `read_page` still answered on a fresh tab. Separately, Quo's `my.quo.com/` read as path `/` with an empty body at 8 and 12 seconds and became "Log in | Quo" only at about 20 — the slow-page trap SONNY-463 records, at roughly double the windows recorded there.
- **Webex's articles are tabbed by platform, and the default tab is not the one Sonny uses.** "Create a space" and "Add people to a space" each carry Desktop, iOS, Android and Web panels, `get_page_text` returns only the active one (Desktop), and the Web steps differ — Desktop starts "At the top of your Spaces list", Web starts "In the app header". Both flows were written from the Web panel, read from the DOM by the tab's own `href`. "Start a conversation" has no Web panel at all and was not used.

Known limitations / deferred scope:
- Five of the group's twenty-five sites are still shallow: `google_chat`, `google_voice`, `mattermost`, `mighty_networks`, `tldv`, for the start-page reasons above. None is blocked on reading: tl;dv's two flows (Uploads; Understanding and setting your preferences), Mattermost's create-channels page and Mighty Networks' create-and-manage-Spaces page were read this session and are recorded on SONNY-502, as Google Chat's and Voice's were last session. What they wait on is SONNY-510's answer, which is the named landing spot; SONNY-507 keeps the general bad-seed question and the two seeds left in place.
- Livestorm's single-flow limitation from this group's previous session is unchanged and not this branch's.

Open questions (required, write "none" if true): none. The three judgement calls this session met — how to take the Google start pages, whether Mattermost's connect-workspace route makes it shippable, and whether tl;dv's sign-in is an acceptable start page — were put to the founders during the session and decided: hold, hold, hold. Recorded on SONNY-502.

Next branch: whatever SONNY-510 decides for start pages; this group's remaining five wait on it, per wave 12's plan on SONNY-463.
