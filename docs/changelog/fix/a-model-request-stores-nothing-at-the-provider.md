### Branch: fix/a-model-request-stores-nothing-at-the-provider
Status: complete
Date: 2026-09-17
Tickets: SONNY-513 — the two Responses adapters now send `store: false`; the probe that found it, the population sweep, the contract reconciliation and the stale-SONNY-110 wording all landed with it.
Reviewed by: fresh session (per WORKFLOW.md step 7) — pending at the time of writing.

Spec sections covered: §16.5 (provider routing and provider-specific retention configuration stays server-side). Contract §4.5 rule 5 is amended by this branch rather than merely referenced.
Files changed: `server/src/model/vision.ts`, `server/src/model/openai.ts`, `server/src/model/cerebras.ts` (a comment recording why it deliberately gets no field), `server/src/config.ts` (doc comment only), `server/.env.example` (comment only), `docs/sonny-backend-api-contract.md` (§4.5 rule 5), `server/test/screen.test.ts`, `server/test/model.test.ts`, plus this entry, `docs/manual-tests/fix/a-model-request-stores-nothing-at-the-provider.md` and `mutation/plans/fix/a-model-request-stores-nothing-at-the-provider.txt`.
Tests: `npm test` → 935 passed, 482 skipped (1417) in 34 files at `b5795402`; `npm run test:db` against a lane-named Postgres → **1417 passed (1417) in 60 files, nothing skipped**, at `b5795402`. `npm run build` exit 0, `npm run typecheck` exit 0, `server/scripts/check-secrets.sh` exit 0 (1261 tracked files), `scripts/no-attribution` exit 0 (0 of 1257 files, 0 of 265 PR bodies) — every exit read with nothing between the command and `$?`.
Mutation plan: `mutation/plans/fix/a-model-request-stores-nothing-at-the-provider.txt` (founder-triggered, not run on this branch; the four mutants were each run once by hand while the tests were written, and each was killed by the named test alone).

Behavior added:
- `POST /v1/screen/analyze` sends `store: false` to the provider, so a redacted capture's reply is not kept as a retrievable response.
- `POST /v1/plan` and `POST /v1/research/synthesize` send the same field, so command text and model replies are not kept either.

Behavior preserved (required, no blanket claims):
- **Screen control end to end.** The rest of `/v1/screen/analyze`'s body is byte-unchanged — same `model`, same single `input` message, same `input_text` + `input_image` parts, same data URL built from the client's own base64 and media type. §4.5 rules 1 and 2 are still asserted by their existing tests, which still pass.
- **Planning and synthesis.** `/v1/plan` and `/v1/research/synthesize` keep their `reasoning`, `text.verbosity` and strict `json_schema` format block; the structured-output test still passes unchanged.
- **The Cerebras planner.** Untouched on the wire — it gets a comment and no field, deliberately (below).
- **Anthropic, transcription and search routes.** Not touched: `/messages` is a stateless API with no such concept, `/audio/transcriptions` is multipart with no `store` parameter, and Tavily is not a model API.
- **The Mac half.** `Sources` and `Tests` are byte-identical between the branch base and its head — `git rev-parse --verify --quiet dc73131c:Sources` and the same at `b5795402` both answer `d69f4725…`, `Tests` answers `f2be2c0d…` at both, with `server` and `docs` reported MOVED and a nonexistent path REFUSED in the same loop, so the check is shown able to produce a negative. The Swift half's commands are therefore not owed, proved rather than assumed.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **The defect was an absent field, which is why the tests assert presence and value separately.** `expect(body.store).toBeFalsy()` passes on `undefined`, which is exactly the state being fixed — so a test written that way would have been green against the bug. Both tests assert `Object.hasOwn(body, "store")` is true *and* the value is `false`, and the mutation plan carries a *drop* mutant beside each *flip* mutant for the same reason: a plan holding only the flips would report coverage the tests do not have.
- **`store` is a Responses-API property, not an OpenAI-family one, and Cerebras deliberately gets nothing.** The documented stateful 30-day default belongs to the Responses API; Chat Completions is a different API, Cerebras does not list `store` among the parameters it accepts, and it documents nothing about unrecognised fields (read 2026-09-17 at `inference-docs.cerebras.ai/api-reference/chat-completions`). Sending one would have been an unverifiable change to a live route against an endpoint no test here can reach, to remove an exposure not documented to exist. **This is a deliberate narrowing of SONNY-513's acceptance criterion**, which says "every model request that carries user content"; the criterion's purpose is met and its literal text is not, and the deviation is recorded on the ticket for a reviewer rather than resolved silently.
- **A grep for `store:` over `server/src` answers with DI wiring, not with request bodies.** `git grep -nE '(^|[^a-zA-Z_])store:' dc73131c -- server/src` returns **15** lines at the branch base, of which **14** are a `store: someStore` constructor argument in `app.ts`, `content/hook.ts`, `routes/billing.ts` and their neighbours and **1** is prose inside a doc comment (`model/limits.ts:306`, "it reads the same store: it") — and **none** is a provider request body. The first draft of this sentence said all fifteen were constructor arguments; appending `| grep -vE 'store: [a-zA-Z]+Store'` printed the one that is not, which is how the overreach was caught. The count was right and the sentence resting on it was wrong — `CLAUDE.md`'s warning about the parenthesis getting less scrutiny than the figure, met while writing this very entry. Anyone sweeping this property later needs the population to be *upstream calls* — `git grep -nE 'fetch\(' -- server/src/model` → six, at `anthropic.ts:246`, `cerebras.ts:182`, `openai.ts:131`, `openai.ts:233`, `tavily.ts:52`, `vision.ts:251` — and then to read what each one posts. That is `CLAUDE.md`'s "a grep count is evidence about text, never about behaviour" on the exact token this branch adds.
- **What settled the population was a probe, not reading.** A throwaway Node stub on a loopback port, driven by the three real adapter factories, printed each body's key list: `/responses` `["model","input"]`, `/responses` `["model","input","text"]`, `/chat/completions` `["model","messages"]` — three routes, `hasStore: false` on all three. Reading the files would have given the same answer; running them is what makes it a measurement, and it is the same harness shape PR #139 used for SONNY-131. The stub is not committed.
- **The half of the probe that a lane cannot run.** Whether the *upstream* honours the field is not established here and was not establishable: today's route is a proxy (`VISION_BASE_URL` defaults to `https://opencode.ai/zen/go/v1`), OpenCode documents neither `store`, retrieval by id, nor `previous_response_id` — silence, which is not a denial — and settling it needs a real credential, which this session had none of (`env | grep -cE '^(OPENAI|TAVILY|ANTHROPIC|CEREBRAS|VISION|OPENCODE)_API_KEY='` → **0**). That half is a founder-owned manual-test row on this branch rather than an unproven claim in this entry.
- **One observation for whoever does SONNY-110's provider move.** OpenCode's own documentation gives its OpenAI-compatible surface as `https://opencode.ai/zen/v1/responses`, while this gateway is configured with `https://opencode.ai/zen/go/v1` — a `/go` segment their docs do not mention. Not this branch's to change and not a defect anyone has demonstrated; recorded because a provider move will re-derive that base URL and should not assume the configured one is documented.

Known limitations / deferred scope:
- The provider-side half of the probe is owed and is a manual-test row (above), gated on a credential.
- Nothing here touches the provider's abuse-monitoring retention, which is a different window, is SONNY-110's, and is not closed by this branch. Any reading of this entry that says screen captures are now unretained would be wrong: they still reach Sonny's own 30–90 day store by founder decision of 2026-08-16, and whatever the provider keeps for abuse monitoring.

Open questions (required, write "none" if true):
- Should the Chat Completions route send `store: false` too, accepting an unverifiable field against a third-party endpoint to satisfy SONNY-513's criterion literally? Recorded on the ticket; a reviewer's or a founder's call, not taken here.

Next branch: none scheduled by this lane. SONNY-110's provider decision (Azure versus Bedrock) is with the founders, and its verification is recorded on that ticket as of 2026-09-17.
