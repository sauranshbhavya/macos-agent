### Branch: fix/a-wait-is-a-poll-not-a-threshold
Status: complete — SONNY-515 and SONNY-418, two independent test-robustness fixes sharing no file
Date: 2026-09-18
Tickets: **SONNY-515** — a wait on the stub backend is judged by a canary sent through the same pipeline instead of by the clock alone, every stub request's handler runs on a thread of its own, and the sweep's two other undeclared deadline loops wait on `HangBackstop`. **SONNY-418** — the four routine fixtures build their 9am in the machine's own zone, which is the zone the scheduler reads, and one test's "the routine really ran" premise now needs the routine to have run.
Reviewed by: pending — a fresh session per WORKFLOW.md step 7.

Spec sections covered: none. Test infrastructure and fixtures only.

Files changed, 16 at the head that opens the PR (`git diff --name-only 8f3d1d02 HEAD | wc -l`):

- `Tests/MacAgentTestSupport/BackendStubURLProtocol.swift` — each request's handler runs on a `Thread` of its own instead of one shared concurrent `DispatchQueue`; `StubSignal`'s default backstop is `CanaryBackstop.ceiling * 2` instead of 10 s; the doc comment's "a queue of our own cannot" sentence is replaced by the measurement that disproved it, and the method for counting blocking handlers gains the two tokens the sweep found missing.
- `Tests/MacAgentTestSupport/CanaryBackstop.swift` — new. The wait, its blocking form for a stub handler, the handoff a handler leaves its outcome in, the two wordings, and the stub-transport canary.
- `Tests/MacAgentTestSupport/HangBackstop.swift` — gains `waitRecordingAStuckWait`, moved from `SkillsCommandCenterTests` unchanged in behaviour.
- `Tests/MacAgentCoreTests/CanaryBackstopTests.swift` — new, 13 tests. `Tests/MacAgentCoreTests/BackendStubURLProtocolTests.swift` — new, 1 test.
- `Tests/MacAgentCoreTests/SonnyBackendClientTests.swift` — the private `pollUntil` is gone; its four call sites, in the three tests the ticket names, are `CanaryBackstop.waitOrAbandon`, and each held `StubSignal` is released in a `defer`.
- `Tests/MacAgentCoreTests/ClientVersionClientTests.swift` — `twoCallersAtOnceMakeOneRequest`'s handler waits with `CanaryBackstop.block`, and the test body abandons on anything but `.held`.
- `Tests/MacAgentTests/MemoryCommandCenterTests.swift` — the approval wait is `HangBackstop.waitRecordingAStuckWait`; the helper's three callers point at its new home; the 9am fixture is on the machine's zone.
- `Tests/MacAgentTests/TaskDeletionReachesTheServerTests.swift` — `waitForDeliveryPasses` polls with `HangBackstop.waitRecordingAStuckWait` and throws on a give-up.
- `Tests/MacAgentTests/ScheduledRoutineRunTests.swift`, `BackendTaskIdentityTests.swift`, `ResumableTaskRunTests.swift` — the 9am fixture on the machine's zone; in the last, one premise assertion strengthened.
- `scripts/mutate-untrusted-failures` — one record, for `CanaryBackstop`'s give-up wording, `sites 1`.
- `mutation/plans/fix/a-wait-is-a-poll-not-a-threshold.txt`, this entry, and `docs/manual-tests/fix/a-wait-is-a-poll-not-a-threshold.md` (no rows owed, and why).

Nothing under `Sources/`, `Package.swift` or `server/` moves (`git diff --name-only 8f3d1d02 HEAD -- Sources Package.swift server | wc -l` → 0), so no server suite is owed.

Tests (every exit code read with nothing between the command and `$?`; every timing carries the load it was measured under, as the one-minute load average `vm.loadavg` reported, on a ten-core Mac with other lanes running — none of these runs was quiet):
- The full flagged suite, `CLAUDE.md`'s command, at `50364d6d` → exit 0, `Test run with 3489 tests in 253 suites passed after 157.067 seconds with 13 known issues`, and no `failed after` line. Started only when `ps -axww -o command | grep -cE '(^| )/[^ ]*(swift-frontend|swift-driver|swiftpm-testing-helper|xctest)'` answered 0 — after 470 s of waiting on other lanes — with the same pattern's shape shown able to find something first (`(launchd|zsh)` in the same position → 14). One-minute load 82.77 at the start, still falling from those lanes' builds, and 32.65 at the end. It was 3475 in 251 at `8f3d1d02` (the before row below): the 14 are `CanaryBackstopTests`' 13 and `BackendStubURLProtocolTests`' one, the 2 suites theirs.
- `scripts/warnings` → exit 0, `scripts/warnings: 0 warnings.`, its own header reading `measured at : 50364d6d plus 2 uncommitted file(s)` — the two were this entry and the manual-test file, neither compiled — with every file in `Sources/` and `Tests/` compiled, 173 s, started with 0 other Swift processes running, one-minute load 15.23 at the start, peaking at 35.55.
- **Why the `50364d6d` figures hold at the head that merges**: the commit that adds this entry and the manual-test file touches nothing else, and `zsh -c 'a=50364d6d; b=HEAD; for p in Sources Tests Package.swift server docs; do x=$(git rev-parse --verify --quiet "${a}:${p}"); ea=$?; y=$(git rev-parse --verify --quiet "${b}:${p}"); eb=$?; if [[ $ea -ne 0 || $eb -ne 0 ]]; then echo "$p REFUSED"; elif [[ "$x" == "$y" ]]; then echo "$p IDENTICAL"; else echo "$p MOVED"; fi; done'` prints IDENTICAL for `Sources` (`026948d0`), `Tests` (`fce3e3ec`), `Package.swift` (`fbbe36d7`) and `server`, and MOVED for `docs` — that MOVED is the control that the loop can say so. Braced `${a}:${p}`, a loop variable that is not `path`, and exit codes read rather than emptiness, for `CLAUDE.md`'s three reasons; `git rev-parse --verify --quiet "50364d6d:nosuchdir"` exits 1, the REFUSED arm's control.
- **The whole flagged suite under `TZ=Asia/Kolkata`**, SONNY-418's own check, at `50364d6d` → exit 0, `Test run with 3489 tests in 253 suites passed after 99.114 seconds with 13 known issues`, no `failed after` line, load 25.30 → 14.90, started with 0 other Swift processes running. The same count as the run above, so no test in the suite fails outside Eastern — which, as the pitfall *A premise that only names the routine* below shows, is not the same as every test meaning something there.
- The zone suites under three zones other than Eastern, at `9dc73245`, with `--filter 'ScheduledRoutineRunTests|MemoryCommandCenterTests|ResumableTaskRunTests|BackendTaskIdentityTests|RoutineScheduleTests|RoutineSchedulerTests|RoutineStreakTests|WorkspaceTaskBreakdownTests|CalendarAndReminderCapabilityTests'`: `TZ=Asia/Kolkata` → exit 0, `Test run with 382 tests in 12 suites passed after 11.085 seconds`; `TZ=America/Los_Angeles` → exit 0, the same 382 in 12, 9.233 s; `TZ=UTC` → exit 0, the same, 8.310 s.
- **The control those three need, since a `TZ` Foundation ignored would make them mean nothing**: the four fixture files as they stood at `8f3d1d02`, run the same way under `TZ=Asia/Kolkata` at `4eb41852` → exit 1, `Test run with 292 tests in 7 suites failed after 29.603 seconds with 77 issues` — 39 failing tests, 56 issues in `ScheduledRoutineRunTests`, 20 in `MemoryCommandCenterTests`, 1 in `BackendTaskIdentityTests`. `ResumableTaskRunTests` passed in that control, and why is the pitfall *A premise that only names the routine* below.
- The touched suites, `--filter 'CanaryBackstopTests|BackendStubURLProtocolTests|SonnyBackendClientTests|ClientVersionClientTests|UntrustedFailureDeclarationTests|HangBackstopTests|MemoryCommandCenterTests|SkillsCommandCenterTests|TaskDeletionReachesTheServerTests|ScheduledRoutineRunTests|ResumableTaskRunTests|BackendTaskIdentityTests'` on the tree committed as `7e268362`–`4eb41852` → exit 0, `Test run with 423 tests in 15 suites passed after 15.640 seconds with 9 known issues`, load 23.47 → 44.68. The three tests the ticket names passed in 0.048 s, 0.054 s and 0.056 s.
- `scripts/mutate mutation/plans/fix/a-wait-is-a-poll-not-a-threshold.txt --check` at `9dc73245` → exit 0, seven mutants, one match each.
- `scripts/mutate selftest` (owed because the declaration file changed) → exit 0, `selftest: all checks passed.`, 344 `PASS` lines and no `FAIL` line, run at `50364d6d`.
- `server/scripts/check-secrets.sh` → exit 0, `check-secrets: clean (1310 tracked files scanned, 12 patterns, 8 baselined fixtures)`, over this branch's final tree with this entry and the manual-test file staged — 1310 against 1308 before they were, which is what says the scan read them.
- `scripts/no-attribution tree` → exit 0, `0 of 1306 tracked file(s) carry an attribution (4 excluded by path)`, over the same staged tree.
- `scripts/changelog-order` → exit 0 over the same staged tree: `this checkout contains all 18 of the branches merged in the one-file-per-branch era` and `19 entry file(s) under docs/changelog and 19 under docs/manual-tests resolve`. All three are re-run at the head that opens the PR, and those results are on the PR and both tickets.

Mutation plan: `mutation/plans/fix/a-wait-is-a-poll-not-a-threshold.txt` (founder-triggered, not run on this branch). Swift half, seven mutants; its header names each one's intended killers and says why the timezone mutant pins a zone half a day from the machine's rather than Eastern.

**What the stub transport did under load, measured by an uncommitted probe** — a `SONNY_STUB_PROBE`-gated static on `BackendStubURLProtocol` appending timestamped lines for each `startLoading`, each handler start with how long it had queued, each `StubSignal` and `StubBarrier` wait, `pollUntil`'s outcome, `ClientVersionClientTests`' window and `waitForDeliveryPasses`' iterations. Reverted before the first commit: `git diff 8f3d1d02 50364d6d -- Tests | grep -ci 'SONNY_STUB_PROBE'` → 0, scoped to `Tests/` because this entry names the variable itself, and `git diff 8f3d1d02 50364d6d -- Tests | grep -ci 'CanaryBackstop'` → 54 is the control that the same diff is being read. Three full flagged-suite runs, all on `8f3d1d02`:

| run | tree | load (1 min) | suite | handler starts | queued ≥ 1 s | queued ≥ 0.1 s | longest queue wait |
|---|---|---|---|---|---|---|---|
| before | probe only | 12.66 → 40.40 | exit 0, 3475 tests in 251 suites, 159.324 s | 553 | 72 | 118 | 7.352 s |
| after | probe + the stub thread (and the backstop file, compiled but not yet used) | 34.39 → 54.79 (peak) | exit 0, 3475 in 251, 120.312 s | 550 | 0 | 0 | 0.018 s |
| after, ten CPU burners (`yes > /dev/null`, one per core, for exactly the run) | probe + everything in `7e268362`–`4eb41852` | 62.27 → 94.72 (peak) | exit 0, 3489 in 253, 173.357 s | 636 | 0 | 0 | 0.026 s |

In the before run all 72 were released within 0.105 s of each other — one grant from GCD's pool, the shape SONNY-515's own comments recorded at 48 per run. `waitForDeliveryPasses` was called 16 times in every run and its poll never iterated.

Behavior added:
- `CanaryBackstop.waitOrAbandon(for:…)`: polls until a condition holds; decides nothing before `deadline` (10 s); from the deadline on runs a canary — by default a request from a `SonnyBackendClient` of its own to a stub host of its own — and counts only trips begun after the deadline; gives up **stuck** once `canaryFloor` (20) trips completed with the condition still false, recording the declared give-up wording and a second sentence nothing declares, or **starved** at `ceiling` (60 s), recording only declared wordings; both throw `HangBackstop.Abandoned`. The verdict itself is `HangBackstop.verdict`, fed the canary's count in place of looks.
- `CanaryBackstop.block` and `CanaryBackstop.Handoff`: the same wait from a stub handler's own thread, leaving its outcome for the test body, which is the only place an issue is recorded against the right test.
- `BackendStubURLProtocol` runs every handler on a `Thread` of its own.
- `HangBackstop.waitRecordingAStuckWait`, shared by three suites.

Behavior preserved (required, no blanket claims):
- The three `SonnyBackendClientTests` tests still enforce their orderings — the straggler's 401 is still held until the first refresh has completed, the refresh is still held across sign-out, and the old session's refresh is still held for the whole of the new-session test; only the waits for "it reached the stub" and their give-up changed.
- `twoCallersAtOnceMakeOneRequest` still holds the first request open from the handler and launches the second caller inside that hold; its budget is still `SonnyBackendTimeouts.auth / 2`, and it still asserts the first document and the request count.
- `tenConcurrent401sCauseExactlyOneRefreshAndEveryRequestThenSucceeds` is untouched; `StubBarrier` is unchanged.
- `waitForDeliveryPasses` still awaits the last delivery handle first; all 16 callers are unchanged (none passed the removed `timeout:`).
- `aRunPausedAtItsApprovalDoesNotReloadTheMemoryRows` still asserts at the pause and still runs its control after cancelling.
- The three `SkillsCommandCenterTests` callers of `waitRecordingAStuckWait` get the same function, moved.
- Every scheduled-run test in the four fixture suites still runs its routine at `tenAM` against a 9am daily schedule enabled a day earlier; the fixture date, 2026-07-15, is unchanged.
- The other zone-pinning files the board triage listed — `RoutineScheduleTests`, `RoutineSchedulerTests`, `RoutineStreakTests`, `WorkspaceTaskBreakdownTests` — and `CalendarAndReminderCapabilityTests`, which the triage did not list, are untouched and pass under all three zones above. Read rather than inherited from the triage, as it asked: each hands its pinned calendar to the code it tests — `calendar:` at every `RoutineScheduler`, `RoutineStreak` and `WorkspaceTaskBreakdown` call, and `Self.newYork` into the runner in `CalendarAndReminderCapabilityTests` — which is the shape the ticket calls correct.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **The stall was never the suite's own blocking handlers.** SONNY-515's earlier comments counted them as the likely cause. In the before run the longest `StubSignal` wait was 0.039 s and the longest `StubBarrier` wait 0.016 s, while 72 handlers queued for up to 7.352 s. Something else in the process held GCD's constrained pool — which is process-wide and capped at 64 threads (`sysctl kern.wq_max_constrained_threads` → 64 here). A thread per request takes the stub out of that pool whatever fills it.
- **The thread change alone does not close the manufactured-kill door, and that was measured rather than assumed.** In the second run no handler queued even 0.1 s, and the probe still found a window of 5.231 s in which it logged nothing at all — no request reached the stub, no handler started, no wait moved — and three `pollUntil` waits taking 7.21 s to 7.25 s to make two or three looks, all ending within 0.05 s of each other. In the burner run three real waits ran past their 10 s deadline — 12.67 s, 14.04 s and 14.04 s, two or three looks each — and ended held. The process as a whole stalls under load; only a witness that runs through the same pipeline can tell that from a defect.
- **What this entry does not claim about those three late waits**: that the old `pollUntil` would have failed them. It took one last look after its deadline, and whether that look came before or after the request landed is exactly what the probe did not record.
- **Why the canary counts only trips begun after the deadline.** A canary running from the start banks trips completed before a stall began, and would then vouch for a pipeline that was frozen when the verdict was taken. `everyCanaryTripBeginsAfterTheDeadline` and `noCanaryIsSentBeforeTheDeadline` hold it, and the plan's M4 puts it back.
- **Why `BackendStubURLProtocolTests` uses `HangBackstop` and not `CanaryBackstop`.** The defect it pins — every handler waiting for a pool thread — exhausts the very pipeline a stub-transport canary travels, so that canary would call its failure starved and the mutant would never count. `HangBackstop` counts looks on the main actor, which the defect does not touch.
- **`ClientVersionClientTests`' window has a ceiling of `SonnyBackendTimeouts.auth * 0.9`**, not `CanaryBackstop.ceiling`, because the handler must answer before the client's own twenty seconds end the fetch; past that the second caller would send its own request and the count would fail in words no declaration covers.
- **SONNY-418: `Calendar(identifier: .gregorian)` with `timeZone = .current`, not `Calendar.current`.** The scheduler reads `Calendar.current`; the fixture needs only the same zone, and PR #198's review measured that explicit-Gregorian-at-the-machine's-zone is the property that matters for SONNY-403's fixture. Keeping the Gregorian calendar keeps "2026-07-15" meaning that date on a Mac set to another calendar system. Nothing under `Sources/` changes; passing the fixture's calendar to the scheduler at the call site, the ticket's other option, needs its own decision and was not taken.
- **A premise that only names the routine is not a premise that it ran** (found by the control run). `ResumableTaskRunTests`' `aScheduledRoutineRunLeavesNoRecordWhileAForegroundRunInTheSameFixtureDoes` passed under Kolkata with the Eastern pin still in place, because it checked the routine ran with `scheduledRunNotice?.contains("Morning")` and the notice for an occurrence missed past the catch-up window — `Sources/MacAgent/AgentViewModel.swift:8387` at `8f3d1d02` — names the routine too. It now asserts `"ran on schedule"`; with the old pin under Kolkata that assertion fails (exit 1, `1 test in 1 suite failed … with 1 issue`, the zone line flipped by hand and back, never by `git checkout`), and with the fixture on the machine's zone it passes.
- **A test whose failure depends on the host is invisible on the host.** The plan's M7 does not restore `America/New_York`: on a Mac in Eastern that mutant is SONNY-418's defect exactly and would come back SURVIVED on the machines the founders run batteries on. It pins twelve hours from the machine's own offset instead, which misses the catch-up window everywhere.

Known limitations / deferred scope:
- **A held stub request is still inside the client's own transport timeout** — `SonnyBackendTimeouts.auth`, 20 s — which is the product's clock, not the harness's. The three `SonnyBackendClientTests` tests and `twoCallersAtOnceMakeOneRequest` each hold a request while the test takes further steps, and a process stall past twenty seconds between those steps fails in the client's words, which no declaration covers. The burner run's stalls reached 14 s. Filed as **SONNY-530** (Backlog, untriaged) with the sites, the measurement and four options, since every fix needs a seam under `Sources/` or a decision about what a battery should count.
- The canary's floor of 20 and the 60 s ceiling are chosen, not measured against a population the way `HangBackstop`'s were; the reasoning is on the constants.
- **`BackendStubURLProtocolTests`' one test is slow in a full run, and that is the price of its witness.** It passed in 4.782 s in the touched-suites run above, in 37.963 s beside ten burners, in 41.784 s in the full run under Kolkata and in 64.416 s in the full run — all of it spent waiting for turns on the main actor, which is the one place its wait can look without the defect it pins also starving the look. It holds eighty parked threads for that long and is still far inside `HangBackstop`'s 180 s ceiling.

Open questions (required, write "none" if true): none.

Next branch: none named here — whatever the coordinator routes next.
