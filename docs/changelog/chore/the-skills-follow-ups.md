### Branch: chore/the-skills-follow-ups
Status: complete — pushed and holding for review; nothing merged
Date: 2026-09-18
Tickets: SONNY-529, SONNY-526, SONNY-524, the three follow-ups from wave 12's start-page work (PR #272).
- **SONNY-526:** the 69 catalogue rows this lane owned whose sign-in address was `https://<domain>/login` or `/login/` were each opened signed out. Three were wrong and are corrected in the pack and the row together: Bigin, Instagram and Midjourney.
- **SONNY-524:** settled. A pack's `signInURL` is the authoritative value and its catalogue row's `sign_in_url` mirrors it, a test now holds the two equal, and the 15 differences are reconciled after each value was opened.
- **SONNY-529:** the per-flow check from SONNY-510's round five runs in the suite with its guard. Microsoft 365 and Zoho Desk are deep again, each with one new identity-host pairing. Midjourney stays shallow on an explicit judgement. The read date is checked as a date. The account-creation words match inside a slug and as a host's first label, measured first. The refusal is renamed. The population test's site rule handles a `.co.uk` domain. The eBay-and-Etsy difference is explained where the word is defined. Pylon's evidence is corrected below.
Reviewed by: not yet reviewed.

Spec sections covered: none. This is skill-pack data, the loader's start-page rule and its tests (SONNY-452's line of work).
Files changed:
- `Sources/MacAgentCore/SkillPackStartPages.swift`: the read-date check, the widened account-creation match, two identity-host pairings, and doc comments.
- `Sources/MacAgentCore/SkillPack.swift`: the read-date guard, the renamed refusal, and the `signInURL` doc comment with the catalogue paragraph that points at it.
- `Tests/MacAgentCoreTests/SkillPackTests.swift`: the sign-in equality line, four new tests, the date and slug cases, and the site rule.
- Seven pack files: `bigin`, `instagram` and `midjourney` (SONNY-526); `outlook` and `outlook_calendar` (SONNY-524); `microsoft` and `zoho_desk` (SONNY-529).
- `docs/sonny-skill-sites.tsv`: the `sign_in_url` cell of 16 rows, and nothing else.
- `mutation/plans/chore/the-skills-follow-ups.txt`, and this entry and its manual-test file.
Tests: the flagged `swift test` command from `CLAUDE.md` → exit 0, `Test run with 3479 tests in 251 suites passed after 99.659 seconds with 8 known issues` at `ba390bb8`, clean tree. It started at 03:37:00Z on 2026-09-19 with no Swift test process running and a one-minute load of 8.89. Each of the four new tests prints one `started` and one `passed` line, and its 7 skip lines are environment gates, none Skill-related. Against `068c23a5`'s 3475, that is the four new tests. `scripts/warnings` → exit 0, `measured at : ba390bb8 (clean)`, `0 warnings`. The Skills suites under `--filter 'SkillPack|Skill'` → exit 0, `Test run with 78 tests in 5 suites passed after 8.921 seconds` at `ba390bb8`, clean tree. No file under `server/` changed (`git diff --quiet 8f3d1d02 ba390bb8 -- server` exits 0), so the server half owes nothing, and no Postgres was started. `server/scripts/check-secrets.sh`, `scripts/no-attribution tree` and `scripts/changelog-order` were run over the tree that carries this entry, and their exits are in the closing comments.
Mutation plan: mutation/plans/chore/the-skills-follow-ups.txt (founder-triggered, not run on this branch). It has 17 mutants (`grep -c '^>>> mutant ' mutation/plans/chore/the-skills-follow-ups.txt` → 17 at `ba390bb8`).
- A1 to A3 hold the read-date check and each of its two halves.
- W1 to W3 undo the widened match's three parts: host labels, runs inside a slug, and the case cut. W4 and W5 widen it past the two near misses the tests hold on purpose, `/registered-users` and a site whose own name is one of the words.
- P1 and P2 drop each new pairing, and P3 adds a pairing nobody read.
- D2 is a data mutant: Notion's pack names a sign-in page its row does not. D3 puts eBay's Watchlist flow back under its one `product` record.
- F1 to F3 remove each of the first-step check's three questions. The check lives in the test file, so its held samples are what must kill them.
- S1 returns the site rule to the last two labels.
- Every `from` block in this plan, in SONNY-510's plan and in the three other plans anchored on files this branch changed matches exactly once at `ba390bb8`: 86 mutants across 5 plans, with 2 mismatched, and both of those were already mismatched at `8f3d1d02` (below, under pitfalls).

Behavior added:
- **The first-step check runs in the suite** (SONNY-529). `everyFlowIsJudgedAtItsOwnFirstStep` asks three things of every shipped flow, with round five's own two patterns word for word:
  - a `product` record that more than one flow starts at;
  - a flow under a `product` record whose first step names a place a signed-out visitor may not reach;
  - a flow of any kind whose first step names account creation.
  A finding a person has read and judged is written into `judgedFirstStepFindings` with its reason, and the two sets must be equal both ways. Today that is Grok's question flow, for the reason round five gave. A new finding fails until someone reads that flow, and a judgement whose flow changes fails too.
  - Its guard travelled with it: a `#require` that the walk loaded more than 100 packs, then counts showing the population it judges is not empty.
  - `theFirstStepCheckFindsEachShapeItExistsFor` holds the check on eBay's old shape (both of the first two questions), a flow that starts at "Sign up" (the third), and two controls that raise nothing. Every sample goes through `SkillPackDecoder` and then the same function the shipped packs meet.
  - Cross-checked against the command it replaces: at `8f3d1d02` round five's command prints `399 6 []` and then only Grok's line, which is exactly the judged set.
- **Microsoft 365 and Zoho Desk are deep again** (SONNY-529), with the flows they had at `0d645464` and a new start URL each. Both were read signed out on 2026-09-18, twice.
  - `https://www.microsoft365.com/login` lands on `login.microsoftonline.com/common/oauth2/v2.0/authorize`, "Sign in to your account", with "No account? Create one!" as a separate route. It returns to `www.microsoft365.com`. New pairing: `login.microsoftonline.com` → `microsoft365.com`.
  - `https://desk.zoho.com/agent` lands on `accounts.zoho.com/signin`, "Zoho Accounts". It is headed "Sign in to access Desk" in text that is no `h1` or `h2`, so the record's heading is empty, and "Don't have a Zoho account? Sign up now" is a separate route. It returns to `desk.zoho.com/agent`. New pairing: `accounts.zoho.com` → `zoho.com`.
  - The population test derives both pairings from the shipped files and still equals the list.
- **Midjourney stays shallow, on the rule applied explicitly** (SONNY-529). `www.midjourney.com` lands on `/home`, an explore page. Its "Log In" button opens a dialog headed "Log in" with Continue with Google and Continue with Discord (03:22:19Z on 2026-09-19, which is 2026-09-18 in the founders' zone). Its "Sign Up" button opens one headed "Sign up" with the same two buttons (03:23:07Z). So the form that signs in is the form that creates an account, which is Fireflies' and Framer's shape, and the page is held. Making images needs an account, so `product` does not fit either.
- **A start page's `read` must be a real date, no earlier than 2026-09-17** (SONNY-529). `2026-13-45`, `2026-02-30`, `2027-02-29` and `1970-01-01` no longer load. The floor is the day SONNY-510 was filed and its first readings are dated, so an earlier date is a reading that could not have happened. There is no upper bound, because the loader runs at every launch and a Mac with a wrong clock would refuse every pack read after its today.
- **The account-creation words match inside a slug and as a host's first labels** (SONNY-529). A URL part is cut at `-`, `_` and a lowercase-to-uppercase boundary, and every run of its words is looked up. So `/signup-free`, `/register-now`, `/sign-up-free`, `/SignUpNow`, `/create-account-now`, `#/free_signup`, `?intent=signup_free` and `signup.<site>` are refused. The four words themselves did not change.
  - **Measured first, as the ticket asked.** Over every declared and landed URL the shipped start pages record, the old match refuses none and the new one newly refuses none: `342 281 0 0` at `8f3d1d02` and `346 284 0 0` at `ba390bb8`. Those are the URL count, the distinct count, the old refusals and the new ones. The same command finds `/signup-free`, `signup.x.com` and `/SignUpNow` and not `/registered-users`, which shows it can come back non-empty. Pointed at a folder with no packs, it exits 1. The command is under pitfalls.
  - **Rejected, and why.** A match on a word's prefix catches `/SignUpFree` with no cut in it, but it refuses `/registered-users`, which is not account creation and which the tests already held as a near miss. `join` stays out as a host label, because Zoom's `join.zoom.us` is a real start URL and joins a meeting. Of the widenings measured, only that one would have refused a shipped URL.
- **The refusal names the missing pairing, and its name says so** (SONNY-529). `landedOnUnpairedHost(url:host:site:)` is now `landingHostNotPairedWithSite(url:host:site:)`, because the host is often listed and only its pairing with this site is missing. Microsoft 365's landing on `login.microsoftonline.com` was exactly that case, refused while that host was listed for Outlook and Teams.
- **The population test's site rule handles a country's second level** (SONNY-529, from review-272b's delta pass). `registrableSite(of:)` takes the last three labels when the two before the country code are a second level names are sold under, such as `co.uk`, `com.au` or `co.jp`, and the last two otherwise. `aPacksSiteIsItsRegistrableDomainEvenUnderACountrysSecondLevel` holds ten samples, because no shipped pack sits on such a domain: 0 of 473 at `ba390bb8`, by the command under pitfalls.
- **`signInURL` is authoritative and the row mirrors it** (SONNY-524). This is the settled reading, and the reasons are below. `everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue` now asserts `sign_in_url` equals the pack, beside `domain` and `category`. An empty cell is a pack with no sign-in page.
- **Three sign-in addresses corrected** (SONNY-526), in the pack and the row together:
  - **Bigin:** `https://bigin.com/login` landed on `www.bigin.com/login/`, "Bigin - Page Not Found". Bigin's own "Sign In" links open Zoho's sign-in. It is now `https://accounts.zoho.com/signin?servicename=ZohoBigin`, which lands on `accounts.zoho.com/signin`, "Zoho Accounts", with "Create account" separate.
  - **Instagram:** `https://instagram.com/login` landed on "Profile isn't available • Instagram". A made-up path, `instagram.com/zzqnotauser529x/`, returns the same title, so `/login` is read as a user name. That is Chatwork's shape. It is now `https://www.instagram.com/accounts/login/`, titled "Instagram", with a password field and "Create new account" separate.
  - **Midjourney:** `https://midjourney.com/login` is a 404, as SONNY-510 found. The site has no sign-in address, only the "Log In" control above, so it is now `https://www.midjourney.com/`, the page that control is on.

Behavior preserved (required, no blanket claims):
- **The start-page rule's other checks are byte-for-byte the same.** These are the host check, the identity-host sign-in-only check, the query checks and the record-per-URL checks. Every `from` block in SONNY-510's plan still matches once.
- **Every URL that loaded before still loads.** The old match was a whole part folded, and the new one reads every run of words, which includes the whole part, so it is a superset. `aStartPageWhosePathNamesAccountCreationDoesNotLoad` is unchanged and passes, including `/registered-users` and `/join` as near misses.
- **Dates that loaded still load.** Every shipped record says `2026-09-17` or `2026-09-18`, and the fixtures say `2026-09-18`.
- **The identity-host list keeps its nine hosts' existing pairings** and adds one site to one host and one new host. `theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake` still reads its expected answer from the raw files and never from the list.
- **Guidance changes only where a sign-in address or a flow changed:** 7 packs' `signInURL`, and Microsoft 365's and Zoho Desk's flows. The Skills page shows no sign-in address, depth or flows, so it does not change.
- **The catalogue changed in one column only.** 16 rows' `sign_in_url` cells, checked by a script comparing every other cell of every row to `8f3d1d02` (under pitfalls). The four rows the pack lanes hold (contactout, wise, expo, railway) and the 60 rows of groups 9, 10 and 11 are untouched.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **SONNY-524: the pack decides, the row mirrors it, and a test holds them equal.** The ticket offered two readings, and the evidence decided between them.
  - *They are one fact.* Both say which page Sonny should sign in on. The catalogue is not a frozen research record either: `domain` and `category` were already held equal to the pack, and the `doc_url` cells change whenever a lane reads a page. So a `sign_in_url` that disagrees is stale, not history.
  - *The pack is the one that decides*, because it is what reaches the planner ("Sign-in page: …"), and it is where a person puts a correction after opening the page. The row was the seed, and SONNY-526 is the measurement of what a seed can be worth: 3 of the 69 were wrong, and one of them, Instagram's, is a user-name page.
  - *So the answer was not "make them match" by copying one over the other.* The 13 pack values were each opened before the rows took them. Outlook and Outlook Calendar went the other way, and it was a pack correction on its own reading: `https://outlook.office365.com` lands on the same Microsoft sign-in page and returns to Outlook. The bare `login.microsoftonline.com/` the sweep had written returns to office.com, and it was chosen for a host rule that no longer reads it. Teams keeps `login.microsoftonline.com/`, because `teams.microsoft.com` sat on "Microsoft Teams" with an empty page for 18 seconds and never reached a sign-in page.
  - *Where a reader meets it:* `SkillPack.signInURL`'s doc comment, the type's paragraph on the catalogue, and the equality assertion's comment. The TSV itself can carry no note. Its first line is its header, the test asserts it exactly, and every reader of the file takes any other line for a row.
  - *What it costs the pack lanes in flight:* a lane that changes a pack's `signInURL` must change its row in the same branch, or the suite fails. That fails closed, where someone sees it. SONNY-526's four rows sit in the three pack lanes, whose tickets tell them to correct a wrong address, so this is the one place the rule will meet them first.
- **A made-up path is the control for a sign-in address.** Instagram's `/login` looked like a page, "Profile isn't available", until a nonsense path returned the same title. Pylon's evidence had the same hole, the other way round (next item).
- **A correction to the record: Pylon's `/signup` evidence proves nothing.** SONNY-510's entry says `app.usepylon.com/signup` "goes back to the sign-in page". So does a made-up path: `app.usepylon.com/zzq-no-such-page-529` lands on `app.usepylon.com/signin`, "Sign In – Pylon" (03:31:08Z), as `/signup` does (03:30:59Z). So the bounce is what the app does with any path, not evidence about sign-up. Pylon's pass under decision A rests on what holds: its homepage offers "Book a demo" and "Talk to Us", and `/pricing` redirects to `/schedule-demo`, "Book a Demo | Pylon", re-read here. That entry is left verbatim, as dated records are, and this is the correction.
- **eBay says `product` and Etsy says `sign-in`, and both are right.** The word is judged at each flow's own first step, not at the page's shape. eBay's one flow begins in the search box, which works signed out. Etsy's two begin in Your account and Shop Manager. `SkillPackStartPage`'s doc comment now says so where the word is defined, so a reader comparing the two records is told why before concluding one is wrong.
- **A mutation plan's anchor goes stale when another branch moves the line, and nothing says so until the battery runs.** Checking this branch's plans found two mutants in `mutation/plans/feature/skills.txt` that already match nothing at `8f3d1d02`: U3 on `SkillPackContentRules.swift` and U6 on `SkillPack.swift`, whose own-domain check SONNY-510 moved into `isOnSite(host:domain:)`. The weekly battery aborts a plan with a zero-match mutant, so `scripts/mutate-all` would stop on that plan. It is outside all three tickets and is filed as SONNY-532.
- **Browser-reading pitfalls met here:**
  - A window not on screen reports `document.visibilityState` as `hidden`. Heavy pages then never render (Linear, Podio, SonarCloud) or freeze the renderer for 45 seconds (Prezi), and screenshots time out. The founder brought the window on screen at about 03:07Z, and the pages that had failed were read again. Discord's form still reached no script, so it was read from a screenshot.
  - The Chrome extension holds a claude.ai session of its own. `claude.ai/login` showed "Sign in - Claude" for about three seconds, then moved to `/new`. So Claude's address could not be read signed out, and it is left as it is.
  - Pinterest's `/login/` rendered an empty document on both reads, as SONNY-510 found on every read, so it is left as it is.
  - **A slip, recorded rather than left out.** On Midjourney, a click meant for the page's "Sign Up" button landed on "Continue with Discord" in the "Log in" dialog, which had not closed. It opened Discord's own sign-in page, signed out, in a new tab. Nothing was typed, and the tab was closed at once. The "Sign Up" reading was then taken by pressing that button through a script, which touches nothing else.
- **The commands behind this entry's figures**, each run from the repository root over a `git archive` of the SHA named:
  - The sign-in address population: `python3 -c "import csv;R=list(csv.DictReader(open('docs/sonny-skill-sites.tsv'),delimiter='\t'));print(sum(r['sign_in_url'] in ('https://'+r['domain']+'/login','https://'+r['domain']+'/login/') for r in R))"`. It prints 74 at `0d645464`, 73 at `8f3d1d02` (Chatwork's row had been corrected in between) and 68 at `ba390bb8`. With the `/login/` form dropped it prints 70 at `0d645464`, so the reviewer's 70 and the coordinator's 74 were both right, about two different shapes: with and without the trailing slash.
  - The pack-and-row mismatch, SONNY-510's own command: `15` at `8f3d1d02` and `0` at `ba390bb8`.
  - The widening measurement: `python3 -c 'import json,glob,re,sys;from urllib.parse import urlsplit as u;W={"signup","register","registration","createaccount"};P=[json.load(open(p)) for p in glob.glob(sys.argv[1]+"/"+"*.skillpack.json")];assert len(P)>100;ws=lambda p:[w.lower() for w in re.split(r"[-_]|(?<=[a-z])(?=[A-Z])",p) if w];hit=lambda p:any("".join(ws(p)[i:j]) in W for i in range(len(ws(p))) for j in range(i+1,len(ws(p))+1));old=lambda x:any(p.lower().replace("-","").replace("_","") in W for p in re.split(r"[/#?&=.]","/".join([u(x).path,u(x).fragment,u(x).query])) if p);new=lambda x:any(hit(p) for p in re.split(r"[/#?&=.]","/".join([u(x).path,u(x).fragment,u(x).query]))+(u(x).hostname or "").split(".")[:-2] if p);U=[s[k] for d in P for s in d.get("startPages",[]) for k in ("url","landedURL")];C=["https://x.com/signup-free","https://signup.x.com/","https://x.com/SignUpNow","https://x.com/registered-users"];print(len(U),len(set(U)),sum(old(x) for x in U),sum(new(x) and not old(x) for x in U),[new(c) for c in C])'`, with the packs folder as its one argument.
  - The deep population, SONNY-510's first command: `187 77 403 162` at `ba390bb8`, against `187 77 399 160` at `068c23a5`. That is 2 packs and 4 flows back, none of them from a bare origin.
  - Multi-label domains: `python3 -c "import json,glob;P=[json.load(open(p)) for p in glob.glob('Sources/MacAgent/Resources/SkillPacks/'+'*.skillpack.json')];assert len(P)>100;L=[d['domain'] for d in P if len(d['domain'].split('.'))>=3 and len(d['domain'].split('.')[-1])==2 and d['domain'].split('.')[-2] in {'co','com','net','org','ac','gov','edu','ne','or','go','ltd','plc'}];print(len(P),len(L))"` prints `473 0` at `ba390bb8`.
  - The catalogue compared cell by cell: `python3 -c 'import csv,subprocess,sys,io;r=lambda s:list(csv.DictReader(io.StringIO(subprocess.check_output(["git","show",s+":docs/sonny-skill-sites.tsv"]).decode()),delimiter="	"));A,B=r(sys.argv[1]),r(sys.argv[2]);assert len(A)==len(B)>100 and [a["id"] for a in A]==[b["id"] for b in B];D=[(a["id"],k) for a,b in zip(A,B) for k in a if a[k]!=b[k]];print(len(A),len(D),sorted({k for _,k in D}),sorted({i for i,_ in D}))' 8f3d1d02 ba390bb8` prints `473 16 ['sign_in_url'] [...]`, the 16 ids being the 13 SONNY-524 rows and the 3 SONNY-526 rows. Its control, `0d645464` against `8f3d1d02`, prints 70 differences over five columns, so it can see a column other than this one.

Known limitations / deferred scope:
- **Two of the 69 could not be read:** Claude (the extension's session) and Pinterest (an empty page). Both are left as they were, and both are named on SONNY-526.
- **The 150 other rows ending in `/login` were not opened.** The ticket said to widen only if the first set turned up more than a couple of bad values. 3 of 69 did, one of them a user-name page, so that is at the line. The figure is on SONNY-526 for the coordinator to decide.
- **The first-step check reads words.** A first step that names a signed-in place in other words passes it. That is why the judged set carries its reason, and why a person still reads each `product` flow.
- **The site rule is a rule, not the public-suffix list.** A country whose second level is not in its set gets two labels, and the population test fails, naming the host and the site it derived.
- **The account-creation match still misses a joined-up slug with no case cut**, such as `/signupfree`. That is deliberate: catching it takes a prefix match, and a prefix match refuses `/registered-users`.
- **Microsoft 365's and Zoho Desk's flow steps were not re-read.** They are as they stood at `0d645464`, restored the way SONNY-510 restored OneDrive's, with only the start URL new.

Open questions (required, write "none" if true):
- **Should SONNY-526 widen to the other 150 `/login` rows?** The ticket's own threshold is "more than a couple of bad values", and 3 of 69 sits at it.
- **SONNY-524 was settled by this lane.** The ticket asked the implementing session to settle the reading, and the reasons are above. The founders can reverse it by deleting one assertion.

Next branch: none named.
