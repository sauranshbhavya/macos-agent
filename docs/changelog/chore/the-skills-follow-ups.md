### Branch: chore/the-skills-follow-ups
Status: complete — review-282's fix round and the scoped round after its re-check applied and pushed; holding for the scoped verification, nothing merged
Date: 2026-09-18, fix round and scoped round 2026-09-19
Tickets: SONNY-529, SONNY-526, SONNY-524, the three follow-ups from wave 12's start-page work (PR #272).
- **SONNY-526:** the 69 catalogue rows this lane owned whose sign-in address was `https://<domain>/login` or `/login/` were each opened signed out. Three were wrong and are corrected in the pack and the row together: Bigin, Instagram and Midjourney. Pinterest, which the first round recorded as unreadable, is a sign-in page and its address is right (review-282's frame-counted reading, below).
- **SONNY-524:** settled. A pack's `signInURL` is the authoritative value and its catalogue row's `sign_in_url` mirrors it, and a test now holds the two equal. The 15 differences are reconciled on readings. Two of the first round's were wrong, Teams and Microsoft Lists, and the fix round corrected them (review-282's findings 1 and 2).
- **SONNY-529:** the per-flow check from SONNY-510's round five runs in the suite with its guard, and each finding carries its step's text. Microsoft 365 and Zoho Desk are deep again, each with one new identity-host pairing, and Microsoft 365's sign-in page is its `/login`. Midjourney stays shallow on an explicit judgement. The read date is checked as a date. The account-creation words match inside a slug, in a query value and as a host's first label; a query key is read only whole; the known refusals are held. The refusal is renamed. The population test's site rule handles a `.co.uk` domain. The eBay-and-Etsy difference is explained where the word is defined. Pylon's evidence is corrected below.
Reviewed by: review-282, a fresh session's full pass at `88aa3453` and a delta pass at `ef36f2fd`, both posted in full on PR #282.
- **Three blocking findings in the full pass, with three different causes.** All three are fixed in the pack and the row, below.
  - F1: Teams kept a sign-in address that returns to office.com. The cause was a reading taken in a window Chrome was not drawing.
  - F2: Microsoft Lists kept one with no return. The cause was comparing only the two values in front of the lane, when the pack's own start URL was a third and better one.
  - F3: Microsoft 365, made deep here, still named a marketing page as its sign-in page. The cause was a correct reading of `/login` that was never applied to the pack's `signInURL`.
- **F4, the founders' ruling on PR #282.** A query key is read in whole pieces, not by runs, and the known refusals are recorded. The measurement is redone over every address the packs carry.
- **F5.** A finding line carries its step.
- **F6 to F8.** Microsoft 365's heading, the page its sign-in returns to, and the `.co.uk` comment.
- **F9, not routed.** An observation about `microsoft_365_admin`, recorded under known limitations.
- **What held up in the full pass:** the reasoning on SONNY-524, Outlook, Bigin, Instagram, Midjourney, Zoho Desk, both pairings, the first-step check, the site rule, the date, the rename, the catalogue diff, the plan's anchors and the ancestry.
- **The delta pass at `ef36f2fd` found one blocker inside F4's own fix.** Reading a key as one word also stopped reading a key that is itself a path, so `index.php?/register`, `?/auth/signup`, `?/signup` and `?user.register=1`, refused on `main` and at `88aa3453`, all loaded. The scoped round cuts each key at `/` and `.` and reads each piece whole (below).
- **It also frame-counted every reading the full pass had relied on**, 250 to 252 animation frames in 2.5 s, from 17:06:32Z to 17:11:23Z. Its own first pass had not been frame-counted, and one of its readings was wrong: Pinterest, which it had confirmed empty. Drawn, Pinterest is a sign-in page. The delta pass also settled F7, below.
- **Three record corrections came from it.** The line above no longer blames all three blockers on the window. The known-refusals test's comment now sets Trello's shipped address apart. And Teams' start-page record now names its heading.

Spec sections covered: none. This is skill-pack data, the loader's start-page rule and its tests (SONNY-452's line of work).
Files changed:
- `Sources/MacAgentCore/SkillPackStartPages.swift`: the read-date check, the widened account-creation match with a query read as keys and values, two identity-host pairings, and doc comments.
- `Sources/MacAgentCore/SkillPack.swift`: the read-date guard, the renamed refusal, and the `signInURL` doc comment with the catalogue paragraph that points at it.
- `Tests/MacAgentCoreTests/SkillPackTests.swift`: the sign-in equality line, five new tests, the date, slug and query-key cases, and the site rule.
- Nine pack files:
  - `bigin`, `instagram` and `midjourney` (SONNY-526);
  - `outlook`, `outlook_calendar`, `teams` and `microsoft_lists` (SONNY-524). Teams' start-page record also now says its heading is "Sign in", a visible `role="heading"`, where it said none. That is review-282's frame-counted reading at 17:07:11Z of `https://teams.microsoft.com`, whose landing is the page the record names, so the record reads `2026-09-19`. Its start URL with the trailing slash was not separately re-read;
  - `microsoft` and `zoho_desk` (SONNY-529).
- `docs/sonny-skill-sites.tsv`: the `sign_in_url` cell of 16 rows, and nothing else.
- `mutation/plans/chore/the-skills-follow-ups.txt`, and SONNY-510's `mutation/plans/fix/a-flow-starts-where-the-work-starts.txt`, where M6 and M14 are re-anchored because this branch's change removed the line both anchored on.
- This entry and its manual-test file.
Tests: the flagged `swift test` command from `CLAUDE.md` → exit 0, `Test run with 3480 tests in 251 suites passed after 99.112 seconds with 8 known issues` at `41b10f98`, clean tree. It started at 17:40:15Z on 2026-09-19 with no Swift test process running and a one-minute load of 5.23. Its 7 skip lines are environment gates, none Skill-related. The count is the fix round's 3480, because the scoped round added its four held refusals to an existing test rather than a new one. Against `068c23a5`'s 3475, that is this branch's five new tests. `scripts/warnings` → exit 0, `measured at : 41b10f98 (clean)`, `0 warnings`. The Skills suites under `--filter 'SkillPack|Skill'` → exit 0, `Test run with 79 tests in 5 suites passed after 8.218 seconds` at `41b10f98`, clean tree. The fix round's figures, at `94dede5b`, were the same three: 3480, 0 and 79. They are history now. The commit that adds this entry changes only these two record files, so the figures describe it too: `git rev-parse --verify --quiet` on `Sources`, `Tests`, `Package.swift` and `server` gives one tree each at `41b10f98` and at that commit, and the scoped-round comments carry the proof with its controls. No file under `server/` changed (`git diff --quiet 8f3d1d02 41b10f98 -- server` exits 0), so the server half owes nothing, and no Postgres was started. `server/scripts/check-secrets.sh`, `scripts/no-attribution tree` and `scripts/changelog-order` were run over the tree that carries this entry, and their exits are in those comments.
Mutation plan: mutation/plans/chore/the-skills-follow-ups.txt (founder-triggered, not run on this branch). It has 22 mutants (`grep -c '^>>> mutant ' mutation/plans/chore/the-skills-follow-ups.txt` → 22 at `41b10f98`).
- A1 to A3 hold the read-date check and each of its two halves.
- W1 to W3 undo the widened match's three parts: host labels, runs inside a slug, and the case cut. W4 and W5 widen it past the two near misses the tests hold on purpose, `/registered-users` and a site whose own name is one of the words.
- K1 reads a query key by runs again, which refuses Snov's address. K2 reads keys not at all, and K3 drops a route's own query. K4 reads a key as one word again, without the cut at `/` and `.`, which loads `index.php?/register`: the scoped round's regression, which came from a widening that looked like a narrowing and needs a mutant of its own.
- P1 and P2 drop each new pairing, and P3 adds a pairing nobody read.
- D2 is a data mutant: Notion's pack names a sign-in page its row does not. D3 puts eBay's Watchlist flow back under its one `product` record.
- F1 to F3 remove each of the first-step check's three questions, and F4 drops the step's text from a finding line. The check lives in the test file, so its held samples are what must kill them.
- S1 returns the site rule to the last two labels.
- **Every plan was checked, not only the ones this branch touched.** Every `from` block in all 30 plans under `mutation/plans/` matches exactly once at `41b10f98` (384 mutants), except U3 and U6 in `feature/skills.txt`, which were already stale at `8f3d1d02` and are SONNY-532's. **SONNY-510's M6 and M14 were anchored on the line this round removed**, and both are re-anchored with their meaning unchanged: the fragment is not read, and the declared URL's query is not read. The check was a script that counts each `from` block's occurrences in its file.

Behavior added:
- **The first-step check runs in the suite** (SONNY-529). `everyFlowIsJudgedAtItsOwnFirstStep` asks three things of every shipped flow, with round five's own two patterns word for word:
  - a `product` record that more than one flow starts at;
  - a flow under a `product` record whose first step names a place a signed-out visitor may not reach;
  - a flow of any kind whose first step names account creation.
  A finding a person has read and judged is written into `judgedFirstStepFindings` with its reason, and the two sets must be equal both ways. Today that is Grok's question flow, for the reason round five gave.
  - **Each finding line carries the first step's whole text** (review-282's F5). Keyed on the matched words alone, a judged step rewritten to begin somewhere signed-in, with the same words, still passed; review-282 proved that with a probe. Now any edit to a judged step fails until someone reads it again. Grok's step is held word for word in `grokFirstStep`.
  - Its guard travelled with it: a `#require` that the walk loaded more than 100 packs, then counts showing the population it judges is not empty.
  - `theFirstStepCheckFindsEachShapeItExistsFor` holds the check on eBay's old shape (both of the first two questions), a flow that starts at "Sign up" (the third), and two controls that raise nothing. Every sample goes through `SkillPackDecoder` and then the same function the shipped packs meet.
  - Cross-checked against the command it replaces: at `8f3d1d02` round five's command prints `399 6 []` and then only Grok's line, which is exactly the judged set.
- **Microsoft 365 and Zoho Desk are deep again** (SONNY-529), with the flows they had at `0d645464` and a new start URL each. Both were read signed out on 2026-09-18, twice.
  - **Microsoft 365.** `https://www.microsoft365.com/login` lands on `login.microsoftonline.com/common/oauth2/v2.0/authorize`, "Sign in to your account", with "No account? Create one!" as a separate route. The record's heading is "Sign in", a visible `role="heading"` div, which SONNY-510's ruling of 03:27:28Z counts. The first round wrote it empty (review-282's F6). New pairing: `login.microsoftonline.com` → `microsoft365.com`.
  - **Microsoft 365's sign-in returns to `https://www.microsoft365.com/landingv2`** (review-282's F7, settled by its delta pass). Three frame-counted readings by review-282 at 17:07:56Z, 17:08:07Z and 17:08:42Z on 2026-09-19 each carried that `redirect_uri`. This lane's reading at 03:28:07Z had carried it too. *As history:* one earlier reading, review-282's at about 05:09Z, carried `https://m365.cloud.microsoft/landingv2`. It did not reproduce, including when the delta pass repeated that reading's order, bare address first and then `/login`. The fix round recorded both as a disagreement, which was right while there was no third reading. Either way, the sign-in returns to Microsoft 365's home and not to office.com.
  - **Zoho Desk.** `https://desk.zoho.com/agent` lands on `accounts.zoho.com/signin`, "Zoho Accounts". It reads "Sign in to access Desk" in a generic element, not a heading of any kind, which review-282 confirmed, so the record's heading stays empty. "Don't have a Zoho account? Sign up now" is a separate route, and sign-in returns to `desk.zoho.com/agent`. New pairing: `accounts.zoho.com` → `zoho.com`.
  - The population test derives both pairings from the shipped files and still equals the list.
- **Microsoft 365's sign-in page is its `/login`** (review-282's F3). The pack and its row named `https://www.microsoft365.com/`, which redirects to `m365.cloud.microsoft`, "Microsoft 365 - Sign into Copilot", a marketing page on another domain. The first round had described exactly that page when it rejected the address for Lists. Both now name `https://www.microsoft365.com/login`, the pack's own start URL.
- **Midjourney stays shallow, on the rule applied explicitly** (SONNY-529).
  - `www.midjourney.com` lands on `/home`, an explore page.
  - Its "Log In" button opens a dialog headed "Log in", with Continue with Google and Continue with Discord (03:22:19Z on 2026-09-19, which is 2026-09-18 in the founders' zone).
  - Its "Sign Up" button opens one headed "Sign up", with the same two buttons (03:23:07Z).
  - So the form that signs in is the form that creates an account, the shape Fireflies and Framer were held for, and the page is held. Making images needs an account, so `product` does not fit either.
  - Review-282 re-read both dialogs on screen, and the judgement holds.
- **A start page's `read` must be a real date, no earlier than 2026-09-17** (SONNY-529). `2026-13-45`, `2026-02-30`, `2027-02-29` and `1970-01-01` no longer load. The floor is the day SONNY-510 was filed and its first readings are dated, so an earlier date is a reading that could not have happened. There is no upper bound, because the loader runs at every launch and a Mac with a wrong clock would refuse every pack read after its today.
- **The account-creation words match inside a slug, in a query value, and as a host's first labels** (SONNY-529). A part is cut at `-`, `_` and a lowercase-to-uppercase boundary, and every run of its words is looked up. So `/signup-free`, `/register-now`, `/sign-up-free`, `/SignUpNow`, `/create-account-now`, `#/free_signup`, `?intent=signup_free` and `signup.<site>` are refused. The four words themselves did not change.
  - **A query key is read in whole pieces, and only a value is read by runs** (review-282's F4, the founders' ruling on PR #282). The query, the URL's own and a single-page route's after a `?` in its fragment, is read as keys and values. A value is read by runs. A key is cut at `/` and `.`, and each piece is read whole. So `?signup` and `?Sign-Up=1` are still refused, and Snov's own sign-in address, with `signup_source=landing`, loads, because `signup_source` has no `/` or `.` and so stays one piece. The query was read at all for values that state an intent (`?mode=signup`, Auth0's `?screen_hint=signup`), and those are values. The tracking parameters it caught live in the keys.
  - **The cut at `/` and `.` is the scoped round's fix** (review-282's delta pass at `ef36f2fd`). The fix round read each key as one word. That also stopped reading a key that is itself a path: an app that routes without URL rewriting puts the page's path in the query as its only key. So `index.php?/register`, `index.php?/auth/signup`, `?/signup` and `?user.register=1`, all refused on `main` and at `88aa3453`, loaded.
    - **The harness shows it with each head's own matcher.** All four are refused by `8f3d1d02`'s and `88aa3453`'s, load under `ef36f2fd`'s, and are refused again under this round's. Snov's address loads under `ef36f2fd`'s and this round's, and is refused under `88aa3453`'s.
    - **The four are held in the suite,** beside the other key cases in `aStartPageNamingAccountCreationInsideASlugOrAHostDoesNotLoad`, and mutant K4 removes the cut.
  - **Measured over every address the packs carry, with the real matcher.** The first round measured the 346 URLs the start pages record, and every one of those had already been read and accepted, so zero was the only possible answer (review-282's F4). This round compiled `SkillPack.swift`, `SkillPackStartPages.swift`, `SkillPackContentRules.swift` and `SearchTextNormalization.swift` into a scratch harness beside the base's matcher, copied from `8f3d1d02`. The harness reads every pack file as raw JSON; its source is on SONNY-529's fix-round comment. At `94dede5b`, and again at `41b10f98` after the scoped round, over the 577 distinct flow-start and `signInURL` addresses:
    - the base's matcher refuses 2, and the new one refuses 3;
    - 1 address is newly refused, and 0 are refused less;
    - with every recorded start page and landing added, 630 addresses, the answer is the same.
    - The newly refused one is Trello's sign-in, `id.atlassian.com/login?application=trello--direct-signup&…`, refused for a run in a value. It is off Trello's own site, so it can never be a start page.
    - The two both refuse are Zoho Books' and Zoho CRM's sign-in addresses, whose `signupurl` value names a `/signup` path. Both are on `accounts.zoho.com`, off their packs' sites.
    - Controls: it finds `/signup-free` and `?intent=signup_free`, and not `?signup_source=landing` or `/registered-users`. Pointed at a folder with no packs it prints `read 0 packs` and exits 1.
    - At `88aa3453`, with that head's own matcher compiled the same way, it reads 579 addresses and newly refuses 2, Snov's and Trello's. That reproduces review-282's figure. Its control for `?signup_source=landing` answers true there and false at `94dede5b`.
  - **Known refusals, held on purpose** (review-282's F4 and the ruling's second half). `knownRefusalsOfTheWiderMatchAreHeld` holds the following, and `SkillPackStartPageRule.accountCreationParts`' doc comment lists them. Each fails closed, and a narrowing that frees one is a change somebody has to make on purpose. None of the first three groups is an address a shipped pack carries:
    - `/event-registration`, `/domain-registration`, `/Register-Domain`, `/account/register-device`, `/un-register`, `/de-register` and `/newsletter-signup`;
    - `?next=/register-success`, `?utm_campaign=signup-q3` and `?ref=signup_page`;
    - `www.register.co.uk`.
    **Trello's shipped sign-in address is the exception, held last.** It is refused for a run in a value, and it can never be a start page because it is off Trello's own site.
  - **Rejected, and why.** A match on a word's prefix catches `/SignUpFree` with no cut in it, but it refuses `/registered-users`, which is not account creation. `join` stays out as a host label, because Zoom's `join.zoom.us` is a real start URL and joins a meeting.
- **The refusal names the missing pairing, and its name says so** (SONNY-529). `landedOnUnpairedHost(url:host:site:)` is now `landingHostNotPairedWithSite(url:host:site:)`, because the host is often listed and only its pairing with this site is missing. Microsoft 365's landing on `login.microsoftonline.com` was exactly that case, refused while that host was listed for Outlook and Teams.
- **The population test's site rule handles a country's second level** (SONNY-529, from review-272b's delta pass). `registrableSite(of:)` takes the last three labels when the two before the country code are a second level names are sold under, such as `co.uk`, `com.au` or `co.jp`, and the last two otherwise. `aPacksSiteIsItsRegistrableDomainEvenUnderACountrysSecondLevel` holds ten samples, because no shipped pack sits on such a domain: 0 of 473 at `94dede5b`, by the command under pitfalls. Review-282 showed the population test live in both directions with a `.co.uk` pack.
- **`signInURL` is authoritative and the row mirrors it** (SONNY-524). This is the settled reading, and the reasons are below. `everyShippedPackLoadsAndEveryOneIsARowOfTheCommittedCatalogue` now asserts `sign_in_url` equals the pack, beside `domain` and `category`. An empty cell is a pack with no sign-in page.
- **Three sign-in addresses corrected** (SONNY-526), in the pack and the row together, each confirmed by review-282's frame-counted readings of 17:10 to 17:11Z on 2026-09-19:
  - **Bigin:** `https://bigin.com/login` landed on `www.bigin.com/login/`, "Bigin - Page Not Found". Bigin's own "Sign In" links open Zoho's sign-in. It is now `https://accounts.zoho.com/signin?servicename=ZohoBigin`, which lands on `accounts.zoho.com/signin`, "Zoho Accounts", with "Create account" separate.
  - **Instagram:** `https://instagram.com/login` landed on "Profile isn't available • Instagram". A made-up path, `instagram.com/zzqnotauser529x/`, returns the same title, so `/login` is read as a user name, which is Chatwork's shape. It is now `https://www.instagram.com/accounts/login/`, titled "Instagram", with a password field and "Create new account" separate.
  - **Midjourney:** `https://midjourney.com/login` is a 404, as SONNY-510 found. The site has no sign-in address, only the "Log In" control above, so it is now `https://www.midjourney.com/`, the page that control is on.

Behavior preserved (required, no blanket claims):
- **The start-page rule's other checks are byte-for-byte the same.** These are the host check, the identity-host sign-in-only check, the landing's query checks and the record-per-URL checks. SONNY-510's plan matches in full at `94dede5b`, with M6 and M14 re-anchored.
- **Every URL that loaded before still loads, and nothing it refused is refused less.** The measurement above prints 0 refused less over 630 addresses. The class the delta pass found, query-routed paths and dotted keys, is refused again, and the four are held.
  - Every part the base read whole is still read at least whole: a path part and a route part by runs, a query value by runs, and a query key in pieces cut at `/` and `.`. The base split a key at those characters too, before folding.
  - `aStartPageWhosePathNamesAccountCreationDoesNotLoad` is unchanged and passes, including `/registered-users` and `/join` as near misses.
  - **This bullet was false at `ef36f2fd` for that class, and no shipped address showed it.** A measurement over the addresses the packs carry cannot see a shape none of them has, so the class has to be probed as a class.
- **Dates that loaded still load.** Every shipped record says `2026-09-17` or `2026-09-18`, and the fixtures say `2026-09-18`.
- **The identity-host list keeps its nine hosts' existing pairings**, adds one site to one host, and adds one new host. `theIdentityHostListIsExactlyTheLandingsTheShippedPacksMake` still reads its expected answer from the raw files and never from the list.
- **Guidance changes only where a sign-in address or a flow changed:** 9 packs' `signInURL`, and Microsoft 365's and Zoho Desk's flows. The Skills page shows no sign-in address, depth or flows, so it does not change.
- **The catalogue changed in one column only.** 16 rows' `sign_in_url` cells differ from `8f3d1d02`, checked by a script comparing every other cell of every row too (under pitfalls). The four rows the pack lanes hold (contactout, wise, expo and railway) and the 60 rows of groups 9, 10 and 11 are untouched.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **SONNY-524: the pack decides, the row mirrors it, and a test holds them equal.** The ticket offered two readings, and the evidence decided between them.
  - *They are one fact.* Both say which page Sonny should sign in on. The catalogue is not a frozen research record either: `domain` and `category` were already held equal to the pack, and the `doc_url` cells change whenever a lane reads a page. So a `sign_in_url` that disagrees is stale, not history.
  - *The pack is the one that decides*, because it is what reaches the planner ("Sign-in page: …"), and it is where a person puts a correction after opening the page. The row was the seed, and SONNY-526 is the measurement of what a seed can be worth: 3 of the 69 were wrong, and one of them, Instagram's, is a user-name page.
  - *So the answer was never "copy one over the other".* Each of the 15 was settled on a reading, and the reading decides which value wins:
    - **11 rows take the pack's value:** the six Google packs, Postman, Notion, Carrd, Cursor and v0.
    - **Outlook and Outlook Calendar** take `https://outlook.office365.com` in the pack. It lands on the same Microsoft sign-in page as the bare value, and it returns to Outlook's mail, where the bare value returns to office.com. For Calendar that is one click away rather than in it (review-282's F7).
    - **Teams takes `https://teams.microsoft.com`** in the pack (review-282's F1). It reaches Microsoft's sign-in after a splash of 8 to 18 seconds and returns to Teams. The first round kept the bare value on a reading that said it never got there, and that reading was taken in a window Chrome was not drawing.
    - **Microsoft Lists takes `https://lists.live.com/`** in both (review-282's F2). That is the pack's own start URL. It lands on the same `login.live.com` sign-in and returns to Lists, while `login.live.com/` returns nowhere. The first round had compared only the two values in front of it.
  - *The equality test cannot tell a right value from a wrong one.* Review-282's point stands: at the first round's head it held the worse Teams value in both places. It is the equality rule working on a wrong input, and the input is a reading. So each reconciled value carries its reading, and a reading from an undrawn window is not one.
  - *Where a reader meets it:* `SkillPack.signInURL`'s doc comment, the type's paragraph on the catalogue, and the equality assertion's comment. The TSV itself can carry no note. Its first line is its header, the test asserts it exactly, and every reader of the file takes any other line for a row.
  - *What it costs the pack lanes in flight:* a lane that changes a pack's `signInURL` must change its row in the same branch, or the suite fails. That fails closed, where someone sees it.
- **A reading counts only in a window Chrome is drawing** (SONNY-510's rule of 2026-09-19T06:13Z). This branch's Teams reading is one of the cases behind it, and so is Pinterest's, found by review-282's delta pass. The rule also names Lists, but Lists' wrong value came from comparing only two values, not from the window.
  - *In the first round*, the window reported `document.visibilityState` as `hidden`. Heavy pages never rendered (Linear, Podio, SonarCloud) or froze the renderer for 45 seconds (Prezi), and screenshots timed out. After the founder brought a window forward at about 03:07Z, several of those pages rendered and were read, but the tab still reported `hidden`. Teams, read at 03:27Z, sat on its splash, and that negative became a wrong value.
  - *The positives stand, and the negatives from that window did not.* A page that reached a sign-in form was read. Two negatives were wrong:
    - Teams, which "never reached" sign-in;
    - Pinterest, recorded as an empty page and so unreadable.
    Drawn, Pinterest's `/login` lands on `www.pinterest.com/login/`, "Pinterest Login": "Welcome to Pinterest", email and password, "Log in", Continue with Google and QR code, and a separate "New to Pinterest? Join for free" (review-282, 250 frames, 17:09:43Z). Review-282's frame-counted re-reads of 17:06 to 17:11Z confirm every other value it checked.
  - *In the fix round*, no reading could be taken at all. The tab measured **0 animation frames in 2.5 seconds** at 06:26:50Z and again, in a new window, at 14:51:55Z. `visibilityState` read `hidden` throughout, and three requests to bring the window forward did not reach the window the tab was in. So the three corrected values rest on:
    - review-282's readings of 2026-09-19, frame-counted in its delta pass at 17:07:11Z (Teams, 251 frames), 17:07:33Z (Lists, 252) and 17:07:56Z to 17:08:42Z (Microsoft 365, three reads);
    - for Teams and Lists, their own start-page records of 2026-09-18, which land on the same sign-in pages;
    - for Microsoft 365, this lane's 03:28Z reading, which did reach sign-in.
    Nothing this round claims to have read anything.
  - **The test for a drawn window is animation frames, not `visibilityState` alone.** Count `requestAnimationFrame` callbacks for a second. A page that is being painted fires dozens, and one that is not fires none, however it answers the other questions.
- **A made-up path is the control for a sign-in address.** Instagram's `/login` looked like a page, "Profile isn't available", until a nonsense path returned the same title. Pylon's evidence had the same hole, the other way round (next item).
- **A correction to the record: Pylon's `/signup` evidence proves nothing.** SONNY-510's entry says `app.usepylon.com/signup` "goes back to the sign-in page". So does a made-up path: `app.usepylon.com/zzq-no-such-page-529` lands on `app.usepylon.com/signin`, "Sign In – Pylon" (03:31:08Z), as `/signup` does (03:30:59Z). So the bounce is what the app does with any path, not evidence about sign-up. Pylon's pass under decision A rests on what holds: its homepage offers "Book a demo" and "Talk to Us", and `/pricing` redirects to `/schedule-demo`, "Book a Demo | Pylon", re-read here. That entry is left verbatim, as dated records are, and this is the correction.
- **A correction to this entry's own first round: "the 150 other rows ending in `/login`" was a number with no command, and it was wrong.** The first command below prints 227 rows ending in `/login` or `/login/` at `8f3d1d02`, 73 of them the exact form, which leaves 154. That is the coordinator's figure on SONNY-526.
- **eBay says `product` and Etsy says `sign-in`, and both are right.** The word is judged at each flow's own first step, not at the page's shape. eBay's one flow begins in the search box, which works signed out. Etsy's two begin in Your account and Shop Manager. `SkillPackStartPage`'s doc comment now says so where the word is defined, so a reader comparing the two records is told why before concluding one is wrong.
- **A mutation plan's anchor goes stale when another branch moves the line, and nothing says so until the battery runs.** It happened here in both directions:
  - Two mutants in `mutation/plans/feature/skills.txt` already matched nothing at `8f3d1d02`: U3 on `SkillPackContentRules.swift`, and U6 on `SkillPack.swift`, whose own-domain check SONNY-510 moved into `isOnSite(host:domain:)`. They are filed as SONNY-532.
  - This branch's fix round removed the line SONNY-510's M6 and M14 anchored on, and re-anchored them in the same change.
  - The check that finds either is cheap: count each `from` block in its file, over every plan, not only the plans a branch expects to touch.
- **Browser-reading pitfalls met here** (the window pitfall is its own item above):
  - The Chrome extension holds a claude.ai session of its own. `claude.ai/login` showed "Sign in - Claude" for about three seconds, then moved to `/new`. So Claude's address could not be read signed out, and it is left as it is.
  - **Pinterest was wrong in this entry until the scoped round.** It said `/login/` rendered an empty document on both reads, and that review-282 found the same on screen. Both readings came from windows that were not drawing, and review-282's first pass was not frame-counted. Drawn, it is a sign-in page, and the guessed address is right (above). SONNY-510 also recorded Pinterest empty "on every read", and its pack went shallow in that sweep, which may be the same cause. That lead is the coordinator's, not this branch's.
  - **A slip, recorded rather than left out.** On Midjourney, a click meant for the page's "Sign Up" button landed on "Continue with Discord" in the "Log in" dialog, which had not closed. It opened Discord's own sign-in page, signed out, in a new tab. Nothing was typed, and the tab was closed at once. The "Sign Up" reading was then taken by pressing that button through a script. SONNY-510's method note of 04:37Z makes the check that prevents it a rule: re-read what is under a click after the page changes state.
- **The commands behind this entry's figures**, each run from the repository root over a `git archive` of the SHA named:
  - The sign-in address population: `python3 -c "import csv;R=list(csv.DictReader(open('docs/sonny-skill-sites.tsv'),delimiter='\t'));print(sum(r['sign_in_url'] in ('https://'+r['domain']+'/login','https://'+r['domain']+'/login/') for r in R))"`. It prints 74 at `0d645464`, 73 at `8f3d1d02` (Chatwork's row had been corrected in between) and 68 at `94dede5b`. With the `/login/` form dropped it prints 70 at `0d645464`, so the reviewer's 70 and the coordinator's 74 were both right, about two different shapes: with and without the trailing slash. With `r['sign_in_url'].endswith(('/login','/login/'))` in place of the exact test it prints 227 at `8f3d1d02`.
  - The pack-and-row mismatch, SONNY-510's own command: `15` at `8f3d1d02` and `0` at `94dede5b`.
  - The first round's widening measurement, kept as history: a Python replica of the first round's match over the start-page URLs only, which printed `342 281 0 0` at `8f3d1d02` and `346 284 0 0` at `ba390bb8`. Review-282 reproduced it with the Swift code. It is superseded by the compiled harness above, because its population could only answer zero.
  - The deep population, SONNY-510's first command: `187 77 403 162` at `94dede5b`, against `187 77 399 160` at `068c23a5`. That is 2 packs and 4 flows back, none of them from a bare origin.
  - Multi-label domains: `python3 -c "import json,glob;P=[json.load(open(p)) for p in glob.glob('Sources/MacAgent/Resources/SkillPacks/'+'*.skillpack.json')];assert len(P)>100;L=[d['domain'] for d in P if len(d['domain'].split('.'))>=3 and len(d['domain'].split('.')[-1])==2 and d['domain'].split('.')[-2] in {'co','com','net','org','ac','gov','edu','ne','or','go','ltd','plc'}];print(len(P),len(L))"` prints `473 0` at `94dede5b`.
  - The catalogue compared cell by cell: `python3 -c 'import csv,subprocess,sys,io;r=lambda s:list(csv.DictReader(io.StringIO(subprocess.check_output(["git","show",s+":docs/sonny-skill-sites.tsv"]).decode()),delimiter="	"));A,B=r(sys.argv[1]),r(sys.argv[2]);assert len(A)==len(B)>100 and [a["id"] for a in A]==[b["id"] for b in B];D=[(a["id"],k) for a,b in zip(A,B) for k in a if a[k]!=b[k]];print(len(A),len(D),sorted({k for _,k in D}),sorted({i for i,_ in D}))' 8f3d1d02 94dede5b` prints `473 16 ['sign_in_url'] [...]`. The 16 are the 11 SONNY-524 rows that took the pack's value, Microsoft Lists, Microsoft 365 and SONNY-526's three; Teams is back to the value it had at `8f3d1d02`. Its control, `0d645464` against `8f3d1d02`, prints 70 differences over five columns, so it can see a column other than this one.

Known limitations / deferred scope:
- **One of the 69 could not be read:** Claude, because of the extension's session. It is left as it was. Pinterest was first recorded here as unreadable. Drawn, it reads as a sign-in page, and its address is right; its correction is on SONNY-526.
- **The 154 other rows ending in `/login` were not opened.** The coordinator ruled on SONNY-526 on 2026-09-19. There is no sweep. The 89 on shallow packs fold into the pack-lane method, and the 65 on deep packs become a small bounded job.
- **The first-step check reads words.** A first step that names a signed-in place in other words passes it. That is why the judged set carries its reason and its step, and why a person still reads each `product` flow.
- **The site rule is a rule, not the public-suffix list.** A country whose second level is not in its set gets two labels, and the population test fails, naming the host and the site it derived. The loader's host check does not share the rule, so `www.register.co.uk` is a known refusal (above).
- **The account-creation match still misses a joined-up slug with no case cut**, such as `/signupfree`. That is deliberate: catching it takes a prefix match, and a prefix match refuses `/registered-users`.
- **Microsoft 365's and Zoho Desk's flow steps were not re-read.** They are as they stood at `0d645464`, restored the way SONNY-510 restored OneDrive's, with only the start URL new.
- **Microsoft 365's flows are admin-center work** (review-282's F9, recorded rather than routed). `microsoft_365_admin` exists for `admin.microsoft.com`, whose `/login` lands on a host already paired with `microsoft.com`. Whoever next deepens that pack could start those flows where the work starts. The ticket asked for Microsoft 365 to come back as it was.

Open questions (required, write "none" if true):
- **SONNY-524 was settled by this lane.** The ticket asked the implementing session to settle the reading, and the reasons are above. The founders can reverse it by deleting one assertion.

Next branch: none named.
