### Branch: chore/a-row-can-say-it-was-read-from-the-site
Status: complete
Date: 2026-09-17
Tickets: **SONNY-501**. The catalogue's `task_flow_docs` column gains a third value, `site`, meaning a row's task flows were read off the live site rather than out of the site's public documentation; the 54 rows that said `shallow` move to it, the validating test widens by exactly that one value, and no pack's depth changes. Founder decision 2026-09-16: every skill pack is deep, which those 54 sites could not be while their rows stood. Wave 10 (SONNY-463), cut from `main` at `3bf70677`, independent of every other lane of the wave — no stack. **Three hops onto `main` afterwards**, each forced by other lanes landing rather than by anything on this branch: onto `325e8ee8` (PR #256), onto `c3bd9d8e` (PR #259) and onto `4683468f` (PRs #257, #258 and #261). Every pre-hop SHA below is labelled history and fails the ancestry check by design.
Reviewed by: a fresh session (review-260) on PR #260 at `3fd9987f` — a pre-hop head, non-ancestral today by design, whose replay after three rebases is `23d14a25` — posted in full there. It re-proved the widening with six mutants of its own rather than reading this branch's — `shallow`, an empty cell, `Site`, a trailing space and `site_read` all refused, only `site` passing — and reproduced both comparisons with controls. Four findings, none in the code. **F1** (writing): the rule had landed as two complete statements with every pointer naming the doc comment as its home, while the CLAUDE.md copy was the longer of the two — the two-copies shape this repository keeps being caught by. **F2**: that bullet claimed the *Claims and evidence* clean-zero family while sitting 26 lines past the section. **F3**: `taskFlowEvidence` was held by nothing — removing `shallow` from it passed all 30 tests — because one use read the set under test as a loop's own population and the other was redundant with the tally. **F4**: all 54 `site` rows name no page and nothing carried that obligation forward, which matters more for `site` than for `deep` because a browser reading leaves no artifact to re-open. All four taken; F4 was built rather than recorded as a reminder. **Then a scoped delta pass**, after the first hop: it rebuilt the conflict resolution from scratch with all four sides read out of git, added the control this session had missed (the same silent revert applied to *this branch's* side, since a resolution can lose either side's work), and found three counts inside the tree still saying 95 after the rebase moved the population to 103 — the rebase rule biting in the quiet direction, where each sentence's argument stays true while its population moves. Fixed by taking the numeral out of the arguments rather than by updating it.

Spec sections covered: none directly. This is the Skills catalogue's own evidence column and the test that validates it.
Files changed:
- `docs/sonny-skill-sites.tsv`: 54 cells in `task_flow_docs`, `shallow` to `site`. Nothing else in the file.
- `Tests/MacAgentCoreTests/SkillPackTests.swift`: `evidenceForADeepPack` and `depthProblem(_:taskFlowDocs:pages:)` in the Helpers section; the shipped-pack loop and `theCommittedCatalogueIsTheListTheFoundersDecided` read them; one new test, `aDeepPackNeedsDocumentedOrSiteReadFlowsAndAShallowPackSitsOnAnyRow`.
- `Sources/MacAgentCore/SkillPack.swift`: one paragraph in the type's doc comment giving the column's three values and what the validating test allows, and one sentence naming — not restating — the rule for claiming `site`. Comment only; no code.
- `CLAUDE.md`: one bullet inside *Claims and evidence*, in the clean-zero family, since a page that serves a scripted client nothing under HTTP 200 is a member of it. **This is the live statement of that rule.**
- `mutation/plans/chore/a-row-can-say-it-was-read-from-the-site.txt`, this entry, and `docs/manual-tests/chore/a-row-can-say-it-was-read-from-the-site.md`.
Tests: the flagged command from `CLAUDE.md` -> `Test run with 3441 tests in 250 suites passed after 91.577 seconds with 8 known issues.`, **exit 0** at `0fd42535`, read from a redirected run with nothing between the command and `$?`; `grep -cE 'Test [A-Za-z]+\(.*\) (failed|recorded an issue)'` over the log answers **0**, and the same pattern over a synthetic failure line answers 1, which is the control. `scripts/warnings` -> **exit 0, `0 warnings.`**, its own header reading `measured at : 0fd42535 (clean)`, every file compiled, 125s. `scripts/no-attribution tree` -> exit 0, 0 of 1257 tracked files (4 excluded by path). `npm run check:secrets` -> exit 0, `clean (1261 tracked files scanned, 12 patterns, 8 baselined fixtures)`. `scripts/changelog-order` -> exit 0 over this entry. **Every figure here is measured at `0fd42535`**, the head this entry commits on top of; the entry and the manual-test file are the only things above it, and they touch `docs/changelog/` and `docs/manual-tests/` alone, so `Sources/`, `Tests/` and `Package.swift` are one hash each at `0fd42535` and at the head that merges (`WORKFLOW.md` step 5's tree-identity proof, with `git diff --name-only` over those three paths printing nothing and the same command without the path list printing the two new files as its control).
Mutation plan: `mutation/plans/chore/a-row-can-say-it-was-read-from-the-site.txt` (founder-triggered, not run on this branch). Seven mutants; `scripts/mutate <plan> --check` -> exit 0, seven `1 match` lines at `0fd42535`. W1 to W3 widen the value check past the two allowed values or drop it outright, W4 reads the pack's depth the wrong way round, W5 drops the page condition, and C1 and C2 are data mutants that make every row read as `shallow` and as naming no page — the wiring proofs, since a mutant of a test's own assertion survives on every tree where the property holds.

Behavior added:
- **A catalogue row can say its flows were read from the live site**, and a pack on such a row may be deep. Nothing else loosens: a row saying neither `deep` nor `site` still keeps every pack on it shallow.
- **A deep pack's row must name at least one page its flows were read from** (review-260's F4), checked by the same `depthProblem` the 473-pack loop and the held samples both travel. A documentation reading leaves an artifact anybody can re-open; a browser reading leaves none, so for a `site` row the `doc_url` cells are the only place that evidence exists. The cell must name an openable page rather than carry a note, so a lane cannot satisfy a rule about missing evidence by typing "read in a browser" into the column.

Behavior preserved (required, no blanket claims):
- **Every pack's depth is exactly what it was.** 473 packs at `4683468f` and at `0fd42535`, identical ids, **0** depths moved, 143 deep and 330 shallow at both. The control flips one depth and adds an id, and the comparison reports both. **That pair has read 95/378, then 103/370, then 143/330 at this branch's three bases** (`3bf70677`, `325e8ee8`, `c3bd9d8e` and now `4683468f`), every step of it PR #256's, #257's, #258's and #261's work and none of it this branch's — which is the whole claim: the tally moves under the branch and the branch moves none of it.
- **Every other catalogue cell is exactly what it was.** All 473 rows by 11 columns compared cell by cell against `4683468f`: 54 differing cells, all `task_flow_docs`, all `shallow` to `site`, with header, row order and id set identical. The control plants two cells in two other columns and the same comparator reports 56.
- **A deep pack on a `shallow` row still fails**, shown with a mutant rather than argued, in both directions on the real population — and the review re-proved it with six of its own, including the empty cell and the near misses.
- The rest of `SkillPackTests` is untouched and runs as before: the money and credential rules, the trigger check, the start-page rule, the one-pack-per-row check and the shipped-packs-folder scan.
- **`main`'s own packs still pass the new page condition**, checked before each hop rather than after: **143 deep packs at `4683468f`, 0 whose row names no page**, with the 54 rows that name none counted as the control so the zero is not a blind reader. It was checked the same way at `325e8ee8` (103, 0) and, while they were still open, at each of the four sibling branch heads then in flight (117, 101, 107 and 95 deep packs, 0 each) — three of which are now inside that 143. So this branch's requirement does not turn `main` red when it lands, measured at every base it has had rather than argued once.

Architectural decisions / pitfalls discovered (required, write "none" if true):
- **The shipped population can no longer hold the value narrowing, and a reader has to be told.** With 0 rows saying `shallow`, the loop over every pack has nothing to refuse: it passes identically with the rule and without it. So the held samples are the whole guard for that half, they travel the same `depthProblem` the loop calls rather than asserting against literals (SONNY-388's lesson), and the plan's mutants aim there. The page condition is the other way round — every shipped deep pack sits on a row naming a page, so the loop holds that one on a live population.
- **Every loop in the samples writes its values out rather than reading a declaration under test.** A loop over `evidenceForADeepPack`, or over a list of the column's words, agrees with whatever that declaration says, including a widening. There was such a list, `taskFlowEvidence`: review-260's F3 measured that removing `shallow` from it passed all 30 tests, because one use read it as a loop's population and the other was redundant with the tally, which refuses an unknown word by counting. Dropped rather than pinned.
- **A rule with two copies is a rule with one stale copy, and nothing in a file says which one a reader got** (review-260's F1). The rule for claiming `site` was written twice, in full, with every pointer running toward the doc comment while the longer copy sat in `CLAUDE.md`. `CLAUDE.md` is the live statement now and the doc comment names it, measured by flattening both files — a line-based grep misses the phrases a doc comment wraps across two `///` lines, which is the trap `CLAUDE.md`'s own line-wrap bullet describes.
- **A count inside an argument goes stale under a rebase while the argument stays true, and nothing in the branch's diff says so.** Three sentences here said "95 deep packs" and stayed correct in substance after a hop made it 103; the verification figures were re-measured and these were not, because they read as prose rather than as figures. The remedy taken is not a bigger number: the arguments no longer rest on the numeral at all, and the size sits in a parenthesis beside the command that produced it — `grep -rl '"depth": "deep"' Sources/MacAgent/Resources/SkillPacks | wc -l` -> 143 at `0fd42535`, against 330 for the `shallow` twin and 0 for a depth no pack has, that last being the control saying the pattern can come back empty. **The remedy then had to work twice**, which is the evidence that it is one: the next update from `main` moved the same count from 103 to 143, and the cost was re-taking two parentheses rather than rewriting three arguments.
- **A catalogue conflict between lanes is adjacency, not disagreement, and it is resolved row-wise with controls rather than by hand-editing the hunk.** PR #256 changed 8 rows and this branch 54, with an empty intersection, and git still conflicted because `gumroad` sits directly above `ghost`; the third hop brought three merges' worth at once — **40 rows from PRs #257, #258 and #261 against this branch's 54, six conflicted hunks, intersection still empty and still only `doc_url` columns against `task_flow_docs`**. The resolution rebuilds the file from all three sides read out of git — each row from whichever side changed it, a row both sides changed **refused** rather than guessed — and is checked by four arms with three controls that each exit 2: a dropped row, a row silently reverted to its base value, and a row neither side touched. **The revert control is the one that matters**, because no row count can see a row put back. The delta pass added a fourth: the same revert applied to this branch's side, since a resolution can lose either side's work and only one direction was covered. All four are part of the check now, and on the third hop all four fired — the own-side revert catching `adp` — before the clean run was believed. **Reusing the same merger across three hops is what made the third one cheap**: 40 rows in, 54 out, 379 untouched, and the reading of each is a command rather than a judgement.

Known limitations / deferred scope: the 54 `site` rows' `doc_url` cells are still empty, and the suite now requires them filled the day a pack on one of those rows becomes deep. That is the pack lanes' work (SONNY-502 to SONNY-505 and the waves after them), which their tickets already require; this branch opens the door and changes no pack.
Open questions: none.

Next branch: the wave's pack groups (SONNY-502 onward), which this unblocks for the 54.
