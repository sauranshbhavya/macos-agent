### Branch: chore/an-entry-is-a-file-of-its-own
Status: complete
Date: 2026-09-16
Tickets: **SONNY-500** — a branch writes its changelog entry and its manual-test rows in files of its own, `scripts/changelog-order` reads the directories, and `WORKFLOW.md` returns to branches cut from `main`. This is wave 10's first branch and merges before any other lane writes an entry.
Reviewed by: pending — a fresh session on the PR.

Spec sections covered: none. Repository tooling and the process records around it.

Files changed, 13 (`git diff --name-only origin/main HEAD | wc -l` → 13 at `279d4dd1`):

- `scripts/changelog-order` (+609 −83) — reads `docs/changelog/` and `docs/manual-tests/` beside the archive, and gained `read` and `manual-tests`.
- `.claude/hooks/verify-tests-before-stop.sh` and its selftest — the trigger reaches both directories; four new arms drive faults that exist only there.
- `docs/changelog/README.md`, `docs/manual-tests/README.md` — the live template and the live rules, new.
- `docs/changelog/chore/an-entry-is-a-file-of-its-own.md` (this file), `docs/manual-tests/chore/an-entry-is-a-file-of-its-own.md` (no rows owed, and why).
- `WORKFLOW.md` — step 2's manual-item bullet, step 3's merge bullet, four paragraphs of step 7, and `## Stacked pull requests` rewritten as `## Pull requests in flight`.
- `CLAUDE.md` — the pointer to the history, the archive pointer, the manual-test pointer, the stack bullet, and one new gotcha (below).
- `README.md`, `.claude/agents/sonny-code-reviewer.md` — the two other pointers at the changelog.
- `docs/sonny-v1-implementation-changelog.md`, `docs/sonny-manual-test-checklist.md` — a note at the head of one section each, saying the file is the archive. **Nothing else in either moved**: the whole branch removes exactly two lines from the two of them, and both are the section headings it replaced with amended versions of themselves (`git diff origin/main HEAD -- docs/sonny-v1-implementation-changelog.md docs/sonny-manual-test-checklist.md | grep -E '^-[^-]'` → 2 lines at `279d4dd1`, `## Where a manual item lives (convention, added 2026-08-26)` and `## Entry Template (v2, 2026-08-02)`). Entries 223 → 223, checklist rows 614 → 614, checklist `###` sections 88 → 88, each counted on both sides of the diff.

Tests: no Swift or server suite is owed, and that is a measurement rather than a judgement. `Sources`, `Tests`, `server` and `Package.swift` are byte-identical between `origin/main` and `279d4dd1` — `git rev-parse --verify --quiet "origin/main:<p>"` against `"HEAD:<p>"` for each, four IDENTICAL; the same loop over `docs`, `WORKFLOW.md`, `CLAUDE.md` and `scripts` prints four MOVED and over `nosuchdir` prints REFUSED, so the check is shown able to produce all three answers before any of them is believed. `git diff --name-only origin/main HEAD -- Sources Tests server Package.swift` → 0 lines, the same claim through a second instrument. What was run, each exit read with nothing between it and the command, at `279d4dd1`:

- `scripts/changelog-order selftest` → exit 0, **44 case(s), 0 failure(s)**.
- `.claude/hooks/verify-tests-before-stop-selftest.sh` → exit 0, **24 case(s), 0 failure(s)**.
- `scripts/changelog-order` → exit 0 (re-run at the head that merges, because this entry is one of its inputs; the PR body and SONNY-500's closing comment carry that reading).
- `scripts/no-attribution tree` → exit 0, 0 of 1248 tracked files. **1244 before this branch's four new files were tracked**, which is the control that says the scan read them.
- `server/scripts/check-secrets.sh` → exit 0, clean, 1252 tracked files scanned. 1248 before, same control.

Mutation plan: none, for the reason `chore/tooling-reads-fresh-main-and-records-catch-up`'s entry gives about the same tool: a plan whose `>>> file` paths sit outside `server/` runs under `scripts/mutate-all` against the flagged Swift suite, which never runs this script, so every mutant would report SURVIVED with no test having seen it and a weekly battery would file one false ticket per mutant. **Eleven mutants were hand-applied instead**, each disabling one arm, each run through the whole `scripts/changelog-order selftest` at `279d4dd1`: the unmerged-count check widened (kills 2 cases), the era filter disabled (3), the entry directory's contribution to the unmerged count (1), the heading/path arm (1), the non-`.md` arm (1), the stale-boundary guard (1), the `sys.stdout.flush()` (1), and the "does not exist" arm in each of the three loops separately (1, 1 and 2). Every one of those ten is killed only by the cases naming the arm it disabled. **The eleventh is the one worth recording, and it is not in that list**: it made every entry file's name read as an unmerged branch rather than a nonexistent one, and it **survived the whole 44-case selftest**. The pitfall two sections down is what it found; re-run against the repaired cases it fails, which is what says the repair holds rather than that the mutant was weak.

Behavior added:

- **A branch writes `docs/changelog/<branch-name>.md` and `docs/manual-tests/<branch-name>.md`**, a slash in the branch name becoming a folder as `mutation/plans/` already does it. Two branches therefore never write to a file the other writes to. **Demonstrated rather than asserted**, in a throwaway clone at `279d4dd1`: two branches each adding both of their files share **0 paths** (`comm -12` over each branch's `git diff --name-only <base> <branch>`) and merge with exit 0 and 0 unmerged paths; **the same two branches under the old shape**, each inserting at the top of `## Entries` and at the end of the checklist's §7, share **2 paths** and the merge exits 1 with both files unmerged. The control is the half that matters — a clean merge proves nothing unless the same demonstration can produce a conflict.
- **`scripts/changelog-order read` and `scripts/changelog-order manual-tests`** assemble every branch's file newest-first, by merge commit, with whatever has not merged at the top, and then the archive behind it. That is the one documented way to read the history in order, named in `WORKFLOW.md` step 7, in `CLAUDE.md`'s pointer, in `README.md`, in the reviewer agent and in both directories' `README.md`. Each prints, as its second line, how many per-branch files it assembled and which archive follows them — a count that cannot silently lose a file, which is the whole reason it is printed. No line total is quoted for either, deliberately: this entry is itself part of what `read` prints, so a number taken here would measure a population its own file belongs to (`CLAUDE.md`, Claims and evidence, the ninth defect).
- **The check's arms**, with the three the ticket names shown firing in that same clone: an entry naming a branch that has no merge and no ref anywhere is a finding at any count (exit 2, 1 finding, naming `docs/changelog/feature/tpyo.md`); a *second* entry naming a branch with a ref and no merge is a finding, since exactly one may — the branch under review (exit 2, 1 finding naming both, and 1 finding naming all three when a third is added); and a branch that merged in the one-file-per-branch era with no file is a finding per directory (exit 2, 1 finding for the entry, 2 when the manual-test file goes as well). Beside them, the clean runs: a branch cut from `main` with its own entry present, exit 0; the era begun with two branches merged in it and both records present, exit 0.
- **The stop hook follows**, shown the same way: on a branch that wrote both its records, exit 0 with nothing on either channel; on the same branch with its entry file deleted, exit 2 with the finding, the mainline line and the era line quoted in order.
- **Every run prints where it resolved the era boundary** — `the one-file-per-branch era begins at PR #N (<branch>), mainline position P`, or `has not begun` — because a boundary that stopped resolving would switch the completeness arm off with no output changing at all.

Behavior preserved (required, no blanket claims):

- **The archive's two-era ordering check**, unchanged in code and still exercised by four selftest cases (a newest-first entry out of order, an oldest-first one, an entry appended at the file's end, the era boundary going missing). It is kept deliberately: that file takes no new entries, so it can no longer go red on honest work, and what it still catches is a session appending an entry there out of habit — which raises no rebase conflict, so the clean merge is the tell.
- **Which mainline gets read** (SONNY-480) and every refusal around it: five ref cases, the diverged refusal's 200-byte fit inside the hook's quote, the `git merge-base` failure told apart from divergence, and reason-then-mainline ordering. All still pass, untouched.
- **`check <path>`** still reads the path it is given, which is how a finding count is quoted as evidence and re-derived by a reviewer. The two directories are read from the repository either way, and the docstring says so.
- **The hook's two battery states**: a live battery still skips both checks and says so three ways, an abandoned mutant still blocks ahead of everything. The literal phrase `test suite was NOT run` that `scripts/mutate selftest` asserts against this file is untouched, and the word `passed` appears nowhere in the hook (`grep -n 'passed' .claude/hooks/verify-tests-before-stop.sh` exits 1 at `279d4dd1`), which is the other thing that arm asserts.
- **Every citation into the archives still resolves.** `git grep -l 'sonny-v1-implementation-changelog' -- Sources Tests server` → **5** files at `279d4dd1`, and the same command for `sonny-manual-test-checklist` → **6**; every one is a doc comment or a test comment pointing at an entry or a section that has not moved, which is the whole reason the archives were left whole. `README.md` names both too, and its sentence was updated to name the command as well as the file.

Architectural decisions / pitfalls discovered (required, write "none" if true):

- **The archive was left whole rather than split, and the reading tools are what made that the cheaper answer rather than merely the lazier one.** Splitting 223 entries would rewrite 17,393 lines in one commit — a diff nobody can review by reading, which is the review this repository actually runs — and would move every byte that the citations above already reach. Against that, the only thing the split would buy is uniformity. The tools do not need it: `read` concatenates the directory and the archive into one stream, so a reader sees one document either way, and the archive's ordering arms keep working over a file that can no longer change, which turns them from a check on honest work into a guard against one specific mistake. **The archive is, in effect, a third era of the same document**, and this tool already had the concept — it has been telling the file's two eras apart since SONNY-225.
- **With one file per branch there is no position to get wrong, so the ordering arm has nothing to check in the new era — and a check that silently has nothing to do is the failure this repository keeps being caught by.** What replaces it is the completeness arm: a branch that merged and wrote no record. That fault is exactly as quiet as a misplaced entry was — nothing else in the tree goes red on a missing file — and it has already happened once, on PR #124, found only because the outgoing coordinator read two branches side by side (`WORKFLOW.md` step 7's record of it). The arm needs a boundary, so `DIRECTORY_ERA_BRANCH` in the script names this branch and every merge at or after it owes both files; the roughly thirty branches that merged before the rule existed and wrote nothing are the archive's and are not checked.
- **That boundary is a constant naming a branch, which is a clean zero waiting to happen**, since a rename would leave the arm vacuous forever while every run still exits 0. Two things answer it: the resolution is printed on every run, clean or not, and an entry naming a branch that *has* merged while the boundary has *not* is itself a finding — the exact symptom a stale constant produces.
- **A branch that owes no entry now writes one line saying so, instead of writing nothing.** It is the only way the completeness arm can exist at all, and it closes the gap `WORKFLOW.md` step 7 already records: #125 and #126 rightly wrote no entry, #124 wrongly wrote none, and nothing but a human comparison could tell those two apart. The alternative — a shared opt-out list — is a file every branch would edit, which is the coupling this whole ticket removes.
- **`git rev-parse <rev>:<path>` echoes its argument when the path does not resolve, so a tree-identity loop guarded on an empty answer reports IDENTICAL for a path that exists in neither tree.** `git rev-parse "HEAD:nosuchdir"` exits 128 and prints `HEAD:nosuchdir` on stdout; `--verify --quiet` exits 1 and prints nothing. This is a new member of the family `CLAUDE.md`'s Claims and evidence section already holds — the zsh `:s` modifier that answers a commit SHA for `$sha:server`, and the loop variable named `path` that destroys `PATH` — and it is worse than both in one respect: it was found by running that bullet's *own* recommended control, which says to refuse an empty answer, on a guard that had one. The bullet now carries it, and the rule it lands on is to read each side's **exit code** rather than the emptiness of its output.
- **The tool's own selftest shipped this repository's shared-marker gotcha, and a deliberately bad mutant is what found it.** Three loops emit the phrase `names a branch that does not exist` — the archive's, the entry directory's and the manual directory's — and three cases asserted the phrase bare, so disabling any one loop left all three green: the mutant that made every entry-file name read as an unmerged branch **survived a 44-case selftest**. That is `CLAUDE.md`'s "a source scan's `contains(token)` over a whole region is satisfied by any of the sites that share the token", arriving in a test file rather than a source scan. The two directory cases are anchored on their own paths now and the archive's on the wording only it produces, and three separate mutants — one per loop — each fail only the case or cases belonging to the loop it disabled: the entry directory's fails one, the manual directory's fails one, the archive's fails both archive cases.
- **Python buffers stdout and does not buffer stderr, so a tool whose findings go to stdout and whose summary goes to stderr comes out backwards in any caller that merges the two.** The stop hook captures this tool with `2>&1`, and before the fix the hook's transcript opened with `1 finding(s).` above the mainline line, the era line and the finding it counted — undoing, in the merged stream, exactly what PR #243's F1 established for stdout alone. One `sys.stdout.flush()` fixes it and a selftest arm reads the merged stream the way the hook does; removing the flush fails that arm, which is how the arm was shown to hold.

Known limitations / deferred scope:

- **The manual-tests directory gets two arms and no others: completeness, and the name that matches no merge and no ref.** It does not get a structural arm, a heading-matches-path arm, or a place in the one-unmerged-entry count — a manual-test file is prose and rows whose shape is the checklist's convention rather than this tool's, and inventing a format for it is a rule this ticket was not asked to create. So a stale manual-test file for a branch that has a ref and never merged is accepted, where the matching *entry* file would be a finding. The two arms it does get are the two that need no format.
- **`docs/sonny-manual-test-checklist.md` is deliberately absent from the stop hook's trigger list.** The tool reads it when it assembles the checklist for a reader and never when it checks, so a diff touching it alone cannot produce a finding and running the tool over that diff could only ever say "clean". The hook's comment says so where a reader of the list will find it.
- **`README.md` at a directory's root is skipped by exact name, and only there.** `docs/changelog/feature/README.md` would still be read as an entry for a branch called `feature/README`, which nothing creates; a rule broad enough to skip it would be a rule that quietly hides a real file. A selftest case pins the skip.

Open questions (required, write "none" if true): none.

Next branch: the rest of wave 10, each cut from `main` and merged as its own review closes.
