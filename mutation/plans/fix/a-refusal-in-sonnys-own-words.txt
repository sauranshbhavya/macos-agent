# SONNY-447 — a refused request shows this repository's sentence, never the planner's. Property:
# the error's description is the fixed sentence whatever the reason; the reason stays on the error
# and reaches the run's act log once; the sentence names no mechanism.
# Killers, each measured by applying the mutant by hand at the fix round's tree (PR #232, 2026-09-11):
# U1 by UnsupportedRefusalTests, AgentRunnerTests.aRefusedPlanThrowsSonnysOwnSentenceAndKeepsThePlannersReason
# and BackendTaskIdentityTests.aRefusalReadsSonnysSentenceOnEverySurfaceTheUserSees; U2 by the runner's
# two refusal tests; U3 by UnsupportedRefusalTests, the surface test and
# AgentRunnerTests.aRefusedPlanThrowsSonnysOwnSentenceAndKeepsThePlannersReason, which checks the literal
# sentence (the delta pass named the third); U4 by
# AgentRunnerTests.aRefusedPlansReasonReachesTheActLog. The unified-log line beside the throw has no
# mutant and no test: nothing in this suite reads the unified log, so its privacy setting is unguarded.

>>> mutant U1 the planner's reason rendered again
>>> file Sources/MacAgentCore/AgentActionExecutor.swift
>>> from
        case .unsupported:
            // **This repository's words, whatever the planner wrote** (SONNY-447). The reason used
>>> to
        case .unsupported(let detail):
            return detail
            // **This repository's words, whatever the planner wrote** (SONNY-447). The reason used
>>> end

>>> mutant U2 the reason dropped from the error: the act-log line carries nothing and the error loses its payload
>>> file Sources/MacAgentCore/AgentActionExecutor.swift
>>> from
            throw AgentExecutionError.unsupported(unsupported.description)
>>> to
            throw AgentExecutionError.unsupported("")
>>> end

>>> mutant U3 the sentence names the mechanism
>>> file Sources/MacAgentCore/AgentActionExecutor.swift
>>> from
    public static let unsupportedRequestSentence = "Sonny can't do that yet."
>>> to
    public static let unsupportedRequestSentence = "Unsupported: no registered tool can do that yet."
>>> end

>>> mutant U4 the act-log line dropped: the trace no longer says why (PR #232's fresh review, F1 and F5)
>>> file Sources/MacAgentCore/AgentRunner.swift
>>> from
            logStore.append(.observe, "The planner refused this request: \(reason)")
            throw AgentExecutionError.unsupported(reason)
>>> to
            throw AgentExecutionError.unsupported(reason)
>>> end
